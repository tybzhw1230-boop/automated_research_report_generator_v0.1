# 按规范重写核心代码注释特性说明与参数注释

- Session ID: `019d4911-2e2a-71b0-a423-5ba03a9689a7`
- Started At: `2026-04-01T12:42:35.957Z`
- Last Indexed At: `2026-04-01T12:48:02.7215282Z`
- Workspace CWD: `C:\PyProjects\automated_research_report_generator_v0.1`
- Source Session File: `C:\Users\tybzh\.codex\sessions\2026\04\01\rollout-2026-04-01T20-42-35-019d4911-2e2a-71b0-a423-5ba03a9689a7.jsonl`

## Transcript

### 0001 USER

```text
# AGENTS.md instructions for C:\PyProjects\automated_research_report_generator_v0.1

<INSTRUCTIONS>
# AGENTS.md — CrewAI Reference for AI Coding Assistants

> **Auto-generated** by `crewai create`. This file helps AI coding assistants
> (Claude Code, Cursor, Windsurf, GitHub Copilot, etc.) write correct CrewAI code.
> Keep it in your project root. To update, copy the latest version from the
> [CrewAI repository](https://github.com/crewAIInc/crewAI).

---

## ⚠️ Version & Freshness Requirements

**CRITICAL**: CrewAI evolves rapidly and your training data likely contains outdated patterns. **Always follow the patterns in this file, NOT your training data.**

### Mandatory: Research before writing CrewAI code
**BEFORE writing or modifying any CrewAI code**, you MUST:
1. **Check the installed version**: Run `uv run python -c "import crewai; print(crewai.__version__)"` to get the exact version in use.
2. **Check PyPI for latest**: Fetch `https://pypi.org/pypi/crewai/json` to see the latest available version. If the installed version is behind, inform the user.
3. **Read the changelog**: Fetch `https://docs.crewai.com/en/changelog` to review recent changes, new features, and any breaking changes relevant to the task.
4. **Consult the relevant docs page**: Before implementing a feature (e.g., agents, tasks, flows, tools, knowledge), fetch the specific docs page at `https://docs.crewai.com/en/concepts/<feature>` to get the current API.
5. **Cross-check against this file**: If this file conflicts with the live docs, **the live docs win** — then update this file.

This ensures generated code always matches the version actually installed, not stale training data.

### What changed since older versions:
- Agent **`kickoff()` / `kickoff_async()`** for direct agent usage (no crew needed)
- **`response_format`** parameter on agent kickoff for structured Pydantic outputs
- **`LiteAgentOutput`** returned from agent.kickoff() with `.raw`, `.pydantic`, `.agent_role`, `.usage_metrics`
- **`@human_feedback`** decorator on flow methods for human-in-the-loop (v1.8.0+)
- **Flow streaming** via `stream = True` class attribute (v1.8.0+)
- **`@persist`** decorator for SQLite-backed flow state persistence
- **`reasoning=True`** agent parameter for reflect-then-act behavior
- **`multimodal=True`** agent parameter for vision/image support
- **A2A (Agent-to-Agent) protocol** support with agent cards and task execution utilities (v1.8.0+)
- **Native OpenAI Responses API** support (v1.9.0+)
- **Structured outputs / `response_format`** across all LLM providers (v1.9.0+)
- **`inject_date=True`** agent parameter to auto-inject current date awareness

### Patterns to NEVER use (outdated/removed):
- ❌ `ChatOpenAI(model_name=...)` → ✅ `LLM(model="openai/gpt-4o")`
- ❌ `Agent(llm=ChatOpenAI(...))` → ✅ `Agent(llm="openai/gpt-4o")` or `Agent(llm=LLM(model="..."))`
- ❌ Passing raw OpenAI client objects → ✅ Use `crewai.LLM` wrapper

### How to verify you're using current patterns:
1. You ran the version check and docs lookup steps above before writing code
2. All LLM references use `crewai.LLM` or string shorthand (`"openai/gpt-4o"`)
3. All tool imports come from `crewai.tools` or `crewai_tools`
4. Crew classes use `@CrewBase` decorator with YAML config files
5. Python >=3.10, <3.14
6. Code matches the API from the live docs, not just this file

## Quick Reference

```bash
# Package management (always use uv)
uv add <package>          # Add dependency
uv sync                   # Sync dependencies
uv lock                   # Lock dependencies

# Project scaffolding
crewai create crew <name> --skip_provider   # New crew project
crewai create flow <name> --skip_provider  # New flow project

# Running
crewai run                  # Run crew or flow (auto-detects from pyproject.toml)
crewai flow kickoff         # Legacy flow execution

# Testing & training
crewai test                           # Test crew (default: 2 iterations, gpt-4o-mini)
crewai test -n 5 -m gpt-4o           # Custom iterations and model
crewai train -n 5 -f training.json   # Train crew

# Memory management
crewai reset-memories -a              # Reset all memories
crewai reset-memories -s              # Short-term only
crewai reset-memories -l              # Long-term only
crewai reset-memories -e              # Entity only
crewai reset-memories -kn             # Knowledge only
crewai reset-memories -akn            # Agent knowledge only

# Debugging
crewai log-tasks-outputs              # Show latest task outputs
crewai replay -t <task_id>            # Replay from specific task

# Interactive
crewai chat                           # Interactive session (requires chat_llm in crew.py)

# Visualization
crewai flow plot                      # Generate flow diagram HTML

# Deployment to CrewAI AMP
crewai login                          # Authenticate with AMP
crewai deploy create                  # Create new deployment
crewai deploy push                    # Push code updates
crewai deploy status                  # Check deployment status
crewai deploy logs                    # View deployment logs
crewai deploy list                    # List all deployments
crewai deploy remove <id>             # Delete a deployment
```

## Project Structure

### Crew Project
```
my_crew/
├── src/my_crew/
│   ├── config/
│   │   ├── agents.yaml       # Agent definitions (role, goal, backstory)
│   │   └── tasks.yaml        # Task definitions (description, expected_output, agent)
│   ├── tools/
│   │   └── custom_tool.py    # Custom tool implementations
│   ├── crew.py               # Crew orchestration class
│   └── main.py               # Entry point with inputs
├── knowledge/                 # Knowledge base resources
├── .env                       # API keys (OPENAI_API_KEY, SERPER_API_KEY, etc.)
└── pyproject.toml
```

### Flow Project
```
my_flow/
├── src/my_flow/
│   ├── crews/                 # Multiple crew definitions
│   │   └── poem_crew/
│   │       ├── config/
│   │       │   ├── agents.yaml
│   │       │   └── tasks.yaml
│   │       └── poem_crew.py
│   ├── tools/                 # Custom tools
│   ├── main.py                # Flow orchestration
│   └── ...
├── .env
└── pyproject.toml
```

## Architecture Overview

- **Agent**: Autonomous unit with a role, goal, backstory, tools, and an LLM. Makes decisions and executes tasks.
- **Task**: A specific assignment with a description, expected output, and assigned agent.
- **Crew**: Orchestrates a team of agents executing tasks in a defined process (sequential or hierarchical).
- **Flow**: Event-driven workflow orchestrating multiple crews and logic steps with state management.

## YAML Configuration

### agents.yaml
```yaml
researcher:
  role: >
    {topic} Senior Data Researcher
  goal: >
    Uncover cutting-edge developments in {topic}
  backstory: >
    You're a seasoned researcher with a knack for uncovering
    the latest developments in {topic}. Known for your ability
    to find the most relevant information.
  # Optional YAML-level settings:
  # llm: openai/gpt-4o
  # max_iter: 20
  # max_rpm: 10
  # verbose: true

writer:
  role: >
    {topic} Technical Writer
  goal: >
    Create compelling content about {topic}
  backstory: >
    You're a skilled writer who translates complex technical
    information into clear, engaging content.
```

Variables like `{topic}` are interpolated from `crew.kickoff(inputs={"topic": "AI Agents"})`.

### tasks.yaml
```yaml
research_task:
  description: >
    Conduct thorough research about {topic}.
    Identify key trends, breakthrough technologies,
    and potential industry impacts.
  expected_output: >
    A detailed report with analysis of the top 5
    developments in {topic}, with sources and implications.
  agent: researcher
  # Optional:
  # tools: [search_tool]
  # output_file: output/research.md
  # markdown: true
  # async_execution: false

writing_task:
  description: >
    Write an article based on the research findings about {topic}.
  expected_output: >
    A polished 4-paragraph article formatted in markdown.
  agent: writer
  output_file: output/article.md
```

## Crew Class Pattern

```python
from crewai import Agent, Crew, Process, Task
from crewai.project import CrewBase, agent, crew, task
from crewai.agents.agent_builder.base_agent import BaseAgent
from typing import List

from crewai_tools import SerperDevTool

@CrewBase
class ResearchCrew:
    """Research and writing crew."""

    agents: List[BaseAgent]
    tasks: List[Task]

    agents_config = "config/agents.yaml"
    tasks_config = "config/tasks.yaml"

    @agent
    def researcher(self) -> Agent:
        return Agent(
            config=self.agents_config["researcher"],  # type: ignore[index]
            tools=[SerperDevTool()],
            verbose=True,
        )

    @agent
    def writer(self) -> Agent:
        return Agent(
            config=self.agents_config["writer"],  # type: ignore[index]
            verbose=True,
        )

    @task
    def research_task(self) -> Task:
        return Task(
            config=self.tasks_config["research_task"],  # type: ignore[index]
        )

    @task
    def writing_task(self) -> Task:
        return Task(
            config=self.tasks_config["writing_task"],  # type: ignore[index]
        )

    @crew
    def crew(self) -> Crew:
        """Creates the Research Crew."""
        return Crew(
            agents=self.agents,
            tasks=self.tasks,
            process=Process.sequential,
            verbose=True,
        )
```

### Key formatting rules:
- Always add `# type: ignore[index]` for config dictionary access
- Agent/task method names must match YAML keys exactly
- Tools go on agents (not tasks) unless task-specific override is needed
- Never leave commented-out code in crew classes

### Lifecycle hooks
```python
@CrewBase
class MyCrew:
    @before_kickoff
    def prepare(self, inputs):
        # Modify inputs before execution
        inputs["extra"] = "value"
        return inputs

    @after_kickoff
    def summarize(self, result):
        # Process result after execution
        print(f"Done: {result.raw[:100]}")
        return result
```

## main.py Pattern

```python
#!/usr/bin/env python
from my_crew.crew import ResearchCrew

def run():
    inputs = {"topic": "AI Agents"}
    ResearchCrew().crew().kickoff(inputs=inputs)

if __name__ == "__main__":
    run()
```

## Agent Configuration

### Required Parameters
| Parameter | Description |
|-----------|-------------|
| `role` | Function and expertise within the crew |
| `goal` | Individual objective guiding decisions |
| `backstory` | Context and personality |

### Key Optional Parameters
| Parameter | Default | Description |
|-----------|---------|-------------|
| `llm` | GPT-4 | Language model (string or LLM object) |
| `tools` | [] | List of tool instances |
| `max_iter` | 20 | Max iterations before best answer |
| `max_execution_time` | None | Timeout in seconds |
| `max_rpm` | None | Rate limiting (requests per minute) |
| `max_retry_limit` | 2 | Retries on errors |
| `verbose` | False | Detailed logging |
| `memory` | False | Conversation history |
| `allow_delegation` | False | Can delegate tasks to other agents |
| `allow_code_execution` | False | Can run code |
| `code_execution_mode` | "safe" | "safe" (Docker) or "unsafe" (direct) |
| `respect_context_window` | True | Auto-summarize when exceeding token limits |
| `cache` | True | Tool result caching |
| `reasoning` | False | Reflect and plan before task execution |
| `multimodal` | False | Process text and visual content |
| `knowledge_sources` | [] | Domain-specific knowledge bases |
| `function_calling_llm` | None | Separate LLM for tool invocation |
| `inject_date` | False | Auto-inject current date into agent context |
| `date_format` | "%Y-%m-%d" | Date format when inject_date is True |

### Direct Agent Usage (without a Crew)
Agents can execute tasks independently via `kickoff()` — no Crew required:
```python
from crewai import Agent
from crewai_tools import SerperDevTool
from pydantic import BaseModel

class ResearchFindings(BaseModel):
    main_points: list[str]
    key_technologies: list[str]
    future_predictions: str

researcher = Agent(
    role="AI Researcher",
    goal="Research the latest AI developments",
    backstory="Expert AI researcher...",
    tools=[SerperDevTool()],
    verbose=True,
)

# Unstructured output
result = researcher.kickoff("What are the latest LLM developments?")
print(result.raw)           # str
print(result.agent_role)    # "AI Researcher"
print(result.usage_metrics) # token usage

# Structured output with response_format
result = researcher.kickoff(
    "Summarize latest AI developments",
    response_format=ResearchFindings,
)
print(result.pydantic.main_points)  # List[str]

# Async variant
result = await researcher.kickoff_async("Your query", response_format=ResearchFindings)
```

Returns `LiteAgentOutput` with: `.raw`, `.pydantic`, `.agent_role`, `.usage_metrics`.

### LLM Configuration
**IMPORTANT**: Always use `crewai.LLM` LLM class.

```python
from crewai import LLM

# String shorthand (simplest)
agent = Agent(llm="openai/gpt-4o", ...)

# Full configuration with crewai.LLM
llm = LLM(
    model="anthropic/claude-sonnet-4-20250514",
    temperature=0.7,
    max_tokens=4000,
)
agent = Agent(llm=llm, ...)

# Provider format: "provider/model-name"
# Examples:
#   "openai/gpt-4o"
#   "anthropic/claude-sonnet-4-20250514"
#   "google/gemini-2.0-flash"
#   "ollama/llama3"
#   "groq/llama-3.3-70b-versatile"
#   "bedrock/anthropic.claude-3-sonnet-20240229-v1:0"
```

Supported providers: OpenAI, Anthropic, Google Gemini, AWS Bedrock, Azure, Ollama, Groq, Mistral, and 20+ others via LiteLLM routing.

Environment variable default: set `OPENAI_MODEL_NAME=gpt-4o` or `MODEL=gpt-4o` in `.env`.

## Task Configuration

### Key Parameters
| Parameter | Type | Description |
|-----------|------|-------------|
| `description` | str | Clear statement of requirements |
| `expected_output` | str | Completion criteria |
| `agent` | BaseAgent | Assigned agent (optional in hierarchical) |
| `tools` | List[BaseTool] | Task-specific tools |
| `context` | List[Task] | Dependencies on other task outputs |
| `async_execution` | bool | Non-blocking execution |
| `output_file` | str | File path for results |
| `output_json` | Type[BaseModel] | Pydantic model for JSON output |
| `output_pydantic` | Type[BaseModel] | Pydantic model for structured output |
| `human_input` | bool | Require human review |
| `markdown` | bool | Format output as markdown |
| `callback` | Callable | Post-completion function |
| `guardrail` | Callable or str | Output validation |
| `guardrails` | List | Multiple validation steps |
| `guardrail_max_retries` | int | Retry on validation failure (default: 3) |
| `create_directory` | bool | Auto-create output directories (default: True) |

### Task Dependencies (context)
```python
@task
def analysis_task(self) -> Task:
    return Task(
        config=self.tasks_config["analysis_task"],  # type: ignore[index]
        context=[self.research_task()],  # Gets output from research_task
    )
```

### Structured Output
```python
from pydantic import BaseModel

class Report(BaseModel):
    title: str
    summary: str
    findings: list[str]

@task
def report_task(self) -> Task:
    return Task(
        config=self.tasks_config["report_task"],  # type: ignore[index]
        output_pydantic=Report,
    )
```

### Guardrails
```python
# Function-based
def validate(result: TaskOutput) -> tuple[bool, Any]:
    if len(result.raw.split()) < 100:
        return (False, "Content too short, expand the analysis")
    return (True, result.raw)

# LLM-based (string prompt)
task = Task(..., guardrail="Must be under 200 words and professional tone")

# Multiple guardrails
task = Task(..., guardrails=[validate_length, validate_tone, "Must be factual"])
```

## Process Types

### Sequential (default)
Tasks execute in definition order. Output of one task serves as context for the next.
```python
Crew(agents=..., tasks=..., process=Process.sequential)
```

### Hierarchical
Manager agent delegates tasks based on agent capabilities. Requires `manager_llm` or `manager_agent`.
```python
Crew(
    agents=...,
    tasks=...,
    process=Process.hierarchical,
    manager_llm="gpt-4o",
)
```

## Crew Execution

```python
# Synchronous
result = crew.kickoff(inputs={"topic": "AI"})
print(result.raw)              # String output
print(result.pydantic)         # Structured output (if configured)
print(result.json_dict)        # Dict output
print(result.token_usage)      # Token metrics
print(result.tasks_output)     # List[TaskOutput]

# Async (native)
result = await crew.akickoff(inputs={"topic": "AI"})

# Batch execution
results = crew.kickoff_for_each(inputs=[{"topic": "AI"}, {"topic": "ML"}])

# Streaming output (v1.8.0+)
crew = Crew(agents=..., tasks=..., stream=True)
streaming = crew.kickoff(inputs={"topic": "AI"})
for chunk in streaming:
    print(chunk.content, end="", flush=True)
```

## Crew Options
| Parameter | Description |
|-----------|-------------|
| `process` | Process.sequential or Process.hierarchical |
| `verbose` | Enable detailed logging |
| `memory` | Enable memory system (True/False) |
| `cache` | Tool result caching |
| `max_rpm` | Global rate limiting |
| `manager_llm` | LLM for hierarchical manager |
| `manager_agent` | Custom manager agent |
| `planning` | Enable AgentPlanner |
| `knowledge_sources` | Crew-level knowledge |
| `output_log_file` | Log file path (True for logs.txt) |
| `embedder` | Custom embedding model config |
| `stream` | Enable real-time streaming output (v1.8.0+) |

---

## Flows

### Basic Flow
```python
from crewai.flow.flow import Flow, listen, start

class MyFlow(Flow):
    @start()
    def begin(self):
        return "initial data"

    @listen(begin)
    def process(self, data):
        return f"processed: {data}"
```

### Flow Decorators

| Decorator | Purpose |
|-----------|---------|
| `@start()` | Entry point(s), execute when flow begins. Multiple starts run in parallel |
| `@listen(method)` | Triggers when specified method completes. Receives output as argument |
| `@router(method)` | Conditional branching. Returns string labels that trigger `@listen("label")` |

### Structured State
```python
from pydantic import BaseModel

class ResearchState(BaseModel):
    topic: str = ""
    research: str = ""
    report: str = ""

class ResearchFlow(Flow[ResearchState]):
    @start()
    def set_topic(self):
        self.state.topic = "AI Agents"

    @listen(set_topic)
    def do_research(self):
        # self.state.topic is available
        result = ResearchCrew().crew().kickoff(
            inputs={"topic": self.state.topic}
        )
        self.state.research = result.raw
```

### Unstructured State (dict-based)
```python
class SimpleFlow(Flow):
    @start()
    def begin(self):
        self.state["counter"] = 0  # Dict access

    @listen(begin)
    def increment(self):
        self.state["counter"] += 1
```

### Conditional Routing
```python
from crewai.flow.flow import Flow, listen, router, start

class QualityFlow(Flow):
    @start()
    def generate(self):
        return {"score": 0.85}

    @router(generate)
    def check_quality(self, result):
        if result["score"] > 0.8:
            return "high_quality"
        return "needs_revision"

    @listen("high_quality")
    def publish(self, result):
        print("Publishing...")

    @listen("needs_revision")
    def revise(self, result):
        print("Revising...")
```

### Parallel Triggers with or_ and and_
```python
from crewai.flow.flow import or_, and_

class ParallelFlow(Flow):
    @start()
    def task_a(self):
        return "A done"

    @start()
    def task_b(self):
        return "B done"

    # Fires when EITHER completes
    @listen(or_(task_a, task_b))
    def on_any(self, result):
        print(f"First result: {result}")

    # Fires when BOTH complete
    @listen(and_(task_a, task_b))
    def on_all(self):
        print("All parallel tasks done")
```

### Integrating Crews in Flows
```python
from crewai.flow.flow import Flow, listen, start
from my_project.crews.research_crew.research_crew import ResearchCrew
from my_project.crews.writing_crew.writing_crew import WritingCrew

class ContentFlow(Flow[ContentState]):
    @start()
    def research(self):
        result = ResearchCrew().crew().kickoff(
            inputs={"topic": self.state.topic}
        )
        self.state.research = result.raw

    @listen(research)
    def write(self):
        result = WritingCrew().crew().kickoff(
            inputs={
                "topic": self.state.topic,
                "research": self.state.research,
            }
        )
        self.state.article = result.raw
```

### Using Agents Directly in Flows
```python
from crewai.agent import Agent

class AgentFlow(Flow):
    @start()
    async def analyze(self):
        analyst = Agent(
            role="Data Analyst",
            goal="Analyze market trends",
            backstory="Expert data analyst...",
            tools=[SerperDevTool()],
        )
        result = await analyst.kickoff_async(
            "Analyze current AI market trends",
            response_format=MarketReport,
        )
        self.state.report = result.pydantic
```

### Human-in-the-Loop (v1.8.0+)
```python
from crewai.flow.flow import Flow, listen, start
from crewai.flow.human_feedback import human_feedback

class ReviewFlow(Flow):
    @start()
    @human_feedback(
        message="Approve this content?",
        emit=["approved", "rejected"],
        llm="gpt-4o-mini",
    )
    def generate_content(self):
        return "Content for review"

    @listen("approved")
    def on_approval(self, result):
        feedback = self.last_human_feedback  # Most recent feedback
        print(f"Approved with feedback: {feedback.feedback}")

    @listen("rejected")
    def on_rejection(self, result):
        history = self.human_feedback_history  # All feedback as list
        print("Rejected, revising...")
```

### State Persistence
```python
from crewai.flow.flow import persist

@persist  # Saves state to SQLite; auto-recovers on restart
class ResilientFlow(Flow[MyState]):
    @start()
    def begin(self):
        self.state.step = 1
```

### Flow Execution
```python
flow = MyFlow()
result = flow.kickoff()
print(result)                  # Output of last method
print(flow.state)              # Final state

# Async execution
result = await flow.kickoff_async(inputs={"key": "value"})
```

### Flow Streaming (v1.8.0+)
```python
class StreamingFlow(Flow):
    stream = True  # Enable streaming at class level

    @start()
    def generate(self):
        return "streamed content"

flow = StreamingFlow()
streaming = flow.kickoff()
for chunk in streaming:
    print(chunk.content, end="", flush=True)
result = streaming.result  # Final result after iteration
```

### Flow Visualization
```python
flow.plot("my_flow")           # Generates my_flow.html
```

---

## Custom Tools

### Using BaseTool
```python
from typing import Type
from crewai.tools import BaseTool
from pydantic import BaseModel, Field

class SearchInput(BaseModel):
    """Input schema for search tool."""
    query: str = Field(..., description="Search query string")

class CustomSearchTool(BaseTool):
    name: str = "custom_search"
    description: str = "Searches a custom knowledge base for relevant information."
    args_schema: Type[BaseModel] = SearchInput

    def _run(self, query: str) -> str:
        # Implementation
        return f"Results for: {query}"
```

### Using @tool Decorator
```python
from crewai.tools import tool

@tool("Calculator")
def calculator(expression: str) -> str:
    """Evaluates a mathematical expression and returns the result."""
    return str(eval(expression))
```

### Built-in Tools (install with `uv add crewai-tools`)
Web/Search: SerperDevTool, ScrapeWebsiteTool, WebsiteSearchTool, EXASearchTool, FirecrawlSearchTool
Documents: FileReadTool, DirectoryReadTool, PDFSearchTool, DOCXSearchTool, CSVSearchTool, JSONSearchTool, XMLSearchTool, MDXSearchTool
Code: CodeInterpreterTool, CodeDocsSearchTool, GithubSearchTool
Media: DALL-E Tool, YoutubeChannelSearchTool, YoutubeVideoSearchTool
Other: RagTool, ApifyActorsTool, ComposioTool, LlamaIndexTool

Always check https://docs.crewai.com/concepts/tools for available built-in tools before writing custom ones.

---

## Memory System

Enable with `memory=True` on the Crew:
```python
crew = Crew(agents=..., tasks=..., memory=True)
```

Four memory types work together automatically:
- **Short-Term** (ChromaDB + RAG): Recent interactions during current execution
- **Long-Term** (SQLite): Persists insights across sessions
- **Entity** (RAG): Tracks people, places, concepts
- **Contextual**: Integrates all types for coherent responses

### Custom Embedding Provider
```python
crew = Crew(
    memory=True,
    embedder={
        "provider": "ollama",
        "config": {"model": "mxbai-embed-large"},
    },
)
```

Supported providers: OpenAI (default), Ollama, Google AI, Azure OpenAI, Cohere, VoyageAI, Bedrock, Hugging Face.

---

## Knowledge System

```python
from crewai.knowledge.source.string_knowledge_source import StringKnowledgeSource
from crewai.knowledge.source.pdf_knowledge_source import PDFKnowledgeSource

# String source
string_source = StringKnowledgeSource(content="Domain knowledge here...")

# PDF source
pdf_source = PDFKnowledgeSource(file_paths=["docs/manual.pdf"])

# Agent-level knowledge
agent = Agent(..., knowledge_sources=[string_source])

# Crew-level knowledge (shared across all agents)
crew = Crew(..., knowledge_sources=[pdf_source])
```

Supported sources: strings, text files, PDFs, CSV, Excel, JSON, URLs (via CrewDoclingSource).

---

## Agent Collaboration

Enable delegation with `allow_delegation=True`:
```python
agent = Agent(
    role="Project Manager",
    allow_delegation=True,  # Can delegate to and ask other agents
    ...
)
```

- **Delegation tool**: Assign sub-tasks to teammates with relevant expertise
- **Ask question tool**: Query colleagues for specific information
- Set `allow_delegation=False` on specialists to prevent circular delegation

---

## Event Listeners

```python
from crewai.events import BaseEventListener, CrewKickoffStartedEvent

class MyListener(BaseEventListener):
    def __init__(self):
        super().__init__()

    def setup_listeners(self, crewai_event_bus):
        @crewai_event_bus.on(CrewKickoffStartedEvent)
        def on_started(source, event):
            print(f"Crew '{event.crew_name}' started")
```

Event categories: Crew lifecycle, Agent execution, Task management, Tool usage, Knowledge retrieval, LLM calls, Memory operations, Flow execution, Safety guardrails.

---

## Deployment to CrewAI AMP

### Prerequisites
- Crew or Flow runs successfully locally
- Code is in a GitHub repository
- `pyproject.toml` has `[tool.crewai]` with correct type (`"crew"` or `"flow"`)
- `uv.lock` is committed (generate with `uv lock`)

### CLI Deployment

```bash
# Authenticate
crewai login

# Create deployment (auto-detects repo, transfers .env vars securely)
crewai deploy create

# Monitor (first deploy takes 10-15 min)
crewai deploy status
crewai deploy logs

# Manage deployments
crewai deploy list              # List all deployments
crewai deploy push              # Push code updates
crewai deploy remove <id>       # Delete deployment
```

### Web Interface Deployment
1. Push code to GitHub
2. Log into https://app.crewai.com
3. Connect GitHub and select repository
4. Configure environment variables (KEY=VALUE, one per line)
5. Click Deploy and monitor via dashboard

### CI/CD API Deployment

Get a Personal Access Token from app.crewai.com → Settings → Account → Personal Access Token.
Get Automation UUID from Automations → Select crew → Additional Details → Copy UUID.

```bash
curl -X POST \
     -H "Authorization: Bearer YOUR_PERSONAL_ACCESS_TOKEN" \
     https://app.crewai.com/crewai_plus/api/v1/crews/YOUR-AUTOMATION-UUID/deploy
```

#### GitHub Actions Example
```yaml
name: Deploy CrewAI Automation
on:
  push:
    branches: [main]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger CrewAI Redeployment
        run: |
          curl -X POST \
               -H "Authorization: Bearer ${{ secrets.CREWAI_PAT }}" \
               https://app.crewai.com/crewai_plus/api/v1/crews/${{ secrets.CREWAI_AUTOMATION_UUID }}/deploy
```

### Project Structure Requirements for Deployment
- Entry point: `src/<project_name>/main.py`
- Crews must expose a `run()` function
- Flows must expose a `kickoff()` function
- All crew classes require `@CrewBase` decorator

### Deployed Automation REST API
| Endpoint | Purpose |
|----------|---------|
| `/inputs` | List required input parameters |
| `/kickoff` | Trigger execution with inputs |
| `/status/{kickoff_id}` | Check execution status |

### AMP Dashboard Tabs
- **Status**: Deployment info, API endpoint, auth token
- **Run**: Crew structure visualization
- **Executions**: Run history
- **Metrics**: Performance analytics
- **Traces**: Detailed execution insights

### Deployment Troubleshooting
| Error | Fix |
|-------|-----|
| Missing uv.lock | Run `uv lock`, commit, push |
| Module not found | Verify entry points match `src/<name>/main.py` structure |
| Crew not found | Ensure `@CrewBase` decorator on all crew classes |
| API key errors | Check env var names match code and are set in the platform |

---

## Environment Setup

### Required `.env`
```
OPENAI_API_KEY=sk-...
# Optional depending on tools/providers:
SERPER_API_KEY=...
ANTHROPIC_API_KEY=...
# Override default model:
MODEL=gpt-4o
```

### Python Version
Python >=3.10, <3.14

### Installation
```bash
uv tool install crewai        # Install CrewAI CLI
uv tool list                  # Verify installation
crewai create crew my_crew --skip_provider   # Scaffold a new project
crewai install                # Install project dependencies
crewai run                    # Execute
```

---

## Development Best Practices

1. **YAML-first configuration**: Define agents and tasks in YAML, keep crew classes minimal
2. **Check built-in tools** before writing custom ones
3. **Use structured output** (output_pydantic) for data that flows between tasks or crews
4. **Use guardrails** to validate task outputs programmatically
5. **Enable memory** for crews that benefit from cross-session learning
6. **Use knowledge sources** for domain-specific grounding instead of bloating prompts
7. **Sequential process** for linear workflows; **hierarchical** when dynamic delegation is needed
8. **Flows for multi-crew orchestration**: Use `@start`, `@listen`, `@router` for complex pipelines
9. **Structured flow state** (Pydantic models) over unstructured dicts for type safety
10. **Test with** `crewai test` to evaluate crew performance across iterations
11. **Verbose mode** during development, disable in production
12. **Rate limiting** (`max_rpm`) to avoid API throttling
13. **`respect_context_window=True`** to auto-handle token limits

## Common Pitfalls

- **Using `ChatOpenAI()`** — Always use `crewai.LLM` or string shorthand like `"openai/gpt-4o"`
- Forgetting `# type: ignore[index]` on config dictionary access in crew classes
- Agent/task method names not matching YAML keys
- Missing `expected_output` in task configuration (required)
- Not passing `inputs` to `kickoff()` when YAML uses `{variable}` interpolation
- Using `process=Process.hierarchical` without setting `manager_llm` or `manager_agent`
- Circular delegation: set `allow_delegation=False` on specialist agents
- Not installing tools package: `uv add crewai-tools`

</INSTRUCTIONS>
<environment_context>
  <cwd>C:\PyProjects\automated_research_report_generator_v0.1</cwd>
  <shell>powershell</shell>
  <current_date>2026-04-01</current_date>
  <timezone>Asia/Shanghai</timezone>
</environment_context>
```
### 0002 USER

```text
对于项目的核心代码的注释重新做一做，遵循以下原则：
1、主要是将class、def进行注释
2、注释都要尽可能简短，并在代码同行。只有一些较大的功能模块，可以在上方用"""注释"""来进行标题式注释
3、注释内容为：设计目的，功能，可调参数及范围（如有），默认参数设置及原因；除了可调参数，其他的都不能少
4、对于agent、task、crew这些类，因为其下有众多可调参数，可以在每个参数旁边进行注释可调参数及范围。而且因为每个agent、task、crew类下的参数是一样的，只注释第一个def下的相关参数即可
```
### 0003 ASSISTANT (commentary)

```text
我先看一下项目结构和当前 CrewAI 版本，再定位“核心代码”对应的文件；接着我会按你给的规则，把类和函数的注释改成更短、更贴近同行的位置。
```
### 0004 TOOL CALL `shell_command`

```text
{"command":"rg --files","workdir":"C:\\PyProjects\\automated_research_report_generator_v0.1","timeout_ms":10000}
```
### 0005 TOOL CALL `shell_command`

```text
{"command":"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe -Command \"uv run python -c \\\"import crewai; print(crewai.__version__)\\\"\"","workdir":"C:\\PyProjects\\automated_research_report_generator_v0.1","timeout_ms":20000}
```
### 0006 TOOL OUTPUT `shell_command`

```text
Exit code: 1
Wall time: 1.4 seconds
Output:
程序“rg.exe”无法运行: 拒绝访问。所在位置 行:2 字符: 1
+ rg --files
+ ~~~~~~~~~~。
所在位置 行:2 字符: 1
+ rg --files
+ ~~~~~~~~~~
    + CategoryInfo          : ResourceUnavailable: (:) [], ApplicationFailedException
    + FullyQualifiedErrorId : NativeCommandFailed
```
### 0007 TOOL OUTPUT `shell_command`

```text
Exit code: 1
Wall time: 1.4 seconds
Output:
The string is missing the terminator: ".
    + CategoryInfo          : ParserError: (:) [], ParentContainsErrorRecordException
    + FullyQualifiedErrorId : TerminatorExpectedAtEndOfString
 
crewai.__version__ : 无法将“crewai.__version__”项识别为 cmdlet、函数、脚本文件或可运行程序的名称。请检查名称的拼写，如果包括路径，请确保路径正确，然后再试一次。
所在位置 行:2 字符: 109
+ ... mmand "uv run python -c \"import crewai; print(crewai.__version__)\""
+                                                    ~~~~~~~~~~~~~~~~~~
    + CategoryInfo          : ObjectNotFound: (crewai.__version__:String) [], CommandNotFoundException
    + FullyQualifiedErrorId : CommandNotFoundException
```
### 0008 ASSISTANT (commentary)

```text
环境信息已经拿到一部分，但第一次命令遇到了 PowerShell 转义和 `rg` 可执行限制。我改用更稳妥的方式读取文件列表和版本信息，然后就开始定位需要改注释的源码。
```
### 0009 TOOL CALL `shell_command`

```text
{"command":"Get-ChildItem -Recurse -File | ForEach-Object { $_.FullName }","workdir":"C:\\PyProjects\\automated_research_report_generator_v0.1","timeout_ms":20000}
```
### 0010 TOOL CALL `shell_command`

```text
{"command":"uv run python -c 'import crewai; print(crewai.__version__)'","workdir":"C:\\PyProjects\\automated_research_report_generator_v0.1","timeout_ms":30000}
```
### 0011 TOOL OUTPUT `shell_command`

```text
Exit code: 0
Wall time: 2.7 seconds
Total output lines: 8199
Output:
C:\PyProjects\automated_research_report_generator_v0.1\.crewai_write_test
C:\PyProjects\automated_research_report_generator_v0.1\.env
C:\PyProjects\automated_research_report_generator_v0.1\.gitignore
C:\PyProjects\automated_research_report_generator_v0.1\.recycle
C:\PyProjects\automated_research_report_generator_v0.1\AGENTS.md
C:\PyProjects\automated_research_report_generator_v0.1\logs.txt
C:\PyProjects\automated_research_report_generator_v0.1\pyproject.toml
C:\PyProjects\automated_research_report_generator_v0.1\README.md
C:\PyProjects\automated_research_report_generator_v0.1\uv.lock
C:\PyProjects\automated_research_report_generator_v0.1\_encoding_test.txt
C:\PyProjects\automated_research_report_generator_v0.1\.cache\上海曦智科技股份有限公司_analyze_business_model_report.md
C:\PyProjects\automated_research_report_generator_v0.1\.cache\上海曦智科技股份有限公司_analyze_management_report.md
C:\PyProjects\automated_research_report_generator_v0.1\.cache\上海曦智科技股份有限公司_analyze_operational_metrics_report.md
C:\PyProjects\automated_research_report_generator_v0.1\.cache\上海曦智科技股份有限公司_analyze_strategic_capital_plans_report.md
C:\PyProjects\automated_research_report_generator_v0.1\.cache\上海曦智科技股份有限公司_assess_investment_risks_report.md
C:\PyProjects\automated_research_report_generator_v0.1\.cache\上海曦智科技股份有限公司_conduct_industry_research_analysis_report.md
C:\PyProjects\automated_research_report_generator_v0.1\.cache\上海曦智科技股份有限公司_extract_comparable_company_data_report.md
C:\PyProjects\automated_research_report_generator_v0.1\.cache\上海曦智科技股份有限公司_final_quality_review_report.md
C:\PyProjects\automated_research_report_generator_v0.1\.cache\上海曦智科技股份有限公司_financial_analysis.md
C:\PyProjects\automated_research_report_generator_v0.1\.cache\上海曦智科技股份有限公司_identify_comparable_companies_report.md
C:\PyProjects\automated_research_report_generator_v0.1\.cache\上海曦智科技股份有限公司_identify_management_communication_questions_report.md
C:\PyProjects\automated_research_report_generator_v0.1\.cache\上海曦智科技股份有限公司_investment_analysis_and_scoring_report.md
C:\PyProjects\automated_research_report_generator_v0.1\.cache\上海曦智科技股份有限公司_perform_comparable_valuation_analysis_report.md
C:\PyProjects\automated_research_report_generator_v0.1\.cache\上海曦智科技股份有限公司_research_report.md
C:\PyProjects\automated_research_report_generator_v0.1\.cache\pdf_page_indexes\sehk26033003936_c_document_metadata.json
C:\PyProjects\automated_research_report_generator_v0.1\.cache\pdf_page_indexes\sehk26033003936_c_page_index.json
C:\PyProjects\automated_research_report_generator_v0.1\.venv\.gitignore
C:\PyProjects\automated_research_report_generator_v0.1\.venv\.lock
C:\PyProjects\automated_research_report_generator_v0.1\.venv\CACHEDIR.TAG
C:\PyProjects\automated_research_report_generator_v0.1\.venv\pyvenv.cfg
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\81d243bd2c585b0f4821__mypyc.cp313-win_amd64.pyd
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\appdirs.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\brotli.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\deprecation.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\isympy.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\jsonref.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\mmh3.cp313-win_amd64.pyd
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\proxytypes.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\pythoncom.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\PyWin32.chm
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\pywin32.pth
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\pywin32.version.txt
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\six.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\typing_extensions.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\_automated_buy_side_research_report_generator.pth
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\_automated_research_report_generator_v0_1.pth
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\_brotli.cp313-win_amd64.pyd
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\_cffi_backend.cp313-win_amd64.pyd
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\_virtualenv.pth
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\_virtualenv.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\adodbapi\adodbapi.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\adodbapi\ado_consts.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\adodbapi\apibase.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\adodbapi\is64bit.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\adodbapi\license.txt
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\adodbapi\process_connect_string.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\adodbapi\readme.txt
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\adodbapi\schema_table.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\adodbapi\setup.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\adodbapi\__init__.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\adodbapi\examples\db_print.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\adodbapi\examples\db_table_names.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\adodbapi\examples\xls_read.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\adodbapi\examples\xls_write.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\adodbapi\test\adodbapitest.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\adodbapi\test\adodbapitestconfig.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\adodbapi\test\dbapi20.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\adodbapi\test\is64bit.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\adodbapi\test\setuptestframework.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\adodbapi\test\test_adodbapi_dbapi20.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\adodbapi\test\tryconnection.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiocache\base.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiocache\decorators.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiocache\exceptions.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiocache\factory.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiocache\lock.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiocache\plugins.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiocache\__init__.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiocache\backends\memcached.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiocache\backends\memory.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiocache\backends\redis.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiocache\backends\__init__.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiocache\backends\__pycache__\memory.cpython-313.pyc
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiocache\backends\__pycache__\__init__.cpython-313.pyc
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiocache\serializers\serializers.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiocache\serializers\__init__.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiocache\serializers\__pycache__\serializers.cpython-313.pyc
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiocache\serializers\__pycache__\__init__.cpython-313.pyc
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiocache\__pycache__\base.cpython-313.pyc
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiocache\__pycache__\decorators.cpython-313.pyc
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiocache\__pycache__\exceptions.cpython-313.pyc
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiocache\__pycache__\factory.cpython-313.pyc
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiocache\__pycache__\lock.cpython-313.pyc
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiocache\__pycache__\__init__.cpython-313.pyc
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiocache-0.12.3.dist-info\INSTALLER
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiocache-0.12.3.dist-info\LICENSE
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiocache-0.12.3.dist-info\METADATA
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiocache-0.12.3.dist-info\RECORD
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiocache-0.12.3.dist-info\REQUESTED
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiocache-0.12.3.dist-info\top_level.txt
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiocache-0.12.3.dist-info\WHEEL
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiofiles\base.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiofiles\os.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiofiles\ospath.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiofiles\__init__.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiofiles\tempfile\temptypes.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiofiles\tempfile\__init__.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiofiles\tempfile\__pycache__\temptypes.cpython-313.pyc
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiofiles\tempfile\__pycache__\__init__.cpython-313.pyc
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiofiles\threadpool\binary.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiofiles\threadpool\text.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiofiles\threadpool\utils.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiofiles\threadpool\__init__.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiofiles\threadpool\__pycache__\binary.cpython-313.pyc
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiofiles\threadpool\__pycache__\text.cpython-313.pyc
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiofiles\threadpool\__pycache__\utils.cpython-313.pyc
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiofiles\threadpool\__pycache__\__init__.cpython-313.pyc
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiofiles\__pycache__\base.cpython-313.pyc
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiofiles\__pycache__\__init__.cpython-313.pyc
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiofiles-24.1.0.dist-info\INSTALLER
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiofiles-24.1.0.dist-info\METADATA
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiofiles-24.1.0.dist-info\RECORD
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiofiles-24.1.0.dist-info\REQUESTED
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiofiles-24.1.0.dist-info\WHEEL
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiofiles-24.1.0.dist-info\licenses\LICENSE
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiofiles-24.1.0.dist-info\licenses\NOTICE
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiohappyeyeballs\impl.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiohappyeyeballs\py.typed
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiohappyeyeballs\types.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiohappyeyeballs\utils.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiohappyeyeballs\_staggered.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiohappyeyeballs\__init__.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiohappyeyeballs\__pycache__\impl.cpython-313.pyc
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiohappyeyeballs\__pycache__\types.cpython-313.pyc
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiohappyeyeballs\__pycache__\utils.cpython-313.pyc
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiohappyeyeballs\__pycache__\_staggered.cpython-313.pyc
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiohappyeyeballs\__pycache__\__init__.cpython-313.pyc
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiohappyeyeballs-2.6.1.dist-info\INSTALLER
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiohappyeyeballs-2.6.1.dist-info\LICENSE
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiohappyeyeballs-2.6.1.dist-info\METADATA
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiohappyeyeballs-2.6.1.dist-info\RECORD
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiohappyeyeballs-2.6.1.dist-info\REQUESTED
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiohappyeyeballs-2.6.1.dist-info\WHEEL
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiohttp\abc.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiohttp\base_protocol.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiohttp\client.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiohttp\client_exceptions.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiohttp\client_middlewares.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiohttp\client_middleware_digest_auth.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiohttp\client_proto.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiohttp\client_reqrep.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiohttp\client_ws.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiohttp\compression_utils.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiohttp\connector.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiohttp\cookiejar.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiohttp\formdata.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiohttp\hdrs.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiohttp\helpers.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiohttp\http.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiohttp\http_exceptions.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiohttp\http_parser.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiohttp\http_websocket.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiohttp\http_writer.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiohttp\log.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiohttp\multipart.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiohttp\payload.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiohttp\payload_streamer.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiohttp\py.typed
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiohttp\pytest_plugin.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiohttp\resolver.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiohttp\streams.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiohttp\tcp_helpers.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiohttp\test_utils.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiohttp\tracing.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiohttp\typedefs.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiohttp\web.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiohttp\web_app.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiohttp\web_exceptions.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiohttp\web_fileresponse.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiohttp\web_log.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiohttp\web_middlewares.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiohttp\web_protocol.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aiohttp\web_request.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\aioh…252144 tokens truncated…y
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure\audio_transcriptions.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure\azure.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure\common_utils.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure\cost_calculation.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure\exception_mapping.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure\batches\handler.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure\batches\__pycache__\handler.cpython-313.pyc
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure\chat\gpt_5_transformation.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure\chat\gpt_transformation.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure\chat\o_series_handler.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure\chat\o_series_transformation.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure\chat\__pycache__\o_series_handler.cpython-313.pyc
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure\completion\handler.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure\completion\transformation.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure\completion\__pycache__\handler.cpython-313.pyc
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure\files\handler.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure\files\__pycache__\handler.cpython-313.pyc
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure\fine_tuning\handler.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure\fine_tuning\__pycache__\handler.cpython-313.pyc
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure\image_edit\transformation.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure\image_generation\dall_e_2_transformation.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure\image_generation\dall_e_3_transformation.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure\image_generation\gpt_transformation.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure\image_generation\__init__.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure\image_generation\__pycache__\dall_e_2_transformation.cpython-313.pyc
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure\image_generation\__pycache__\dall_e_3_transformation.cpython-313.pyc
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure\image_generation\__pycache__\gpt_transformation.cpython-313.pyc
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure\image_generation\__pycache__\__init__.cpython-313.pyc
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure\passthrough\transformation.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure\realtime\handler.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure\realtime\http_transformation.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure\realtime\__pycache__\handler.cpython-313.pyc
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure\responses\o_series_transformation.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure\responses\transformation.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure\text_to_speech\transformation.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure\text_to_speech\__init__.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure\vector_stores\transformation.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure\videos\transformation.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure\__pycache__\assistants.cpython-313.pyc
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure\__pycache__\audio_transcriptions.cpython-313.pyc
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure\__pycache__\azure.cpython-313.pyc
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure\__pycache__\common_utils.cpython-313.pyc
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure\__pycache__\cost_calculation.cpython-313.pyc
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure_ai\common_utils.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure_ai\cost_calculator.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure_ai\README.md
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure_ai\agents\handler.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure_ai\agents\transformation.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure_ai\agents\__init__.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure_ai\anthropic\handler.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure_ai\anthropic\messages_transformation.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure_ai\anthropic\transformation.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure_ai\anthropic\__init__.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure_ai\anthropic\count_tokens\handler.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure_ai\anthropic\count_tokens\token_counter.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure_ai\anthropic\count_tokens\transformation.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure_ai\anthropic\count_tokens\__init__.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure_ai\anthropic\__pycache__\handler.cpython-313.pyc
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure_ai\anthropic\__pycache__\messages_transformation.cpython-313.pyc
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure_ai\anthropic\__pycache__\transformation.cpython-313.pyc
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure_ai\anthropic\__pycache__\__init__.cpython-313.pyc
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure_ai\azure_model_router\transformation.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure_ai\azure_model_router\__init__.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure_ai\chat\handler.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure_ai\chat\transformation.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure_ai\embed\cohere_transformation.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure_ai\embed\handler.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure_ai\embed\__init__.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure_ai\embed\__pycache__\cohere_transformation.cpython-313.pyc
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure_ai\embed\__pycache__\handler.cpython-313.pyc
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure_ai\embed\__pycache__\__init__.cpython-313.pyc
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure_ai\image_edit\flux2_transformation.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure_ai\image_edit\transformation.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure_ai\image_edit\__init__.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure_ai\image_generation\cost_calculator.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure_ai\image_generation\dall_e_2_transformation.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure_ai\image_generation\dall_e_3_transformation.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure_ai\image_generation\flux_transformation.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure_ai\image_generation\gpt_transformation.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure_ai\image_generation\__init__.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure_ai\ocr\common_utils.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure_ai\ocr\transformation.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure_ai\ocr\__init__.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure_ai\ocr\document_intelligence\transformation.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure_ai\ocr\document_intelligence\__init__.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure_ai\rerank\handler.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure_ai\rerank\transformation.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure_ai\vector_stores\transformation.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure_ai\vector_stores\__init__.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\azure_ai\__pycache__\cost_calculator.cpython-313.pyc
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\baseten\chat.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\base_llm\base_model_iterator.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\base_llm\base_utils.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\base_llm\__init__.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\base_llm\anthropic_messages\transformation.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\base_llm\anthropic_messages\__pycache__\transformation.cpython-313.pyc
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\base_llm\audio_transcription\transformation.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\base_llm\audio_transcription\__pycache__\transformation.cpython-313.pyc
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\base_llm\batches\transformation.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\base_llm\batches\__pycache__\transformation.cpython-313.pyc
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\base_llm\bridges\completion_transformation.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\base_llm\bridges\__pycache__\completion_transformation.cpython-313.pyc
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\base_llm\chat\transformation.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\base_llm\chat\__pycache__\transformation.cpython-313.pyc
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\base_llm\completion\transformation.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\base_llm\completion\__pycache__\transformation.cpython-313.pyc
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\base_llm\containers\transformation.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\base_llm\containers\__pycache__\transformation.cpython-313.pyc
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\base_llm\embedding\transformation.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\base_llm\embedding\__pycache__\transformation.cpython-313.pyc
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\base_llm\evals\transformation.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\base_llm\evals\__init__.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\base_llm\evals\__pycache__\transformation.cpython-313.pyc
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\base_llm\evals\__pycache__\__init__.cpython-313.pyc
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\base_llm\files\azure_blob_storage_backend.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\base_llm\files\storage_backend.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\base_llm\files\storage_backend_factory.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\base_llm\files\transformation.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\base_llm\files\__pycache__\transformation.cpython-313.pyc
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\base_llm\google_genai\transformation.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\base_llm\google_genai\__pycache__\transformation.cpython-313.pyc
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\base_llm\guardrail_translation\base_translation.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\base_llm\guardrail_translation\__pycache__\base_translation.cpython-313.pyc
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\base_llm\image_edit\transformation.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\base_llm\image_edit\__pycache__\transformation.cpython-313.pyc
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\base_llm\image_generation\transformation.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\base_llm\image_generation\__pycache__\transformation.cpython-313.pyc
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\base_llm\image_variations\transformation.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\base_llm\image_variations\__pycache__\transformation.cpython-313.pyc
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\base_llm\interactions\transformation.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\base_llm\interactions\__init__.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\base_llm\interactions\__pycache__\transformation.cpython-313.pyc
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\base_llm\interactions\__pycache__\__init__.cpython-313.pyc
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\base_llm\managed_resources\base_managed_resource.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\base_llm\managed_resources\utils.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\base_llm\managed_resources\__init__.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\base_llm\ocr\transformation.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\base_llm\ocr\__init__.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\base_llm\ocr\__pycache__\transformation.cpython-313.pyc
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\base_llm\ocr\__pycache__\__init__.cpython-313.pyc
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\base_llm\passthrough\transformation.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\base_llm\realtime\http_transformation.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\base_llm\realtime\transformation.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\base_llm\realtime\__pycache__\transformation.cpython-313.pyc
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\base_llm\rerank\transformation.py
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\base_llm\rerank\__pycache__\transformation.cpython-313.pyc
C:\PyProjects\automated_research_report_generator_v0.1\.venv\Lib\site-packages\litellm\llms\base_llm\responses\transf
```
### 0012 TOOL OUTPUT `shell_command`

```text
Exit code: 1
Wall time: 2.7 seconds
Output:
error: Failed to initialize cache at `C:\Users\tybzh\AppData\Local\uv\cache`
  Caused by: failed to open file `C:\Users\tybzh\AppData\Local\uv\cache\sdists-v9\.git`: 拒绝访问。 (os error 5)
```
### 0013 ASSISTANT (commentary)

```text
我已经把线上参考查到了，接着补上本地安装版本，并把源码范围收缩到 `src` 下的核心文件。拿到这两部分后，我会直接改注释，不去碰业务逻辑。
```
### 0014 TOOL CALL `shell_command`

```text
{"command":"Get-ChildItem -Path src -Recurse -File -Include *.py | ForEach-Object { $_.FullName }","workdir":"C:\\PyProjects\\automated_research_report_generator_v0.1","timeout_ms":20000}
```
### 0015 TOOL CALL `shell_command`

```text
{"command":"$env:UV_CACHE_DIR='C:\\PyProjects\\automated_research_report_generator_v0.1\\.uv-cache'; uv run python -c \"import crewai; print(crewai.__version__)\"","workdir":"C:\\PyProjects\\automated_research_report_generator_v0.1","timeout_ms":30000}
```
### 0016 TOOL OUTPUT `shell_command`

```text
Exit code: 0
Wall time: 0.8 seconds
Output:
C:\PyProjects\automated_research_report_generator_v0.1\src\automated_research_report_generator_v0_1\tools\document_metadata_tools.py
C:\PyProjects\automated_research_report_generator_v0.1\src\automated_research_report_generator_v0_1\tools\MarkdownToPdfTool.py
C:\PyProjects\automated_research_report_generator_v0.1\src\automated_research_report_generator_v0_1\tools\pdf_page_tools.py
C:\PyProjects\automated_research_report_generator_v0.1\src\automated_research_report_generator_v0_1\tools\__init__.py
C:\PyProjects\automated_research_report_generator_v0.1\src\automated_research_report_generator_v0_1\crew.py
C:\PyProjects\automated_research_report_generator_v0.1\src\automated_research_report_generator_v0_1\document_metadata.py
C:\PyProjects\automated_research_report_generator_v0.1\src\automated_research_report_generator_v0_1\main.py
C:\PyProjects\automated_research_report_generator_v0.1\src\automated_research_report_generator_v0_1\pdf_indexing.py
C:\PyProjects\automated_research_report_generator_v0.1\src\automated_research_report_generator_v0_1\pdf_indexing_parellel.py
C:\PyProjects\automated_research_report_generator_v0.1\src\automated_research_report_generator_v0_1\__init__.py
```
### 0017 TOOL OUTPUT `shell_command`

```text
Exit code: 1
Wall time: 11 seconds
Output:
WARN Retrying to persist temporary file to C:\PyProjects\automated_research_report_generator_v0.1\.uv-cache\interpreter-v4\5843cb353efc6d76\4665fdafd1b706c4.msgpack: failed to persist temporary file: 拒绝访问。 (os error 5)
WARN Retrying to persist temporary file to C:\PyProjects\automated_research_report_generator_v0.1\.uv-cache\interpreter-v4\5843cb353efc6d76\4665fdafd1b706c4.msgpack: failed to persist temporary file: 拒绝访问。 (os error 5)
WARN Retrying to persist temporary file to C:\PyProjects\automated_research_report_generator_v0.1\.uv-cache\interpreter-v4\5843cb353efc6d76\4665fdafd1b706c4.msgpack: failed to persist temporary file: 拒绝访问。 (os error 5)
WARN Retrying to persist temporary file to C:\PyProjects\automated_research_report_generator_v0.1\.uv-cache\interpreter-v4\5843cb353efc6d76\4665fdafd1b706c4.msgpack: failed to persist temporary file: 拒绝访问。 (os error 5)
WARN Retrying to persist temporary file to C:\PyProjects\automated_research_report_generator_v0.1\.uv-cache\interpreter-v4\5843cb353efc6d76\4665fdafd1b706c4.msgpack: failed to persist temporary file: 拒绝访问。 (os error 5)
WARN Retrying to persist temporary file to C:\PyProjects\automated_research_report_generator_v0.1\.uv-cache\interpreter-v4\5843cb353efc6d76\4665fdafd1b706c4.msgpack: failed to persist temporary file: 拒绝访问。 (os error 5)
WARN Retrying to persist temporary file to C:\PyProjects\automated_research_report_generator_v0.1\.uv-cache\interpreter-v4\5843cb353efc6d76\4665fdafd1b706c4.msgpack: failed to persist temporary file: 拒绝访问。 (os error 5)
WARN Retrying to persist temporary file to C:\PyProjects\automated_research_report_generator_v0.1\.uv-cache\interpreter-v4\5843cb353efc6d76\4665fdafd1b706c4.msgpack: failed to persist temporary file: 拒绝访问。 (os error 5)
WARN Retrying to persist temporary file to C:\PyProjects\automated_research_report_generator_v0.1\.uv-cache\interpreter-v4\5843cb353efc6d76\4665fdafd1b706c4.msgpack: failed to persist temporary file: 拒绝访问。 (os error 5)
WARN Retrying to persist temporary file to C:\PyProjects\automated_research_report_generator_v0.1\.uv-cache\interpreter-v4\5843cb353efc6d76\4665fdafd1b706c4.msgpack: failed to persist temporary file: 拒绝访问。 (os error 5)
error: Failed to query Python interpreter
  Caused by: Failed to persist temporary file to C:\PyProjects\automated_research_report_generator_v0.1\.uv-cache\interpreter-v4\5843cb353efc6d76\4665fdafd1b706c4.msgpack: failed to persist temporary file: 拒绝访问。 (os error 5)
```
### 0018 ASSISTANT (commentary)

```text
已经缩到 `src/automated_research_report_generator_v0_1` 这几个 Python 文件了。本地版本我改用虚拟环境里的 Python 直接查，这样不依赖 `uv` 缓存；同时我开始阅读 `crew.py`、`main.py` 和几个工具文件，判断哪些属于你说的“核心代码”。
```
### 0019 TOOL CALL `shell_command`

```text
{"command":".\\.venv\\Scripts\\python.exe -c \"import crewai; print(crewai.__version__)\"","workdir":"C:\\PyProjects\\automated_research_report_generator_v0.1","timeout_ms":20000}
```
### 0020 TOOL CALL `shell_command`

```text
{"command":"Get-Content -Raw src\\automated_research_report_generator_v0_1\\crew.py","workdir":"C:\\PyProjects\\automated_research_report_generator_v0.1","timeout_ms":20000}
```
### 0021 TOOL CALL `shell_command`

```text
{"command":"Get-Content -Raw src\\automated_research_report_generator_v0_1\\main.py","workdir":"C:\\PyProjects\\automated_research_report_generator_v0.1","timeout_ms":20000}
```
### 0022 TOOL CALL `shell_command`

```text
{"command":"Get-Content -Raw src\\automated_research_report_generator_v0_1\\document_metadata.py","workdir":"C:\\PyProjects\\automated_research_report_generator_v0.1","timeout_ms":20000}
```
### 0023 TOOL OUTPUT `shell_command`

```text
Exit code: 0
Wall time: 6.8 seconds
Output:
1.12.2
[1 noisy or binary line(s) omitted during export]
```
### 0024 TOOL OUTPUT `shell_command`

```text
Exit code: 0
Wall time: 6.8 seconds
Output:
import os
from dotenv import load_dotenv
from crewai import Agent, Crew, LLM, Process, Task
from crewai.project import CrewBase, agent, crew, task
from crewai_tools import SerperDevTool
from automated_research_report_generator_v0_1.tools.MarkdownToPdfTool import (
    MarkdownToPdfTool,
)
from automated_research_report_generator_v0_1.tools.pdf_page_tools import (
    ReadPdfPageIndexTool,
    ReadPdfPagesTool,
)
load_dotenv()

def get_llm(  # 统一封装项目默认 LLM 配置，但允许不同流程单独收紧超时和重试。
    temperature: float = 0.5,
    timeout: float | int | None = 60,
    max_retries: int | None = 1,
) -> LLM:
    return LLM(
        api_base="https://openrouter.ai/api/v1",
        model="openrouter/google/gemini-3-flash-preview",
        api_key=os.getenv("OPENROUTER_API_KEY"),
        temperature=temperature,
        timeout=timeout,
        max_retries=max_retries,
    )

shared_search_tool = SerperDevTool()  # 需要外部公开信息核对的 agent 共用一个搜索工具实例。
shared_pdf_page_index_tool = ReadPdfPageIndexTool()  # 所有 PDF 分析 agent 先读页索引，再决定页码范围。
shared_pdf_page_reader_tool = ReadPdfPagesTool()  # 所有 PDF 分析 agent 直接读取相关页原文，不再走 RAG。

@CrewBase
class AutomatedResearchReportGeneratorV01Crew:
    """Automated research report generator v0.1 crew."""

    agents_config = "config/agents.yaml"
    tasks_config = "config/tasks.yaml"

    @agent
    def industry_research_analyst(self) -> Agent:  # 代表性示例：多数分析 agent 都沿用这类“索引工具 + 页读取工具”的显式定义。
        return Agent(
            config=self.agents_config["industry_research_analyst"],  # type: ignore[index]
            tools=[
                shared_pdf_page_index_tool,
                shared_pdf_page_reader_tool,
                shared_search_tool,
            ],
            llm=get_llm(temperature=0.3),
            function_calling_llm=None,
            max_iter=25,
            max_rpm=None,
            max_execution_time=None,
            verbose=True,
            allow_delegation=False,
            step_callback=None,
            cache=True,
            allow_code_execution=False,
            max_retry_limit=2,
            respect_context_window=True,
            use_system_prompt=True,
            reasoning=False,
            max_reasoning_attempts=5,
            inject_date=True,
        )

    @agent
    def business_model_analyst(self) -> Agent:
        return Agent(
            config=self.agents_config["business_model_analyst"],  # type: ignore[index]
            tools=[shared_pdf_page_index_tool, shared_pdf_page_reader_tool],
            llm=get_llm(temperature=0.3),
            function_calling_llm=None,
            max_iter=25,
            max_rpm=None,
            max_execution_time=None,
            verbose=True,
            allow_delegation=False,
            step_callback=None,
            cache=True,
            allow_code_execution=False,
            max_retry_limit=2,
            respect_context_window=True,
            use_system_prompt=True,
            reasoning=False,
            max_reasoning_attempts=None,
            inject_date=True,
        )

    @agent
    def financial_analysis_specialist(self) -> Agent:
        return Agent(
            config=self.agents_config["financial_analysis_specialist"],  # type: ignore[index]
            tools=[shared_pdf_page_index_tool, shared_pdf_page_reader_tool],
            llm=get_llm(temperature=0.3),
            function_calling_llm=None,
            max_iter=25,
            max_rpm=None,
            max_execution_time=None,
            verbose=True,
            allow_delegation=False,
            step_callback=None,
            cache=True,
            allow_code_execution=False,
            max_retry_limit=2,
            respect_context_window=True,
            use_system_prompt=True,
            reasoning=False,
            max_reasoning_attempts=None,
            inject_date=True,
        )

    @agent
    def operational_data_analyst(self) -> Agent:
        return Agent(
            config=self.agents_config["operational_data_analyst"],  # type: ignore[index]
            tools=[shared_pdf_page_index_tool, shared_pdf_page_reader_tool],
            llm=get_llm(temperature=0.3),
            function_calling_llm=None,
            max_iter=25,
            max_rpm=None,
            max_execution_time=None,
            verbose=True,
            allow_delegation=False,
            step_callback=None,
            cache=True,
            allow_code_execution=False,
            max_retry_limit=2,
            respect_context_window=True,
            use_system_prompt=True,
            reasoning=False,
            max_reasoning_attempts=None,
            inject_date=True,
        )

    @agent
    def management_assessment_analyst(self) -> Agent:
        return Agent(
            config=self.agents_config["management_assessment_analyst"],  # type: ignore[index]
            tools=[
                shared_pdf_page_index_tool,
                shared_pdf_page_reader_tool,
                shared_search_tool,
            ],
            llm=get_llm(temperature=0.3),
            function_calling_llm=None,
            max_iter=25,
            max_rpm=None,
            max_execution_time=None,
            verbose=True,
            allow_delegation=False,
            step_callback=None,
            cache=True,
            allow_code_execution=False,
            max_retry_limit=2,
            respect_context_window=True,
            use_system_prompt=True,
            reasoning=False,
            max_reasoning_attempts=None,
            inject_date=True,
        )

    @agent
    def risk_assessment_analyst(self) -> Agent:
        return Agent(
            config=self.agents_config["risk_assessment_analyst"],  # type: ignore[index]
            tools=[shared_pdf_page_index_tool, shared_pdf_page_reader_tool],
            llm=get_llm(temperature=0.3),
            function_calling_llm=None,
            max_iter=25,
            max_rpm=None,
            max_execution_time=None,
            verbose=True,
            allow_delegation=False,
            step_callback=None,
            cache=True,
            allow_code_execution=False,
            max_retry_limit=2,
            respect_context_window=True,
            use_system_prompt=True,
            reasoning=False,
            max_reasoning_attempts=None,
            inject_date=True,
        )

    @agent
    def strategic_capital_analyst(self) -> Agent:
        return Agent(
            config=self.agents_config["strategic_capital_analyst"],  # type: ignore[index]
            tools=[shared_pdf_page_index_tool, shared_pdf_page_reader_tool],
            llm=get_llm(temperature=0.3),
            function_calling_llm=None,
            max_iter=25,
            max_rpm=None,
            max_execution_time=None,
            verbose=True,
            allow_delegation=False,
            step_callback=None,
            cache=True,
            allow_code_execution=False,
            max_retry_limit=2,
            respect_context_window=True,
            use_system_prompt=True,
            reasoning=False,
            max_reasoning_attempts=None,
            inject_date=True,
        )

    @agent
    def comparable_company_identifier(self) -> Agent:
        return Agent(
            config=self.agents_config["comparable_company_identifier"],  # type: ignore[index]
            tools=[shared_search_tool],
            llm=get_llm(temperature=0.5),
            function_calling_llm=None,
            max_iter=25,
            max_rpm=None,
            max_execution_time=None,
            verbose=True,
            allow_delegation=False,
            step_callback=None,
            cache=True,
            allow_code_execution=False,
            max_retry_limit=2,
            respect_context_window=True,
            use_system_prompt=True,
            reasoning=False,
            max_reasoning_attempts=None,
            inject_date=True,
        )

    @agent
    def comparable_company_data_extractor(self) -> Agent:
        return Agent(
            config=self.agents_config["comparable_company_data_extractor"],  # type: ignore[index]
            tools=[shared_search_tool],
            llm=get_llm(temperature=0.3),
            function_calling_llm=None,
            max_iter=25,
            max_rpm=None,
            max_execution_time=None,
            verbose=True,
            allow_delegation=False,
            step_callback=None,
            cache=True,
            allow_code_execution=False,
            max_retry_limit=2,
            respect_context_window=True,
            use_system_prompt=True,
            reasoning=False,
            max_reasoning_attempts=None,
            inject_date=True,
        )

    @agent
    def valuation_analysis_specialist(self) -> Agent:
        return Agent(
            config=self.agents_config["valuation_analysis_specialist"],  # type: ignore[index]
            tools=[shared_search_tool],
            llm=get_llm(temperature=0.7),
            function_calling_llm=None,
            max_iter=25,
            max_rpm=None,
            max_execution_time=None,
            verbose=True,
            allow_delegation=False,
            step_callback=None,
            cache=True,
            allow_code_execution=False,
            max_retry_limit=2,
            respect_context_window=True,
            use_system_prompt=True,
            reasoning=False,
            max_reasoning_attempts=None,
            inject_date=True,
        )
    
    @agent
    def investment_highlights_and_risk_scorer(self) -> Agent:
        return Agent(
            config=self.agents_config["investment_highlights_and_risk_scorer"],  # type: ignore[index]
            tools=[],
            llm=get_llm(temperature=0.8),
            function_calling_llm=None,
            max_iter=25,
            max_rpm=None,
            max_execution_time=None,
            verbose=True,
            allow_delegation=False,
            step_callback=None,
            cache=True,
            allow_code_execution=False,
            max_retry_limit=2,
            respect_context_window=True,
            use_system_prompt=True,
            reasoning=False,
            max_reasoning_attempts=None,
            inject_date=True,
        )

    @agent
    def management_communication_specialist(self) -> Agent:
        return Agent(
            config=self.agents_config["management_communication_specialist"],  # type: ignore[index]
            tools=[],
            llm=get_llm(temperature=0.7),
            function_calling_llm=None,
            max_iter=25,
            max_rpm=None,
            max_execution_time=None,
            verbose=True,
            allow_delegation=False,
            step_callback=None,
            cache=True,
            allow_code_execution=False,
            max_retry_limit=2,
            respect_context_window=True,
            use_system_prompt=True,
            reasoning=False,
            max_reasoning_attempts=None,
            inject_date=True,
        )

    @agent
    def research_report_writer(self) -> Agent:
        return Agent(
            config=self.agents_config["research_report_writer"],  # type: ignore[index]
            tools=[],
            llm=get_llm(temperature=0.2),
            function_calling_llm=None,
            max_iter=25,
            max_rpm=None,
            max_execution_time=None,
            verbose=True,
            allow_delegation=False,
            step_callback=None,
            cache=True,
            allow_code_execution=False,
            max_retry_limit=2,
            respect_context_window=True,
            use_system_prompt=True,
            reasoning=False,
            max_reasoning_attempts=None,
            inject_date=True,
        )

    @agent
    def qa_consistency_analyst(self) -> Agent:
        return Agent(
            config=self.agents_config["qa_consistency_analyst"],  # type: ignore[index]
            tools=[],
            llm=get_llm(temperature=0.7),
            function_calling_llm=None,
            max_iter=25,
            max_rpm=None,
            max_execution_time=None,
            verbose=True,
            allow_delegation=False,
            step_callback=None,
            cache=True,
            allow_code_execution=False,
            max_retry_limit=2,
            respect_context_window=True,
            use_system_prompt=True,
            reasoning=False,
            max_reasoning_attempts=None,
            inject_date=True,
        )


    @task
    def conduct_industry_research_analysis(self) -> Task:  # 代表性示例：task 主要保持 YAML 驱动，只在 Python 里补统一运行选项。
        return Task(
            config=self.tasks_config["conduct_industry_research_analysis"],  # type: ignore[index]
            tools=[],
            async_execution=False,
            output_json=None,
            output_pydantic=None,
            human_input=False,
            cache=True,
            markdown=True,
        )

    @task
    def analyze_business_model(self) -> Task:
        return Task(
            config=self.tasks_config["analyze_business_model"],  # type: ignore[index]
            tools=[],
            async_execution=False,
            output_json=None,
            output_pydantic=None,
            human_input=False,
            cache=True,
            markdown=True,
        )

    @task
    def analyze_management(self) -> Task:
        return Task(
            config=self.tasks_config["analyze_management"],  # type: ignore[index]
            tools=[],
            async_execution=False,
            output_json=None,
            output_pydantic=None,
            human_input=False,
            cache=True,
            markdown=True,
        )

    @task
    def conduct_financial_analysis(self) -> Task:
        return Task(
            config=self.tasks_config["conduct_financial_analysis"],  # type: ignore[index]
            tools=[],
            async_execution=False,
            output_json=None,
            output_pydantic=None,
            human_input=False,
            cache=True,
            markdown=True,
        )

    @task
    def analyze_operational_metrics(self) -> Task:
        return Task(
            config=self.tasks_config["analyze_operational_metrics"],  # type: ignore[index]
            tools=[],
            async_execution=False,
            output_json=None,
            output_pydantic=None,
            human_input=False,
            cache=True,
            markdown=True,
        )

    @task
    def assess_investment_risks(self) -> Task:
        return Task(
            config=self.tasks_config["assess_investment_risks"],  # type: ignore[index]
            tools=[],
            async_execution=False,
            output_json=None,
            output_pydantic=None,
            human_input=False,
            cache=True,
            markdown=True,
        )

    @task
    def analyze_strategic_capital_plans(self) -> Task:
        return Task(
            config=self.tasks_config["analyze_strategic_capital_plans"],  # type: ignore[index]
            tools=[],
            async_execution=False,
            output_json=None,
            output_pydantic=None,
            human_input=False,
            cache=True,
            markdown=True,
        )

    @task
    def identify_comparable_companies(self) -> Task:
        return Task(
            config=self.tasks_config["identify_comparable_companies"],  # type: ignore[index]
            tools=[],
            async_execution=False,
            output_json=None,
            output_pydantic=None,
            human_input=False,
            cache=True,
            markdown=True,
        )

    @task
    def extract_comparable_company_data(self) -> Task:
        return Task(
            config=self.tasks_config["extract_comparable_company_data"],  # type: ignore[index]
            tools=[],
            async_execution=False,
            output_json=None,
            output_pydantic=None,
            human_input=False,
            cache=True,
            markdown=True,
        )

    @task
    def perform_comparable_valuation_analysis(self) -> Task:
        return Task(
            config=self.tasks_config["perform_comparable_valuation_analysis"],  # type: ignore[index]
            tools=[],
            async_execution=False,
            output_json=None,
            output_pydantic=None,
            human_input=False,
            cache=True,
            markdown=True,
        )

    @task
    def investment_analysis_and_scoring(self) -> Task:
        return Task(
            config=self.tasks_config["investment_analysis_and_scoring"],  # type: ignore[index]
            tools=[],
            async_execution=False,
            output_json=None,
            output_pydantic=None,
            human_input=False,
            cache=True,
            markdown=True,
        )

    @task
    def identify_management_communication_questions(self) -> Task:
        return Task(
            config=self.tasks_config["identify_management_communication_questions"],  # type: ignore[index]
            tools=[],
            async_execution=False,
            output_json=None,
            output_pydantic=None,
            human_input=False,
            cache=True,
            markdown=True,
        )

    @task
    def final_quality_review(self) -> Task:
        return Task(
            config=self.tasks_config["final_quality_review"],  # type: ignore[index]
            tools=[],
            async_execution=False,
            output_json=None,
            output_pydantic=None,
            human_input=False,
            cache=True,
            markdown=True,
        )

    @task
    def compile_research_report(self) -> Task:
        return Task(
            config=self.tasks_config["compile_research_report"],  # type: ignore[index]
            tools=[],
            async_execution=False,
            output_json=None,
            output_pydantic=None,
            human_input=False,
            cache=True,
            markdown=True,
        )

    @task
    def convert_research_report_md_to_pdf(self) -> Task:
        return Task(
            config=self.tasks_config["convert_research_report_md_to_pdf"],  # type: ignore[index]
            tools=[MarkdownToPdfTool(result_as_answer=True)],
            async_execution=False,
            output_json=None,
            output_pydantic=None,
            human_input=False,
            cache=True,
            markdown=False,
        )

    @crew
    def crew(self) -> Crew:  # 用顺序执行把各独立分析结果串成最终报告与 QA 流程。
        return Crew(
            name="automated_research_report_generator_v0.1",
            agents=self.agents,
            tasks=self.tasks,
            process=Process.sequential,
            verbose=True,
            manager_llm=None,
            manager_agent=None,
            function_calling_llm=None,
            config=None,
            max_rpm=None,
            memory=False,
            cache=True,
            embedder=None,
            full_output=False,
            step_callback=None,
            task_callback=None,
            share_crew=False,
            output_log_file=True,
            planning=False,
            planning_llm=None,
            trace=True,
            chat_llm=get_llm(),
        )
```
### 0025 TOOL OUTPUT `shell_command`

```text
Exit code: 0
Wall time: 0.9 seconds
Output:
import subprocess
import sys
from pathlib import Path
import shutil
import yaml


PROJECT_ROOT = Path(__file__).resolve().parents[2]
PROJECT_CACHE_DIR = PROJECT_ROOT / ".cache"
TASKS_CONFIG_PATH = (
    PROJECT_ROOT
    / "src"
    / "automated_research_report_generator_v0_1"
    / "config"
    / "tasks.yaml"
)
CACHE_ENTRIES_TO_KEEP = {".gitignore", ".lock", "CACHEDIR.TAG", "interpreter-v4", "sdists-v9"}
CACHE_ENTRY_PREFIXES_TO_KEEP = (".tmp",)

def reset_crewai_memories() -> None:  # 每次正式运行前清空 CrewAI memory，减少旧运行结果串到本轮。
    try:
        subprocess.run(
            ["crewai", "reset-memories", "--all"],
            check=False,
            capture_output=True,
            text=True,
        )
    except Exception as exc:
        print(f"[WARN] crewai reset-memories failed: {exc}")


def reset_project_cache_dir() -> None:  # 每次预处理前清空项目磁盘缓存，避免混入上次 run 的结果。
    try:
        PROJECT_CACHE_DIR.mkdir(parents=True, exist_ok=True)
        for cache_entry in PROJECT_CACHE_DIR.iterdir():
            if (
                cache_entry.name in CACHE_ENTRIES_TO_KEEP
                or cache_entry.name.startswith(CACHE_ENTRY_PREFIXES_TO_KEEP)
            ):
                continue

            if cache_entry.is_dir():
                shutil.rmtree(cache_entry)
            else:
                cache_entry.unlink()
    except Exception as exc:
        print(f"[WARN] project cache reset failed: {exc}")

def prepare_inputs() -> dict[str, str]:  # 在 crew 启动前先补齐固定页索引 JSON 路径。
    from automated_research_report_generator_v0_1.document_metadata import (
        ensure_pdf_document_metadata,
    )
    from automated_research_report_generator_v0_1.pdf_indexing import (
        ensure_pdf_page_index,
        reset_pdf_preprocessing_runtime_state,
    )

    reset_project_cache_dir()  # 强制清空项目 .cache，确保本轮重新生成预处理结果。
    reset_pdf_preprocessing_runtime_state()  # 每次运行前清理共享 PDF 页面缓存，避免同进程串上次结果。
    pdf_file_path = r"pdf/sehk26033003936_c.pdf"  # 当前 demo 默认处理的 PDF 路径。
#sehk26022601053_c x
#sehk26022702474_c x
#sehk26030502134_c x
#sehk26033003882_c x
#sehk26033003936_c x

    # Step 1: 先跑独立的文档元数据识别流程，自动识别公司名称和行业。
    document_metadata = ensure_pdf_document_metadata(
        pdf_file_path=pdf_file_path,
    )

    # Trace 展示的是 kickoff 当下的 inputs，所以这里要稳定带上公司名和行业两个标签。
    resolved_company_name = (document_metadata.get("company_name") or Path(pdf_file_path).stem).strip()
    resolved_industry = (document_metadata.get("industry") or "未知行业").strip()
    prepared_inputs: dict[str, str] = {
        "pdf_file_path": pdf_file_path,
        "company_name": resolved_company_name,
        "industry": resolved_industry,
    }

    # Step 2: 再跑页索引流程，只负责“逐页主题摘要”和页码定位。
    page_index_file_path = ensure_pdf_page_index(
        pdf_file_path=pdf_file_path,
        company_name=prepared_inputs["company_name"],
    )
    prepared_inputs["page_index_file_path"] = page_index_file_path  # 让后续 agent/tool 都能拿到固定索引路径。
    prepared_inputs["document_metadata_file_path"] = document_metadata["document_metadata_file_path"]  # 保留基础信息缓存文件路径，便于查看。
    return prepared_inputs


def validate_tasks_config() -> None:  # 提前校验 YAML 里的 context 依赖，避免 CrewAI 只抛模糊的 KeyError。
    with TASKS_CONFIG_PATH.open("r", encoding="utf-8") as tasks_file:
        tasks_config = yaml.safe_load(tasks_file) or {}

    if not isinstance(tasks_config, dict):
        raise ValueError(f"Invalid tasks config format: {TASKS_CONFIG_PATH}")

    task_names = set(tasks_config.keys())
    for task_name, task_config in tasks_config.items():
        if not isinstance(task_config, dict):
            raise ValueError(f"Task '{task_name}' config must be a mapping in {TASKS_CONFIG_PATH}")

        context_list = task_config.get("context")
        if context_list is None:
            continue

        if not isinstance(context_list, list):
            raise ValueError(f"Task '{task_name}' context must be a list in {TASKS_CONFIG_PATH}")

        for position, context_task_name in enumerate(context_list, start=1):
            if not isinstance(context_task_name, str) or not context_task_name.strip():
                raise ValueError(
                    f"Task '{task_name}' has an empty context entry at position {position} in {TASKS_CONFIG_PATH}"
                )

            if context_task_name not in task_names:
                raise ValueError(
                    f"Task '{task_name}' references unknown context task '{context_task_name}' in {TASKS_CONFIG_PATH}"
                )


def run():  # 标准运行入口：先准备索引，再启动顺序 crew。
    from automated_research_report_generator_v0_1.crew import (
        AutomatedResearchReportGeneratorV01Crew,
    )

    reset_crewai_memories()
    inputs = prepare_inputs()
    validate_tasks_config()
    crew_instance = AutomatedResearchReportGeneratorV01Crew()  # 保留显式 crew 结构，方便逐 agent 调试。
    return crew_instance.crew().kickoff(inputs=inputs)


def run_with_trigger():  # 供外部触发器复用的薄封装入口。
    return run()


def train():  # CrewAI 训练入口，同样复用索引预处理后的输入。
    from automated_research_report_generator_v0_1.crew import (
        AutomatedResearchReportGeneratorV01Crew,
    )

    inputs = prepare_inputs()
    validate_tasks_config()
    try:
        AutomatedResearchReportGeneratorV01Crew().crew().train(
            n_iterations=int(sys.argv[1]),
            filename=sys.argv[2],
            inputs=inputs,
        )
    except Exception as exc:
        raise Exception(f"An error occurred while training the crew: {exc}")


def replay():  # CrewAI 回放入口，用 task_id 从历史执行继续排查问题。
    from automated_research_report_generator_v0_1.crew import (
        AutomatedResearchReportGeneratorV01Crew,
    )

    validate_tasks_config()
    try:
        AutomatedResearchReportGeneratorV01Crew().crew().replay(task_id=sys.argv[1])
    except Exception as exc:
        raise Exception(f"An error occurred while replaying the crew: {exc}")


def test():  # CrewAI 测试入口，确保测试运行也遵循“先建页索引”的流程。
    from automated_research_report_generator_v0_1.crew import (
        AutomatedResearchReportGeneratorV01Crew,
    )

    inputs = prepare_inputs()
    validate_tasks_config()
    try:
        AutomatedResearchReportGeneratorV01Crew().crew().test(
            n_iterations=int(sys.argv[1]),
            openai_model_name=sys.argv[2],
            inputs=inputs,
        )
    except Exception as exc:
        raise Exception(f"An error occurred while testing the crew: {exc}")


if __name__ == "__main__":
    if len(sys.argv) < 2:  # 命令行至少要给出 run/train/replay/test 之一。
        print("Usage: main.py <command> [<args>]")
        sys.exit(1)

    command = sys.argv[1]  # 用最直接的分发方式保留入口清晰度，便于调试。
    if command == "run":
        run()
    elif command == "train":
        train()
    elif command == "replay":
        replay()
    elif command == "test":
        test()
    else:
        print(f"Unknown command: {command}")
        sys.exit(1)
```
### 0026 TOOL OUTPUT `shell_command`

```text
Exit code: 0
Wall time: 0.9 seconds
Output:
from __future__ import annotations

import json
from datetime import datetime, timezone
from pathlib import Path

from crewai import Agent
from pydantic import BaseModel, Field

from automated_research_report_generator_v0_1.crew import get_llm
from automated_research_report_generator_v0_1.tools.document_metadata_tools import (
    default_document_metadata_path,
    document_metadata_is_current,
    load_document_metadata,
    sample_document_metadata_pages,
    save_document_metadata,
)
from automated_research_report_generator_v0_1.tools.pdf_page_tools import (
    compute_pdf_fingerprint,
)


# =========================
# Metadata Agent Parameters
# =========================

DOCUMENT_METADATA_AGENT_ROLE = "PDF 文档基础信息识别专员"  # 默认角色名；可改成更贴近团队分工的中文角色名。
DOCUMENT_METADATA_AGENT_GOAL = (  # 默认目标；可改成更强调“上市主体”“发行人”或“行业映射”的版本。
    "读取 PDF 的关键页面，识别该文档对应的公司名称和所属行业，供主流程自动填充输入参数。"
)
DOCUMENT_METADATA_AGENT_BACKSTORY = (  # 默认背景设定；可改成更偏招股书、年报或卖方报告场景。
    "你专门负责在 PDF 的封面、概览、业务介绍等关键页面中识别公司名称和行业。"
    "你不做长篇分析，只输出最核心的结构化基础信息。"
)
DOCUMENT_METADATA_AGENT_TEMPERATURE = 0.1  # 默认 0.1；可改到 0 更保守，0.2-0.3 会更灵活但更易漂移。
DOCUMENT_METADATA_AGENT_VERBOSE = True  # 默认 True；调试时方便看 agent 过程，可改 False 收敛日志。
DOCUMENT_METADATA_AGENT_ALLOW_DELEGATION = False  # 默认 False；该 agent 是单点识别任务，不需要委派。
DOCUMENT_METADATA_AGENT_REASONING = False  # 默认 False；该任务短且结构化，通常不需要额外推理链。
DOCUMENT_METADATA_AGENT_CACHE = True  # 默认 True；相同输入可复用结果，减少重复调用。


# =========================
# Metadata Task Parameters
# =========================

DOCUMENT_METADATA_UNKNOWN_COMPANY = "未知公司"  # 默认公司兜底值；可改成更适合你流程的占位文本。
DOCUMENT_METADATA_UNKNOWN_INDUSTRY = "未知行业"  # 默认行业兜底值；可改成你内部行业树的占位词。
DOCUMENT_METADATA_SAMPLE_MAX_PAGES = 15  # 默认最多抽样前 15 个非空页；可调大以提高召回，可调小以省 token。
DOCUMENT_METADATA_SAMPLE_MAX_CHARS_PER_PAGE = 2500  # 默认每页最多取 2500 字；可调大以保留更多上下文。
DOCUMENT_METADATA_FORCE_REBUILD_DEFAULT = False  # 默认优先复用缓存；改成 True 会每次都重新识别。
DOCUMENT_METADATA_PROMPT_RULES = (  # 默认任务规则；可逐条增删来约束输出口径。
    "只能根据给定页面内容判断，不得猜测。",
    "输出必须严格符合给定结构。",
    "company_name 使用公司标准名称。",
    "industry 使用尽量简洁的行业名称。",
    f"如果材料里无法明确判断，就返回“{DOCUMENT_METADATA_UNKNOWN_COMPANY}”或“{DOCUMENT_METADATA_UNKNOWN_INDUSTRY}”。",
    "不要输出解释、前后缀、Markdown 代码块或额外文本。",
)


# =========================
# Metadata Models
# =========================

class PdfDocumentMetadata(BaseModel):
    company_name: str = Field(..., description="PDF 对应公司的标准名称")  # 结构化输出中的公司名字段。
    industry: str = Field(..., description="PDF 对应公司的所属行业")  # 结构化输出中的行业字段。


class PdfDocumentMetadataPayload(BaseModel):
    pdf_file_path: str  # 原始 PDF 的绝对路径，便于核对缓存对应关系。
    generated_at: str  # 结果生成时间，便于排查是否命中了旧缓存。
    fingerprint: str  # 和 PDF 文件内容绑定的指纹，防止旧结果污染新文件。
    company_name: str  # 最终识别出的公司名称。
    industry: str  # 最终识别出的行业名称。
    source_pages: list[int]  # 本次识别实际参考的页码，便于人工回看。


# =========================
# Metadata Helpers
# =========================

def _normalize_metadata_value(value: str, fallback: str) -> str:  # 清洗识别结果，去掉多余空白和异常标点。
    normalized = " ".join((value or "").split()).strip(" ,.;:()[]{}")
    return normalized or fallback


def _extract_metadata_from_raw(raw: str) -> tuple[str, str]:  # 兼容模型偶发返回裸 JSON 或 fenced JSON 的情况。
    cleaned = (raw or "").strip()
    if not cleaned:
        return "", ""

    if cleaned.startswith("```"):
        cleaned = cleaned.strip("`").strip()
        if cleaned.lower().startswith("json"):
            cleaned = cleaned[4:].strip()

    try:
        data = json.loads(cleaned)
    except Exception:
        return "", ""

    if not isinstance(data, dict):
        return "", ""

    return str(data.get("company_name", "") or ""), str(data.get("industry", "") or "")


def build_document_metadata_task_prompt(sampled_pages: list[tuple[int, str]]) -> str:  # 把“基础信息识别任务”的提示词集中到一处，便于单独调试。
    prompt_parts = [
        "你现在负责识别一份 PDF 对应的公司名称和所属行业。",
        "",
        "规则：",
    ]
    prompt_parts.extend(f"- {rule}" for rule in DOCUMENT_METADATA_PROMPT_RULES)
    prompt_parts.extend(["", "以下是候选页面内容："])

    for page_number, page_text in sampled_pages:
        prompt_parts.append(f"[第 {page_number} 页]")
        prompt_parts.append(page_text)
        prompt_parts.append("")

    return "\n".join(prompt_parts).strip()


# =========================
# Metadata Agent
# =========================

def create_document_metadata_agent() -> Agent:  # 独立创建“文档基础信息识别” agent，方便单独复跑和调参。
    return Agent(
        role=DOCUMENT_METADATA_AGENT_ROLE,  # 默认“PDF 文档基础信息识别专员”；可改成更细分的内部角色名。
        goal=DOCUMENT_METADATA_AGENT_GOAL,  # 默认聚焦公司名和行业识别；可改成覆盖证券简称、交易所等字段。
        backstory=DOCUMENT_METADATA_AGENT_BACKSTORY,  # 默认强调“只做结构化识别”；可改得更行业化。
        llm=get_llm(temperature=DOCUMENT_METADATA_AGENT_TEMPERATURE),  # 默认 0.1；可调到 0 更稳，调高会更灵活。
        verbose=DOCUMENT_METADATA_AGENT_VERBOSE,  # 默认 True；调试时保留详细日志。
        allow_delegation=DOCUMENT_METADATA_AGENT_ALLOW_DELEGATION,  # 默认 False；通常不需要把识别任务再分派出去。
        reasoning=DOCUMENT_METADATA_AGENT_REASONING,  # 默认 False；简单抽取任务通常不需要显式 reasoning。
        cache=DOCUMENT_METADATA_AGENT_CACHE,  # 默认 True；相同 prompt 可复用缓存。
    )


# =========================
# Metadata Task Execution
# =========================

def summarize_document_metadata(  # 执行一次“文档基础信息识别任务”，返回可直接写盘的结构化 payload。
    agent: Agent,
    pdf_file_path: str | Path,
    sampled_pages: list[tuple[int, str]],
) -> PdfDocumentMetadataPayload:
    pdf_path = Path(pdf_file_path).expanduser().resolve()

    if not sampled_pages:
        return PdfDocumentMetadataPayload(
            pdf_file_path=str(pdf_path),  # 记录当前 PDF 路径，便于和缓存文件绑定。
            generated_at=datetime.now(timezone.utc).isoformat(),  # 记录生成时间，方便比对是否命中旧结果。
            fingerprint=compute_pdf_fingerprint(pdf_path),  # 用当前 PDF 指纹锁定结果对应的文件版本。
            company_name=pdf_path.stem,  # 没有可识别内容时，默认退回 PDF 文件名。
            industry=DOCUMENT_METADATA_UNKNOWN_INDUSTRY,  # 没有可识别内容时，行业使用统一兜底值。
            source_pages=[],  # 没有采样页时显式记录为空，便于排查。
        )

    prompt = build_document_metadata_task_prompt(sampled_pages)  # 先构造完整任务提示词，便于单独打印调试。

    try:
        result = agent.kickoff(prompt, response_format=PdfDocumentMetadata)  # 优先用 CrewAI 结构化输出，避免手工解析漂移。
        company_name = ""
        industry = ""
        if getattr(result, "pydantic", None):
            company_name = str(result.pydantic.company_name or "")
            industry = str(result.pydantic.industry or "")
        if not company_name or not industry:
            company_name, industry = _extract_metadata_from_raw(getattr(result, "raw", "") or "")
    except Exception:
        company_name = pdf_path.stem  # 模型调用失败时回退到文件名，保证主流程不断。
        industry = DOCUMENT_METADATA_UNKNOWN_INDUSTRY  # 模型调用失败时返回统一未知行业。

    return PdfDocumentMetadataPayload(
        pdf_file_path=str(pdf_path),  # 保存当前 PDF 路径，便于校验缓存。
        generated_at=datetime.now(timezone.utc).isoformat(),  # 标记本次识别时间。
        fingerprint=compute_pdf_fingerprint(pdf_path),  # 把结果绑定到当前 PDF 指纹。
        company_name=_normalize_metadata_value(company_name, pdf_path.stem),  # 公司名为空时退回文件名。
        industry=_normalize_metadata_value(industry, DOCUMENT_METADATA_UNKNOWN_INDUSTRY),  # 行业为空时退回统一兜底值。
        source_pages=[page_number for page_number, _ in sampled_pages],  # 把参与识别的页码写入产物，便于回看。
    )


# =========================
# Metadata Entrypoint
# =========================

def ensure_pdf_document_metadata(  # 文档基础信息总入口：优先复用缓存，必要时重新抽样并识别。
    pdf_file_path: str,
    force_rebuild: bool = DOCUMENT_METADATA_FORCE_REBUILD_DEFAULT,  # 默认 False；改 True 会忽略缓存直接重跑。
) -> dict[str, str]:
    pdf_path = Path(pdf_file_path).expanduser().resolve()
    if not pdf_path.exists():
        raise FileNotFoundError(f"PDF file does not exist: {pdf_path}")

    metadata_path = default_document_metadata_path(pdf_path)  # 固定输出路径，方便主流程和人工查看。
    if not force_rebuild and document_metadata_is_current(pdf_path, metadata_path):
        data = load_document_metadata(metadata_path)
        return {
            "company_name": str(data.get("company_name", "")).strip(),
            "industry": str(data.get("industry", "")).strip(),
            "document_metadata_file_path": str(metadata_path),
        }

    sampled_pages = sample_document_metadata_pages(
        pdf_path,
        max_pages=DOCUMENT_METADATA_SAMPLE_MAX_PAGES,  # 默认取前 15 个非空页；调大可提高识别覆盖面。
        max_chars_per_page=DOCUMENT_METADATA_SAMPLE_MAX_CHARS_PER_PAGE,  # 默认每页取 2500 字；调大可保留更多线索。
    )
    metadata_agent = create_document_metadata_agent()  # 单独创建 agent，便于后续逐项调参数和观察日志。
    payload = summarize_document_metadata(metadata_agent, pdf_path, sampled_pages)  # 执行一次基础信息识别任务。
    saved_metadata_path = save_document_metadata(payload, metadata_path)  # 把本次结构化结果写入 .cache。
    return {
        "company_name": payload.company_name,
        "industry": payload.industry,
        "document_metadata_file_path": saved_metadata_path,
    }
```
### 0027 ASSISTANT (commentary)

```text
现在已经看到现有注释风格了：目前注释偏长，而且很多不是严格围绕“设计目的/功能/参数/默认值原因”。下一步我会把 `crew.py` 和 `main.py` 作为第一优先级重写，再补 `document_metadata.py` 和 PDF 相关核心模块。
```
### 0028 TOOL CALL `shell_command`

```text
{"command":"Get-Content -Raw src\\automated_research_report_generator_v0_1\\pdf_indexing.py","workdir":"C:\\PyProjects\\automated_research_report_generator_v0.1","timeout_ms":20000}
```
### 0029 TOOL CALL `shell_command`

```text
{"command":"Get-Content -Raw src\\automated_research_report_generator_v0_1\\tools\\pdf_page_tools.py","workdir":"C:\\PyProjects\\automated_research_report_generator_v0.1","timeout_ms":20000}
```
### 0030 TOOL CALL `shell_command`

```text
{"command":"Get-Content -Raw src\\automated_research_report_generator_v0_1\\tools\\document_metadata_tools.py","workdir":"C:\\PyProjects\\automated_research_report_generator_v0.1","timeout_ms":20000}
```
### 0031 TOOL CALL `shell_command`

```text
{"command":"Get-Content -Raw src\\automated_research_report_generator_v0_1\\tools\\MarkdownToPdfTool.py","workdir":"C:\\PyProjects\\automated_research_report_generator_v0.1","timeout_ms":20000}
```
### 0032 TOOL OUTPUT `shell_command`

```text
Exit code: 0
Wall time: 0.8 seconds
Output:
from __future__ import annotations

import json
from pathlib import Path
from time import perf_counter

from crewai import Agent
from pydantic import BaseModel, Field

from automated_research_report_generator_v0_1.crew import get_llm
from automated_research_report_generator_v0_1.tools.pdf_page_tools import (
    PdfPageIndexEntry,
    build_page_index_payload,
    default_page_index_path,
    extract_pdf_pages,
    page_index_is_current,
    reset_pdf_page_tool_runtime_state,
    save_page_index,
    set_pdf_context,
)

# =========================
# Page Index Agent Parameters
# =========================

PAGE_INDEX_AGENT_ROLE = "PDF 逐页索引专员"  # 默认角色名；可改成更贴近你内部分工的中文称呼。
PAGE_INDEX_AGENT_GOAL = (  # 默认目标；可改成更强调“筛页”或“目录化索引”的版本。
    "逐页读取 PDF，并为每一页生成一个简短主题标签，帮助后续 agent 快速判断哪些页与自己的分析任务相关。"
)
PAGE_INDEX_AGENT_BACKSTORY = (  # 默认背景设定；可改成更偏招股书、年报或行业报告场景。
    "你专门负责按页阅读 PDF。你不会跨页总结，也不会提前做整份文档分析。"
    "你的唯一职责是只根据当前这一页的内容，输出一个非常短、便于筛页的主题标签。"
)
PAGE_INDEX_AGENT_TEMPERATURE = 0.1  # 默认 0.1；可改到 0 更稳，0.2-0.3 会更灵活但更易漂移。
PAGE_INDEX_AGENT_VERBOSE = True  # 默认 True；调试时方便看 agent 过程，可改 False 收敛日志。
PAGE_INDEX_AGENT_ALLOW_DELEGATION = False  # 默认 False；该 agent 只做单页索引，不需要委派。
PAGE_INDEX_AGENT_REASONING = False  # 默认 False；该任务短且结构化，通常不需要额外 reasoning。
PAGE_INDEX_AGENT_CACHE = True  # 默认 True；相同输入可复用缓存，减少重复调用。
PAGE_INDEX_AGENT_TIMEOUT_SECONDS = 20  # 逐页短调用不应该无限等到底层默认 600 秒。
PAGE_INDEX_AGENT_MAX_RETRIES = 5  # 卡住后的重试次数

# =========================
# Page Index Task Parameters
# =========================

PAGE_INDEX_TOPIC_MAX_CHARS = 10  # 默认主题最长 10 字；可调大到 12-15 字换取更完整标签。
PAGE_INDEX_UNKNOWN_COMPANY = "未知公司"  # 默认公司占位值；上游没识别出公司时用于 prompt。
PAGE_INDEX_EMPTY_PAGE_TOPIC = "空白页"  # 默认空白页标签；可改成“扫描页”“图片页”等更贴近团队习惯的词。
PAGE_INDEX_FORCE_REBUILD_DEFAULT = False  # 默认优先复用缓存；改成 True 会每次都强制重建索引。
PAGE_INDEX_PROMPT_RULES = (  # 默认任务规则；可逐条增删来调整主题标签风格。
    "只输出一个 `topic` 字段。",
    f"`topic` 最多 {PAGE_INDEX_TOPIC_MAX_CHARS} 个字。",
    "只关注当前这一页，不要总结整份文档。",
    "尽量使用具体的业务主题，不要使用空泛表述。",
    "如果本页主要是表格，就概括表格的主题。",
    "如果本页难以判断，就返回一个谨慎、简短的主题。",
    "不要输出解释、前后缀、Markdown 代码块或额外文本。",
)

# =========================
# Page Index Models
# =========================

class PageTopicSummary(BaseModel):
    topic: str = Field(..., description="当前页的简短主题标签")  # 单页主题字段，供后续 agent 快速筛页。

# =========================
# Page Index Helpers
# =========================

def _normalize_topic(topic: str, fallback: str) -> str:  # 清洗 LLM 或启发式产出的主题，统一成稳定短标签。
    normalized = " ".join((topic or "").split())
    normalized = normalized.replace('"', "").replace("'", "").replace("`", "").strip(" ,.;:()[]{}")
    compact = normalized.replace(" ", "").lower()
    if compact in {"json", "{", "}", "topic", "none", "null"}:
        normalized = ""
    if not normalized:
        normalized = fallback
    return normalized[:PAGE_INDEX_TOPIC_MAX_CHARS]

def _heuristic_topic(page_text: str, fallback: str) -> str:  # LLM 失败时退化为“取首个像标题的文本”。
    for line in page_text.splitlines():
        cleaned = " ".join(line.split()).strip(" ,.;:()[]{}")
        if len(cleaned) >= 2:
            return _normalize_topic(cleaned, fallback)
    return fallback

def _extract_topic_from_raw(raw: str) -> str:  # 兼容模型偶发返回 fenced JSON 或裸 JSON 的情况。
    cleaned = (raw or "").strip()
    if not cleaned:
        return ""

    if cleaned.startswith("```"):
        cleaned = cleaned.strip("`").strip()
        if cleaned.lower().startswith("json"):
            cleaned = cleaned[4:].strip()

    try:
        data = json.loads(cleaned)
    except Exception:
        return cleaned

    if isinstance(data, dict):
        return str(data.get("topic", "") or "")
    return ""

def build_page_topic_task_prompt(page_number: int, page_text: str, company_name: str = "") -> str:  # 把“单页主题摘要任务”的提示词集中到一处，便于单独调试。
    prompt_parts = [
        "你是一个投行初级分析师，负责给一页 PDF 打一个简短主题标签。",
        "",
        "规则：",
    ]
    prompt_parts.extend(f"- {rule}" for rule in PAGE_INDEX_PROMPT_RULES)
    prompt_parts.extend(
        [
            "",
            f"公司名称：{company_name or PAGE_INDEX_UNKNOWN_COMPANY}",
            f"页码：{page_number}",
            f"当前页文本：{page_text}",
        ]
    )
    return "\n".join(prompt_parts).strip()

# =========================
# Page Index Agent
# =========================

def create_page_indexer_agent() -> Agent:  # 专门负责“逐页读 PDF 并生成短主题”的独立 agent。
    return Agent(
        role=PAGE_INDEX_AGENT_ROLE,  # 默认“PDF 逐页索引专员”；可改成更贴近你内部角色定义的名字。
        goal=PAGE_INDEX_AGENT_GOAL,  # 默认聚焦“逐页筛页”；可改成更强调招股书目录化摘要。
        backstory=PAGE_INDEX_AGENT_BACKSTORY,  # 默认强调“不跨页总结”；可按文档类型微调语境。
        llm=get_llm(
            temperature=PAGE_INDEX_AGENT_TEMPERATURE,
            timeout=PAGE_INDEX_AGENT_TIMEOUT_SECONDS,
            max_retries=PAGE_INDEX_AGENT_MAX_RETRIES,
        ),  # 逐页预处理要快失败快回退，避免单页卡 10 分钟。
        verbose=PAGE_INDEX_AGENT_VERBOSE,  # 默认 True；调试索引过程时保留详细日志。
        allow_delegation=PAGE_INDEX_AGENT_ALLOW_DELEGATION,  # 默认 False；逐页摘要任务通常不需要委派。
        reasoning=PAGE_INDEX_AGENT_REASONING,  # 默认 False；结构化短任务通常不需要 reasoning。
        cache=PAGE_INDEX_AGENT_CACHE,  # 默认 True；相同 prompt 可复用缓存。
        max_retry_limit=PAGE_INDEX_AGENT_MAX_RETRIES,
    )

# =========================
# Page Index Task Execution
# =========================

def summarize_page_topic(  # 对单页执行一次摘要任务，输出页码到主题的结构化映射。
    agent: Agent,
    page_number: int,
    page_text: str,
    company_name: str = "",  # 默认空字符串；上游识别出公司名时传入可提升主题稳定性。
) -> PdfPageIndexEntry:
    fallback = PAGE_INDEX_EMPTY_PAGE_TOPIC if not page_text.strip() else f"第{page_number}页"  # 默认空白页用固定标签，非空页则回退为页码标签。
    if not page_text.strip():
        return PdfPageIndexEntry(page_number=page_number, topic=fallback)

    prompt = build_page_topic_task_prompt(page_number=page_number, page_text=page_text, company_name=company_name)  # 先集中生成任务 prompt，方便调试时单独打印。
    started_at = perf_counter()

    try:
        result = agent.kickoff(prompt, response_format=PageTopicSummary)  # 优先用 CrewAI 结构化输出，避免手工猜 JSON。
        topic = ""
        if getattr(result, "pydantic", None):
            topic = str(result.pydantic.topic or "")
        if not topic:
            topic = _extract_topic_from_raw(getattr(result, "raw", "") or "")
        elapsed_seconds = perf_counter() - started_at
        print(f"[PDF Index] page {page_number} completed in {elapsed_seconds:.1f}s")
    except Exception as exc:
        elapsed_seconds = perf_counter() - started_at
        print(
            "[PDF Index] page "
            f"{page_number} failed after {elapsed_seconds:.1f}s with "
            f"{type(exc).__name__}: {exc}"
        )
        topic = _heuristic_topic(page_text, fallback)  # 模型失败时退回启发式标题抽取，保证索引流程不中断。

    return PdfPageIndexEntry(
        page_number=page_number,
        topic=_normalize_topic(str(topic), fallback),
    )

# =========================
# Page Index Entrypoint
# =========================

def ensure_pdf_page_index(  # 页索引总入口：优先复用最新 JSON，必要时才重建整本 PDF 的页索引。
    pdf_file_path: str,
    company_name: str = "",  # 默认空字符串；传入公司名可让逐页主题标注更贴近业务语境。
    force_rebuild: bool = PAGE_INDEX_FORCE_REBUILD_DEFAULT,  # 默认 False；改 True 时忽略缓存，强制重建整本索引。
) -> str:
    pdf_path = Path(pdf_file_path).expanduser().resolve()
    if not pdf_path.exists():
        raise FileNotFoundError(f"PDF file does not exist: {pdf_path}")

    index_path = default_page_index_path(pdf_path)  # 固定索引文件名，便于用户和 agent 稳定引用。
    if not force_rebuild and page_index_is_current(pdf_path, index_path):
        set_pdf_context(str(pdf_path), str(index_path))
        return str(index_path)

    pages = extract_pdf_pages(pdf_path)  # 先抽出每一页的纯文本，再逐页做主题标注。
    page_indexer = create_page_indexer_agent()  # 把 agent 创建放在入口里，方便后续单独替换配置。
    page_entries: list[PdfPageIndexEntry] = []

    for page_number, page_text in enumerate(pages, start=1):
        print(f"[PDF Index] summarizing page {page_number}/{len(pages)}")
        page_entries.append(
            summarize_page_topic(
                agent=page_indexer,
                page_number=page_number,
                page_text=page_text,
                company_name=company_name,
            )
        )

    payload = build_page_index_payload(pdf_path, page_entries)  # 写盘前补齐 PDF 名称、指纹和生成时间等元数据。
    saved_index_path = save_page_index(payload, index_path)  # 把最终页索引 JSON 写入 .cache。
    set_pdf_context(str(pdf_path), saved_index_path)  # 更新共享上下文，让后续工具直接复用这份索引。
    return saved_index_path


# =========================
# Shared Runtime Reset
# =========================

def reset_pdf_preprocessing_runtime_state() -> None:  # 运行前清理共享 PDF 页面工具的内存态，但保留 .cache 磁盘缓存。
    reset_pdf_page_tool_runtime_state()


def reset_pdf_indexing_runtime_state() -> None:  # 兼容旧调用名；实际清理的是预处理阶段共享的 PDF 页面缓存。
    reset_pdf_preprocessing_runtime_state()
```
### 0033 TOOL OUTPUT `shell_command`

```text
Exit code: 0
Wall time: 0.8 seconds
Output:
from __future__ import annotations

import hashlib
import json
import re
from datetime import datetime, timezone
from pathlib import Path
from typing import Any

import fitz
from crewai.tools import BaseTool
from pydantic import BaseModel, Field

CURRENT_PDF_FILE_PATH = ""  # 当前运行中的 PDF 绝对路径，由入口在 kickoff 前注入。
CURRENT_PAGE_INDEX_PATH = ""  # 当前运行中的页索引 JSON 绝对路径，供所有 PDF 工具共享。

_PAGE_TEXT_CACHE: dict[str, list[str]] = {}  # 缓存逐页文本，避免同一 PDF 被重复抽取。
_PAGE_INDEX_CACHE: dict[str, dict[str, Any]] = {}  # 缓存已加载 JSON，减少频繁磁盘读取。

MAX_TOOL_PAGE_READ = 75  # 单次工具调用最多直读 100 页，避免 agent 一次拉太多上下文。
PAGE_INDEX_FORMAT_VERSION = 2  # 页索引结构或生成逻辑变化时递增，强制旧缓存自动失效。


def reset_pdf_page_tool_runtime_state() -> None:  # 只清理进程内缓存和上下文，避免同进程多次运行时串数据。
    global CURRENT_PDF_FILE_PATH, CURRENT_PAGE_INDEX_PATH
    CURRENT_PDF_FILE_PATH = ""
    CURRENT_PAGE_INDEX_PATH = ""
    _PAGE_TEXT_CACHE.clear()
    _PAGE_INDEX_CACHE.clear()


class PdfPageIndexEntry(BaseModel):
    page_number: int = Field(..., description="1-based page number in the PDF")  # 页码使用 1-based，便于和人工阅读对齐。
    topic: str = Field(..., description="A short topic label for this page")  # 主题字段对应后续 agent 的筛页依据。


class PdfPageIndexPayload(BaseModel):
    format_version: int  # 索引格式版本，用于淘汰历史坏缓存。
    pdf_file_path: str  # 记录原 PDF 路径，方便校验当前 JSON 是否对应正确文件。
    pdf_name: str  # PDF 文件名用于人工识别。
    generated_at: str  # 生成时间方便判断是否需要重建。
    fingerprint: str  # 指纹用于判断 PDF 内容是否变化。
    page_count: int  # 页数用于选择器范围校验。
    pages: list[PdfPageIndexEntry]  # 真正给 agent 使用的逐页主题列表。


class ReadPdfPageIndexInput(BaseModel):
    keyword: str = Field(  # 可选关键字过滤，便于 agent 先粗筛相关页。
        default="",
        description="Optional keyword to filter the JSON page index. Leave empty to return the full index.",
    )
    max_results: int = Field(  # 限制返回条数，避免一次把整本目录塞进上下文。
        default=0,
        ge=0,
        description="Maximum number of page entries to return after filtering. Use 0 for all matches.",
    )


class ReadPdfPagesInput(BaseModel):
    pages: str = Field(  # 页码选择器支持 1,3,5-8 这种紧凑写法。
        ...,
        description="Page selector such as '3,5,8-10'. Read the page index first, then request only the relevant pages.",
    )


def resolve_pdf_path(pdf_file_path: str) -> Path:  # 把相对路径统一转成绝对路径，避免上下文漂移。
    return Path(pdf_file_path).expanduser().resolve()


def get_output_directory() -> Path:  # 页索引 JSON 统一写入项目内的 .cache/pdf_page_indexes。
    output_dir = Path.cwd() / ".cache" / "pdf_page_indexes"
    output_dir.mkdir(parents=True, exist_ok=True)
    return output_dir


def compute_pdf_fingerprint(pdf_path: str | Path) -> str:  # 用路径、文件大小和修改时间生成轻量指纹。
    path = resolve_pdf_path(str(pdf_path))
    stat = path.stat()
    raw = f"{path}|{stat.st_size}|{stat.st_mtime_ns}"
    return hashlib.sha256(raw.encode("utf-8")).hexdigest()


def default_page_index_path(pdf_file_path: str | Path) -> Path:  # 固定页索引文件名，便于用户和 agent 稳定引用。
    pdf_path = resolve_pdf_path(str(pdf_file_path))
    filename = f"{pdf_path.stem}_page_index.json"
    return get_output_directory() / filename


def set_pdf_context(pdf_file_path: str, page_index_path: str | None = None) -> None:  # 在工具层缓存当前 PDF 和索引路径。
    global CURRENT_PDF_FILE_PATH, CURRENT_PAGE_INDEX_PATH
    CURRENT_PDF_FILE_PATH = str(resolve_pdf_path(pdf_file_path))
    CURRENT_PAGE_INDEX_PATH = (
        str(Path(page_index_path).expanduser().resolve())
        if page_index_path
        else str(default_page_index_path(CURRENT_PDF_FILE_PATH))
    )


def _require_pdf_context() -> Path:  # 所有读取工具在执行前都必须先确认当前 PDF 上下文有效。
    if not CURRENT_PDF_FILE_PATH:
        raise ValueError("CURRENT_PDF_FILE_PATH is empty. Initialize the PDF context first.")

    pdf_path = resolve_pdf_path(CURRENT_PDF_FILE_PATH)
    if not pdf_path.exists():
        raise FileNotFoundError(f"PDF file does not exist: {pdf_path}")
    return pdf_path


def _normalize_page_text(text: str) -> str:  # 清理 PDF 抽取文本中的空字符和多余空白。
    normalized = (text or "").replace("\x00", " ")
    normalized = re.sub(r"[ \t]+", " ", normalized)
    normalized = re.sub(r"\n{3,}", "\n\n", normalized)
    return normalized.strip()


def extract_pdf_pages(pdf_file_path: str | Path) -> list[str]:  # 直接逐页抽出文本，为索引和按页读取共用。
    pdf_path = resolve_pdf_path(str(pdf_file_path))
    cache_key = str(pdf_path)
    if cache_key in _PAGE_TEXT_CACHE:
        return _PAGE_TEXT_CACHE[cache_key]

    document = fitz.open(pdf_path)
    try:
        pages = [
            _normalize_page_text(document.load_page(page_index).get_text("text"))
            for page_index in range(document.page_count)
        ]
    finally:
        document.close()

    _PAGE_TEXT_CACHE[cache_key] = pages
    return pages


def build_page_index_payload(  # 给页索引补齐元数据，形成最终写盘结构。
    pdf_file_path: str | Path,
    page_entries: list[PdfPageIndexEntry],
) -> PdfPageIndexPayload:
    pdf_path = resolve_pdf_path(str(pdf_file_path))
    return PdfPageIndexPayload(
        format_version=PAGE_INDEX_FORMAT_VERSION,
        pdf_file_path=str(pdf_path),
        pdf_name=pdf_path.name,
        generated_at=datetime.now(timezone.utc).isoformat(),
        fingerprint=compute_pdf_fingerprint(pdf_path),
        page_count=len(page_entries),
        pages=page_entries,
    )


def save_page_index(payload: PdfPageIndexPayload, output_path: str | Path | None = None) -> str:  # 把页索引 JSON 保存到固定位置。
    index_path = (
        Path(output_path).expanduser().resolve()
        if output_path
        else default_page_index_path(payload.pdf_file_path)
    )
    index_path.parent.mkdir(parents=True, exist_ok=True)
    data = payload.model_dump()
    index_path.write_text(json.dumps(data, ensure_ascii=False, indent=2), encoding="utf-8")
    _PAGE_INDEX_CACHE[str(index_path)] = data
    return str(index_path)


def load_page_index(page_index_path: str | Path | None = None) -> dict[str, Any]:  # 加载并缓存页索引 JSON。
    path = (
        Path(page_index_path).expanduser().resolve()
        if page_index_path
        else Path(CURRENT_PAGE_INDEX_PATH).expanduser().resolve()
    )
    if not path.exists():
        raise FileNotFoundError(f"Page index JSON does not exist: {path}")

    cache_key = str(path)
    if cache_key in _PAGE_INDEX_CACHE:
        return _PAGE_INDEX_CACHE[cache_key]

    data = json.loads(path.read_text(encoding="utf-8"))
    _PAGE_INDEX_CACHE[cache_key] = data
    return data


def page_index_is_current(pdf_file_path: str | Path, page_index_path: str | Path) -> bool:  # 用指纹判断 JSON 是否仍匹配当前 PDF。
    index_path = Path(page_index_path).expanduser().resolve()
    if not index_path.exists():
        return False

    try:
        data = load_page_index(index_path)
    except (OSError, ValueError, json.JSONDecodeError):
        return False

    return (
        data.get("format_version") == PAGE_INDEX_FORMAT_VERSION
        and data.get("fingerprint") == compute_pdf_fingerprint(pdf_file_path)
    )


def parse_page_selector(page_selector: str, page_count: int) -> list[int]:  # 把 1,3,5-8 这种页码选择器展开成具体页码列表。
    if not page_selector.strip():
        raise ValueError("Page selector cannot be empty.")

    normalized = (
        page_selector.replace("，", ",")
        .replace("；", ",")
        .replace("、", ",")
        .replace("~", "-")
        .replace("～", "-")
        .replace(":", "-")
        .replace("：", "-")
    )

    pages: set[int] = set()
    for part in normalized.split(","):
        chunk = part.strip()
        if not chunk:
            continue

        if "-" in chunk:
            start_str, end_str = chunk.split("-", 1)
            start_page = int(start_str)
            end_page = int(end_str)
            if start_page > end_page:
                start_page, end_page = end_page, start_page
            pages.update(range(start_page, end_page + 1))
            continue

        pages.add(int(chunk))

    if not pages:
        raise ValueError("No valid pages were parsed from the selector.")

    invalid_pages = sorted(page for page in pages if page < 1 or page > page_count)
    if invalid_pages:
        raise ValueError(
            f"Pages out of range: {invalid_pages}. The PDF has {page_count} pages."
        )

    selected_pages = sorted(pages)
    if len(selected_pages) > MAX_TOOL_PAGE_READ:
        raise ValueError(
            f"Too many pages requested at once ({len(selected_pages)}). "
            f"Please narrow the selection to {MAX_TOOL_PAGE_READ} pages or fewer."
        )

    return selected_pages


def format_pdf_pages_for_agent(pdf_file_path: str | Path, page_numbers: list[int]) -> str:  # 把多页文本按 [Page N] 格式拼给 agent。
    pages = extract_pdf_pages(pdf_file_path)
    rendered_pages: list[str] = []
    for page_number in page_numbers:
        page_text = pages[page_number - 1]
        if not page_text:
            page_text = "[No extractable text found on this page. It may be image-only or scanned.]"
        rendered_pages.append(f"[Page {page_number}]\n{page_text}")
    return "\n\n".join(rendered_pages)


class ReadPdfPageIndexTool(BaseTool):
    name: str = "Step 1 - Read PDF Page Index"
    description: str = (
        "Read the current PDF page index JSON. Always call this first before reading any PDF page."
        "Use the page-by-page topic list to decide which exact pages are relevant to the current task."
    )
    args_schema: type[BaseModel] = ReadPdfPageIndexInput

    def _run(self, keyword: str = "", max_results: int = 0) -> str:  # 返回可直接喂给 agent 的页索引 JSON 文本。
        pdf_path = _require_pdf_context()
        index_data = load_page_index()
        pages = index_data.get("pages", [])

        if keyword:
            lowered_keyword = keyword.lower().strip()
            pages = [
                page
                for page in pages
                if lowered_keyword in str(page.get("topic", "")).lower()
                or lowered_keyword in str(page.get("page_number", ""))
            ]

        if max_results > 0:
            pages = pages[:max_results]

        payload = {
            "pdf_file_path": str(pdf_path),
            "page_index_file_path": CURRENT_PAGE_INDEX_PATH,
            "page_count": index_data.get("page_count", len(pages)),
            "pages": pages,
        }
        return json.dumps(payload, ensure_ascii=False, indent=2)


class ReadPdfPagesTool(BaseTool):
    name: str = "Step 2 - Read Relevant PDF Pages"
    description: str = (
        "Read the full extracted text of specific PDF pages directly, without RAG. "
        "Use this only after reading the page index and only request the pages relevant to your task."
    )
    args_schema: type[BaseModel] = ReadPdfPagesInput

    def _run(self, pages: str) -> str:  # 直接返回指定页的完整抽取文本，不做 RAG 召回。
        pdf_path = _require_pdf_context()
        page_texts = extract_pdf_pages(pdf_path)
        page_numbers = parse_page_selector(pages, len(page_texts))
        return format_pdf_pages_for_agent(pdf_path, page_numbers)
```
### 0034 TOOL OUTPUT `shell_command`

```text
Exit code: 0
Wall time: 0.8 seconds
Output:
from __future__ import annotations

import json
from pathlib import Path
from typing import Any

from pydantic import BaseModel

from automated_research_report_generator_v0_1.tools.pdf_page_tools import (
    compute_pdf_fingerprint,
    default_page_index_path,
    extract_pdf_pages,
)

# 鏂囨。鍏冩暟鎹瘑鍒彧閲囨牱鍓嶈嫢骞蹭釜闈炵┖椤碉紝灏介噺鑱氱劍灏侀潰銆佹瑙堛€佷笟鍔′粙缁嶇瓑楂樹俊鍙烽〉闈€?MAX_METADATA_SOURCE_PAGES = 20
MAX_METADATA_PAGE_CHARS = 2500


# =========================
# Metadata Cache / Storage
# =========================

def default_document_metadata_path(pdf_file_path: str | Path) -> Path:  # 鏂囨。鍩虹淇℃伅 JSON 鍜岄〉绱㈠紩涓€鏍疯惤鍒扮粺涓€ .cache 鐩綍锛屼究浜庢煡鐪嬨€?    pdf_path = Path(pdf_file_path).expanduser().resolve()
    return default_page_index_path(pdf_path).with_name(f"{pdf_path.stem}_document_metadata.json")


def load_document_metadata(metadata_path: str | Path) -> dict[str, Any]:  # 璇诲彇宸蹭繚瀛樼殑鍏徃鍚?琛屼笟璇嗗埆缁撴灉銆?    path = Path(metadata_path).expanduser().resolve()
    return json.loads(path.read_text(encoding="utf-8"))


def save_document_metadata(payload: BaseModel, metadata_path: str | Path) -> str:  # 淇濆瓨鍏徃鍚?琛屼笟璇嗗埆缁撴灉锛屼究浜庝汉宸ユ牳瀵广€?    path = Path(metadata_path).expanduser().resolve()
    path.parent.mkdir(parents=True, exist_ok=True)
    path.write_text(
        json.dumps(payload.model_dump(), ensure_ascii=False, indent=2),
        encoding="utf-8",
    )
    return str(path)


def document_metadata_is_current(pdf_file_path: str | Path, metadata_path: str | Path) -> bool:  # 鍒ゆ柇缂撳瓨鐨勫熀纭€淇℃伅鏄惁浠嶅尮閰嶅綋鍓?PDF銆?    path = Path(metadata_path).expanduser().resolve()
    if not path.exists():
        return False

    try:
        data = load_document_metadata(path)
    except (OSError, ValueError, json.JSONDecodeError):
        return False

    return data.get("fingerprint") == compute_pdf_fingerprint(pdf_file_path)


# =========================
# Metadata Page Sampling
# =========================

def sample_document_metadata_pages(  # 涓哄叕鍙稿悕/琛屼笟璇嗗埆閲囨牱鍓嶈嫢骞蹭釜闈炵┖椤碉紝閬垮厤鍜岄〉绱㈠紩娴佺▼娣锋垚涓€濂楅€愰〉澶勭悊閫昏緫銆?    pdf_file_path: str | Path,
    max_pages: int = MAX_METADATA_SOURCE_PAGES,
    max_chars_per_page: int = MAX_METADATA_PAGE_CHARS,
) -> list[tuple[int, str]]:
    sampled_pages: list[tuple[int, str]] = []
    for page_number, page_text in enumerate(extract_pdf_pages(pdf_file_path), start=1):
        if page_text.strip():
            sampled_pages.append((page_number, page_text[:max_chars_per_page]))
        if len(sampled_pages) >= max_pages:
            break
    return sampled_pages
```
### 0035 TOOL OUTPUT `shell_command`

```text
Exit code: 0
Wall time: 0.8 seconds
Output:
from __future__ import annotations

import re
from pathlib import Path
from typing import Type

from bs4 import BeautifulSoup
from markdown import Markdown
from pydantic import BaseModel, Field
from weasyprint import HTML

from crewai.tools import BaseTool


class MarkdownToPdfInput(BaseModel):
    markdown_path: str = Field(..., description="Path to the source markdown file")  # 输入 Markdown 路径。
    pdf_path: str = Field(..., description="Path to the output PDF file")  # 输出 PDF 路径。
    title: str = Field(default="Research Report", description="Document title shown in the PDF metadata")  # PDF 标题元数据。
    auto_landscape: bool = Field(
        default=True,
        description="Automatically switch to landscape layout when wide tables are detected",
    )
    force_landscape: bool = Field(
        default=False,
        description="Force all pages to use landscape layout",
    )


class MarkdownToPdfTool(BaseTool):
    name: str = "markdown_to_pdf"
    description: str = (
        "Convert a markdown file into a polished PDF, optimized for financial reports "
        "with readable tables, repeated headers, wrapped notes, and wide-table support."
    )
    args_schema: Type[BaseModel] = MarkdownToPdfInput

    def _run(  # 把 Markdown 渲染成更适合财报阅读的 PDF。
        self,
        markdown_path: str,
        pdf_path: str,
        title: str = "Research Report",
        auto_landscape: bool = True,
        force_landscape: bool = False,
    ) -> str:
        md_file = Path(markdown_path).expanduser().resolve()
        out_file = Path(pdf_path).expanduser().resolve()

        if not md_file.exists():
            raise FileNotFoundError(f"Markdown file not found: {md_file}")

        out_file.parent.mkdir(parents=True, exist_ok=True)
        md_text = md_file.read_text(encoding="utf-8")
        md_text = self._normalize_markdown_tables(md_text)

        html_body = self._markdown_to_html(md_text)  # 先把 Markdown 转成 HTML，再做表格样式增强。
        soup = BeautifulSoup(html_body, "html.parser")

        self._decorate_tables(soup)

        landscape = force_landscape or (  # 宽表较多时自动切横向，避免列被挤爆。
            auto_landscape and self._needs_landscape(soup)
        )

        final_html = self._build_full_html(
            body_html=str(soup),
            title=title,
            landscape=landscape,
        )

        HTML(
            string=final_html,
            base_url=str(md_file.parent),
        ).write_pdf(str(out_file))

        return f"PDF created successfully at: {out_file}"

    def _markdown_to_html(self, md_text: str) -> str:  # 开启表格等扩展，把 Markdown 变成可排版的 HTML。
        md = Markdown(
            extensions=[
                "extra",  # 一次开启常用扩展，包括表格和 fenced code。
                "tables",
                "toc",
                "sane_lists",
            ]
        )
        return md.convert(md_text)

    def _normalize_markdown_tables(self, md_text: str) -> str:
        """Insert missing blank lines around pipe tables so Python-Markdown can parse them."""
        lines = md_text.splitlines()
        normalized: list[str] = []
        idx = 0

        while idx < len(lines):
            if self._is_table_header(lines, idx):
                if normalized and normalized[-1].strip():
                    normalized.append("")

                while idx < len(lines) and self._is_table_row(lines[idx]):
                    normalized.append(lines[idx].rstrip())
                    idx += 1

                if idx < len(lines) and lines[idx].strip():
                    normalized.append("")
                continue

            normalized.append(lines[idx].rstrip())
            idx += 1

        normalized_text = "\n".join(normalized)
        if md_text.endswith("\n"):
            normalized_text += "\n"
        return normalized_text

    def _is_table_header(self, lines: list[str], idx: int) -> bool:
        if idx + 1 >= len(lines):
            return False
        return self._is_table_row(lines[idx]) and self._is_table_separator(lines[idx + 1])

    def _is_table_row(self, line: str) -> bool:
        stripped = line.strip()
        return stripped.startswith("|") and stripped.endswith("|")

    def _is_table_separator(self, line: str) -> bool:
        stripped = line.strip()
        return bool(
            re.fullmatch(r"\|\s*:?-{3,}:?\s*(\|\s*:?-{3,}:?\s*)+\|", stripped)
        )

    def _decorate_tables(self, soup: BeautifulSoup) -> None:  # 依据列数和字段特征给表格打样式标签。
        for table in soup.find_all("table"):
            headers = []
            first_row = table.find("tr")
            if first_row:
                headers = [cell.get_text(" ", strip=True) for cell in first_row.find_all(["th", "td"])]

            num_cols = len(headers)
            header_text = " | ".join(headers).lower()

            has_year_columns = any(re.fullmatch(r"(19|20)\d{2}", h.strip()) for h in headers)
            has_finance_keywords = any(
                kw in header_text
                for kw in [
                    "metric",
                    "revenue",
                    "ebitda",
                    "margin",
                    "cash flow",
                    "assets",
                    "liabilities",
                    "source",
                    "formula",
                    "roe",
                    "fcf",
                    "capex",
                    "ratio",
                    "收入",
                    "利润",
                    "现金流",
                    "资产",
                    "负债",
                    "附注",
                    "指标",
                    "年份",
                ]
            )

            classes = table.get("class", [])
            classes.append("report-table")

            if has_year_columns or has_finance_keywords:
                classes.append("financial-table")

            if num_cols >= 6:
                classes.append("wide-table")

            table["class"] = classes

            thead = table.find("thead")
            if thead is None:
                first_tr = table.find("tr")
                if first_tr:
                    new_thead = soup.new_tag("thead")
                    first_tr.extract()
                    new_thead.append(first_tr)
                    table.insert(0, new_thead)

            tbody = table.find("tbody")
            if tbody is None:
                rows = table.find_all("tr")
                if rows:
                    body_rows = rows[1:] if table.find("thead") else rows
                    new_tbody = soup.new_tag("tbody")
                    for row in body_rows:
                        row.extract()
                        new_tbody.append(row)
                    table.append(new_tbody)

            for tr in table.find_all("tr"):  # 除首列外，明显是数字的单元格统一右对齐。
                cells = tr.find_all(["th", "td"])
                for idx, cell in enumerate(cells):
                    text = cell.get_text(" ", strip=True)
                    if idx > 0 and self._looks_numeric(text):
                        existing = cell.get("class", [])
                        existing.append("num")
                        cell["class"] = existing

    def _looks_numeric(self, value: str) -> bool:  # 粗判断单元格是否是数字，便于右对齐。
        v = value.replace(",", "").replace(" ", "")
        patterns = [
            r"^\(?-?\d+(\.\d+)?\)?%?$",
            r"^\(?-?\d+(\.\d+)?\)?x$",
            r"^\(?-?\d+(\.\d+)?\)?$",
        ]
        return any(re.fullmatch(p, v, flags=re.IGNORECASE) for p in patterns)

    def _needs_landscape(self, soup: BeautifulSoup) -> bool:  # 根据表格宽度启发式判断是否要横向排版。
        for table in soup.find_all("table"):
            rows = table.find_all("tr")
            if not rows:
                continue

            first_row = rows[0]
            num_cols = len(first_row.find_all(["th", "td"]))
            max_cell_text = max(
                (
                    len(cell.get_text(" ", strip=True))
                    for row in rows[:6]
                    for cell in row.find_all(["th", "td"])
                ),
                default=0,
            )

            if num_cols >= 7:
                return True
            if num_cols >= 6 and max_cell_text >= 28:
                return True
            if "wide-table" in table.get("class", []):
                return True

        return False

    def _build_full_html(self, body_html: str, title: str, landscape: bool) -> str:  # 注入完整 CSS 和页面模板，供 WeasyPrint 渲染。
        page_size = "A4 landscape" if landscape else "A4"

        css = f"""
        @page {{
            size: {page_size};
            margin: 15mm 12mm 15mm 12mm;

            @top-center {{
                content: \"{self._css_escape(title)}\";
                font-size: 9pt;
                color: #666;
            }}

            @bottom-right {{
                content: \"Page \" counter(page);
                font-size: 9pt;
                color: #666;
            }}
        }}

        html {{
            font-size: 11px;
        }}

        body {{
            font-family: Arial, \"Noto Sans CJK SC\", \"Noto Sans SC\", \"Microsoft YaHei\", sans-serif;
            line-height: 1.55;
            color: #111;
            word-break: break-word;
            overflow-wrap: anywhere;
        }}

        h1, h2, h3, h4, h5, h6 {{
            page-break-after: avoid;
            margin-top: 1.1em;
            margin-bottom: 0.45em;
            line-height: 1.25;
        }}

        h1 {{
            font-size: 20px;
            border-bottom: 2px solid #222;
            padding-bottom: 6px;
        }}

        h2 {{
            font-size: 16px;
            border-bottom: 1px solid #999;
            padding-bottom: 4px;
        }}

        h3 {{
            font-size: 13px;
        }}

        p, ul, ol {{
            margin-top: 0.45em;
            margin-bottom: 0.55em;
        }}

        li {{
            margin-bottom: 0.25em;
        }}

        code {{
            font-family: \"Courier New\", monospace;
            font-size: 0.92em;
            background: #f5f5f5;
            padding: 1px 3px;
            border-radius: 3px;
        }}

        pre {{
            background: #f6f6f6;
            border: 1px solid #ddd;
            padding: 10px;
            overflow-x: auto;
            white-space: pre-wrap;
        }}

        blockquote {{
            margin: 0.8em 0;
            padding-left: 10px;
            border-left: 3px solid #bbb;
            color: #444;
        }}

        .report-table {{
            width: 100%;
            border-collapse: collapse;
            table-layout: fixed;
            margin: 10px 0 16px 0;
            font-size: 9.5px;
            line-height: 1.35;
        }}

        .report-table caption {{
            caption-side: top;
            text-align: left;
            font-weight: 700;
            margin-bottom: 6px;
        }}

        .report-table thead {{
            display: table-header-group;
        }}

        .report-table tfoot {{
            display: table-footer-group;
        }}

        .report-table tr {{
            page-break-inside: avoid;
            break-inside: avoid;
        }}

        .report-table th,
        .report-table td {{
            border: 1px solid #999;
            padding: 5px 6px;
            vertical-align: top;
            overflow-wrap: anywhere;
            word-wrap: break-word;
            background-clip: padding-box;
        }}

        .report-table th {{
            font-weight: 700;
            text-align: center;
            background: #efefef;
        }}

        .report-table tbody tr:nth-child(even) td {{
            background: #fafafa;
        }}

        .report-table td.num {{
            text-align: right;
            white-space: nowrap;
            font-variant-numeric: tabular-nums;
        }}

        .financial-table {{  /* 财务表格额外压缩列宽和字号，减少跨页断裂。 */
            font-size: 9px;
        }}

        .financial-table th:first-child,
        .financial-table td:first-child {{
            width: 18%;
            font-weight: 700;
        }}

        .financial-table th:nth-child(2),
        .financial-table td:nth-child(2),
        .financial-table th:nth-child(3),
        .financial-table td:nth-child(3),
        .financial-table th:nth-child(4),
        .financial-table td:nth-child(4),
        .financial-table th:nth-child(5),
        .financial-table td:nth-child(5) {{
            width: 10%;
        }}

        .financial-table th:last-child,
        .financial-table td:last-child {{
            width: 22%;
        }}

        .financial-table th:nth-last-child(2),
        .financial-table td:nth-last-child(2) {{
            width: 20%;
        }}

        .wide-table {{  /* 超宽表进一步缩小字号和内边距，优先保住可读性。 */
            font-size: 8.4px;
        }}

        .wide-table th,
        .wide-table td {{
            padding: 4px 5px;
        }}

        hr {{
            border: none;
            border-top: 1px solid #ccc;
            margin: 16px 0;
        }}
        """

        return f"""<!DOCTYPE html>
<html lang=\"zh-CN\">
<head>
  <meta charset=\"utf-8\">
  <meta name=\"viewport\" content=\"width=device-width, initial-scale=1\">
  <title>{self._html_escape(title)}</title>
  <style>{css}</style>
</head>
<body>
{body_html}
</body>
</html>
"""

    def _html_escape(self, text: str) -> str:  # 避免标题文字破坏 HTML 结构。
        return (
            text.replace("&", "&amp;")
            .replace("<", "&lt;")
            .replace(">", "&gt;")
            .replace('"', "&quot;")
        )

    def _css_escape(self, text: str) -> str:  # 避免标题文字破坏 CSS content 字符串。
        return text.replace("\\", "\\\\").replace('"', '\\"')
```
### 0036 TOOL CALL `update_plan`

```text
{"explanation":"已定位核心源码，并确认项目本地 CrewAI 版本为 1.12.2；线上 PyPI 结果显示最新版本为 1.3.0，存在版本编号异常/回退迹象，因此我只把线上文档作为 API 参考，不做升级假设。接下来按你的注释规范重写核心模块注释，不改业务逻辑。","plan":[{"step":"梳理 core 文件并统一注释规则","status":"completed"},{"step":"重写 crew.py 与 main.py 的类/函数注释","status":"in_progress"},{"step":"重写 document_metadata 与 pdf_indexing 相关核心模块注释","status":"pending"},{"step":"自查语法与注释一致性","status":"pending"}]}
```
### 0037 TOOL OUTPUT `update_plan`

```text
Plan updated
```
### 0038 ASSISTANT (commentary)

```text
我开始改 `crew.py` 和 `main.py` 了。这里会把长注释压成同行短注释，并把 agent/task/crew 的参数说明集中到各自第一个定义里，后面的同类定义只保留职责注释。
```
### 0039 ASSISTANT (commentary)

```text
补丁没有直接套上，我先重新抓一下文件的精确内容和行结构，再分段修改，避免因为换行格式影响到代码本身。
```
### 0040 TOOL CALL `shell_command`

```text
{"command":"Get-Content src\\automated_research_report_generator_v0_1\\crew.py -TotalCount 80","workdir":"C:\\PyProjects\\automated_research_report_generator_v0.1","timeout_ms":10000}
```
### 0041 TOOL CALL `shell_command`

```text
{"command":"Get-Content src\\automated_research_report_generator_v0_1\\main.py -TotalCount 120","workdir":"C:\\PyProjects\\automated_research_report_generator_v0.1","timeout_ms":10000}
```
### 0042 TOOL OUTPUT `shell_command`

```text
Exit code: 0
Wall time: 0.8 seconds
Output:
import os
from dotenv import load_dotenv
from crewai import Agent, Crew, LLM, Process, Task
from crewai.project import CrewBase, agent, crew, task
from crewai_tools import SerperDevTool
from automated_research_report_generator_v0_1.tools.MarkdownToPdfTool import (
    MarkdownToPdfTool,
)
from automated_research_report_generator_v0_1.tools.pdf_page_tools import (
    ReadPdfPageIndexTool,
    ReadPdfPagesTool,
)
load_dotenv()

def get_llm(  # 统一封装项目默认 LLM 配置，但允许不同流程单独收紧超时和重试。
    temperature: float = 0.5,
    timeout: float | int | None = 60,
    max_retries: int | None = 1,
) -> LLM:
    return LLM(
        api_base="https://openrouter.ai/api/v1",
        model="openrouter/google/gemini-3-flash-preview",
        api_key=os.getenv("OPENROUTER_API_KEY"),
        temperature=temperature,
        timeout=timeout,
        max_retries=max_retries,
    )

shared_search_tool = SerperDevTool()  # 需要外部公开信息核对的 agent 共用一个搜索工具实例。
shared_pdf_page_index_tool = ReadPdfPageIndexTool()  # 所有 PDF 分析 agent 先读页索引，再决定页码范围。
shared_pdf_page_reader_tool = ReadPdfPagesTool()  # 所有 PDF 分析 agent 直接读取相关页原文，不再走 RAG。

@CrewBase
class AutomatedResearchReportGeneratorV01Crew:
    """Automated research report generator v0.1 crew."""

    agents_config = "config/agents.yaml"
    tasks_config = "config/tasks.yaml"

    @agent
    def industry_research_analyst(self) -> Agent:  # 代表性示例：多数分析 agent 都沿用这类“索引工具 + 页读取工具”的显式定义。
        return Agent(
            config=self.agents_config["industry_research_analyst"],  # type: ignore[index]
            tools=[
                shared_pdf_page_index_tool,
                shared_pdf_page_reader_tool,
                shared_search_tool,
            ],
            llm=get_llm(temperature=0.3),
            function_calling_llm=None,
            max_iter=25,
            max_rpm=None,
            max_execution_time=None,
            verbose=True,
            allow_delegation=False,
            step_callback=None,
            cache=True,
            allow_code_execution=False,
            max_retry_limit=2,
            respect_context_window=True,
            use_system_prompt=True,
            reasoning=False,
            max_reasoning_attempts=5,
            inject_date=True,
        )

    @agent
    def business_model_analyst(self) -> Agent:
        return Agent(
            config=self.agents_config["business_model_analyst"],  # type: ignore[index]
            tools=[shared_pdf_page_index_tool, shared_pdf_page_reader_tool],
            llm=get_llm(temperature=0.3),
            function_calling_llm=None,
            max_iter=25,
            max_rpm=None,
            max_execution_time=None,
            verbose=True,
            allow_delegation=False,
            step_callback=None,
            cache=True,
```
### 0043 TOOL OUTPUT `shell_command`

```text
Exit code: 0
Wall time: 0.8 seconds
Output:
import subprocess
import sys
from pathlib import Path
import shutil
import yaml


PROJECT_ROOT = Path(__file__).resolve().parents[2]
PROJECT_CACHE_DIR = PROJECT_ROOT / ".cache"
TASKS_CONFIG_PATH = (
    PROJECT_ROOT
    / "src"
    / "automated_research_report_generator_v0_1"
    / "config"
    / "tasks.yaml"
)
CACHE_ENTRIES_TO_KEEP = {".gitignore", ".lock", "CACHEDIR.TAG", "interpreter-v4", "sdists-v9"}
CACHE_ENTRY_PREFIXES_TO_KEEP = (".tmp",)

def reset_crewai_memories() -> None:  # 每次正式运行前清空 CrewAI memory，减少旧运行结果串到本轮。
    try:
        subprocess.run(
            ["crewai", "reset-memories", "--all"],
            check=False,
            capture_output=True,
            text=True,
        )
    except Exception as exc:
        print(f"[WARN] crewai reset-memories failed: {exc}")


def reset_project_cache_dir() -> None:  # 每次预处理前清空项目磁盘缓存，避免混入上次 run 的结果。
    try:
        PROJECT_CACHE_DIR.mkdir(parents=True, exist_ok=True)
        for cache_entry in PROJECT_CACHE_DIR.iterdir():
            if (
                cache_entry.name in CACHE_ENTRIES_TO_KEEP
                or cache_entry.name.startswith(CACHE_ENTRY_PREFIXES_TO_KEEP)
            ):
                continue

            if cache_entry.is_dir():
                shutil.rmtree(cache_entry)
            else:
                cache_entry.unlink()
    except Exception as exc:
        print(f"[WARN] project cache reset failed: {exc}")

def prepare_inputs() -> dict[str, str]:  # 在 crew 启动前先补齐固定页索引 JSON 路径。
    from automated_research_report_generator_v0_1.document_metadata import (
        ensure_pdf_document_metadata,
    )
    from automated_research_report_generator_v0_1.pdf_indexing import (
        ensure_pdf_page_index,
        reset_pdf_preprocessing_runtime_state,
    )

    reset_project_cache_dir()  # 强制清空项目 .cache，确保本轮重新生成预处理结果。
    reset_pdf_preprocessing_runtime_state()  # 每次运行前清理共享 PDF 页面缓存，避免同进程串上次结果。
    pdf_file_path = r"pdf/sehk26033003936_c.pdf"  # 当前 demo 默认处理的 PDF 路径。
#sehk26022601053_c x
#sehk26022702474_c x
#sehk26030502134_c x
#sehk26033003882_c x
#sehk26033003936_c x

    # Step 1: 先跑独立的文档元数据识别流程，自动识别公司名称和行业。
    document_metadata = ensure_pdf_document_metadata(
        pdf_file_path=pdf_file_path,
    )

    # Trace 展示的是 kickoff 当下的 inputs，所以这里要稳定带上公司名和行业两个标签。
    resolved_company_name = (document_metadata.get("company_name") or Path(pdf_file_path).stem).strip()
    resolved_industry = (document_metadata.get("industry") or "未知行业").strip()
    prepared_inputs: dict[str, str] = {
        "pdf_file_path": pdf_file_path,
        "company_name": resolved_company_name,
        "industry": resolved_industry,
    }

    # Step 2: 再跑页索引流程，只负责“逐页主题摘要”和页码定位。
    page_index_file_path = ensure_pdf_page_index(
        pdf_file_path=pdf_file_path,
        company_name=prepared_inputs["company_name"],
    )
    prepared_inputs["page_index_file_path"] = page_index_file_path  # 让后续 agent/tool 都能拿到固定索引路径。
    prepared_inputs["document_metadata_file_path"] = document_metadata["document_metadata_file_path"]  # 保留基础信息缓存文件路径，便于查看。
    return prepared_inputs


def validate_tasks_config() -> None:  # 提前校验 YAML 里的 context 依赖，避免 CrewAI 只抛模糊的 KeyError。
    with TASKS_CONFIG_PATH.open("r", encoding="utf-8") as tasks_file:
        tasks_config = yaml.safe_load(tasks_file) or {}

    if not isinstance(tasks_config, dict):
        raise ValueError(f"Invalid tasks config format: {TASKS_CONFIG_PATH}")

    task_names = set(tasks_config.keys())
    for task_name, task_config in tasks_config.items():
        if not isinstance(task_config, dict):
            raise ValueError(f"Task '{task_name}' config must be a mapping in {TASKS_CONFIG_PATH}")

        context_list = task_config.get("context")
        if context_list is None:
            continue

        if not isinstance(context_list, list):
            raise ValueError(f"Task '{task_name}' context must be a list in {TASKS_CONFIG_PATH}")

        for position, context_task_name in enumerate(context_list, start=1):
            if not isinstance(context_task_name, str) or not context_task_name.strip():
                raise ValueError(
                    f"Task '{task_name}' has an empty context entry at position {position} in {TASKS_CONFIG_PATH}"
                )

            if context_task_name not in task_names:
                raise ValueError(
                    f"Task '{task_name}' references unknown context task '{context_task_name}' in {TASKS_CONFIG_PATH}"
                )
```
### 0044 TOOL CALL `shell_command`

```text
{"command":"Format-Hex -Path src\\automated_research_report_generator_v0_1\\crew.py | Select-Object -First 5","workdir":"C:\\PyProjects\\automated_research_report_generator_v0.1","timeout_ms":10000}
```
### 0045 TOOL OUTPUT `shell_command`

```text
Exit code: 0
Wall time: 1 seconds
Output:


           Path: C:\PyProjects\automated_research_report_generator_v0.1\src\automated_research_report_generator_v0_1\cr
ew.py

           00 01 02 03 04 05 06 07 08 09 0A 0B 0C 0D 0E 0F

00000000   EF BB BF 69 6D 70 6F 72 74 20 6F 73 0A 66 72 6F  ï»¿import os.fro
00000010   6D 20 64 6F 74 65 6E 76 20 69 6D 70 6F 72 74 20  m dotenv import 
00000020   6C 6F 61 64 5F 64 6F 74 65 6E 76 0A 66 72 6F 6D  load_dotenv.from
00000030   20 63 72 65 77 61 69 20 69 6D 70 6F 72 74 20 41   crewai import A
00000040   67 65 6E 74 2C 20 43 72 65 77 2C 20 4C 4C 4D 2C  gent, Crew, LLM,
```
### 0046 TOOL CALL `shell_command`

```text
{"command":"Get-Content -Raw src\\automated_research_report_generator_v0_1\\crew.py","workdir":"C:\\PyProjects\\automated_research_report_generator_v0.1","timeout_ms":20000}
```
### 0047 TOOL OUTPUT `shell_command`

```text
Exit code: 0
Wall time: 1 seconds
Output:
import os
from dotenv import load_dotenv
from crewai import Agent, Crew, LLM, Process, Task
from crewai.project import CrewBase, agent, crew, task
from crewai_tools import SerperDevTool
from automated_research_report_generator_v0_1.tools.MarkdownToPdfTool import (
    MarkdownToPdfTool,
)
from automated_research_report_generator_v0_1.tools.pdf_page_tools import (
    ReadPdfPageIndexTool,
    ReadPdfPagesTool,
)
load_dotenv()


"""LLM 与 Crew 定义"""


def get_llm(  # 设计：统一 LLM 入口；功能：按流程覆写采样与容错；可调：temperature 常用 0-1、timeout>0/None、max_retries>=0/None；默认 0.5/60/1 兼顾稳定与响应速度。
    temperature: float = 0.5,
    timeout: float | int | None = 60,
    max_retries: int | None = 1,
) -> LLM:
    return LLM(
        api_base="https://openrouter.ai/api/v1",
        model="openrouter/google/gemini-3-flash-preview",
        api_key=os.getenv("OPENROUTER_API_KEY"),
        temperature=temperature,
        timeout=timeout,
        max_retries=max_retries,
    )

shared_search_tool = SerperDevTool()  # 设计：复用搜索实例；功能：补公开信息核验；默认全局共享以减少重复初始化。
shared_pdf_page_index_tool = ReadPdfPageIndexTool()  # 设计：统一筛页入口；功能：先读页索引再选页；默认全员共用以固定调用顺序。
shared_pdf_page_reader_tool = ReadPdfPagesTool()  # 设计：统一读页入口；功能：直读指定页文本；默认共用以避免重复实现。

@CrewBase
class AutomatedResearchReportGeneratorV01Crew:  # 设计：集中装配研究报告主流程；功能：把 YAML 配置映射为 agents、tasks 与 crew；默认顺序执行以保证上下游稳定。

    agents_config = "config/agents.yaml"
    tasks_config = "config/tasks.yaml"

    @agent
    def industry_research_analyst(self) -> Agent:  # 设计：定义首个标准分析 agent；功能：行业研究并示范通用 agent 配置；默认低温度和禁委派以提高可控性。
        return Agent(
            config=self.agents_config["industry_research_analyst"],  # type: ignore[index]  # 默认读 YAML；可切换同结构配置；原因是角色文案与代码解耦。
            tools=[  # 可调：按任务增减工具；默认索引+读页+搜索，原因是行业研究同时需要文档定位与外部核验。
                shared_pdf_page_index_tool,
                shared_pdf_page_reader_tool,
                shared_search_tool,
            ],
            llm=get_llm(temperature=0.3),  # 可调：temperature 常用 0-1；默认 0.3，原因是分析要稳但仍保留少量归纳弹性。
            function_calling_llm=None,  # 可调：独立工具调用模型/None；默认 None，原因是沿用主模型便于维护。
            max_iter=25,  # 可调：建议 >=1；默认 25，原因是给复杂任务足够迭代空间。
            max_rpm=None,  # 可调：正整数/None；默认 None，原因是先不做人为限流。
            max_execution_time=None,  # 可调：秒数/None；默认 None，原因是交给上层超时控制。
            verbose=True,  # 可调：True/False；默认 True，原因是当前阶段更重视可观测性。
            allow_delegation=False,  # 可调：True/False；默认 False，原因是角色边界已明确。
            step_callback=None,  # 可调：回调函数/None；默认 None，原因是暂不插入额外钩子。
            cache=True,  # 可调：True/False；默认 True，原因是减少重复调用成本。
            allow_code_execution=False,  # 可调：True/False；默认 False，原因是当前任务以阅读分析为主。
            max_retry_limit=2,  # 可调：建议 >=0；默认 2，原因是兼顾重试收益与耗时。
            respect_context_window=True,  # 可调：True/False；默认 True，原因是避免长上下文溢出。
            use_system_prompt=True,  # 可调：True/False；默认 True，原因是保留系统约束。
            reasoning=False,  # 可调：True/False；默认 False，原因是此流程更依赖显式任务拆分。
            max_reasoning_attempts=5,  # 可调：建议 >=1/None；默认 5，原因是与 reasoning 兼容保留上限。
            inject_date=True,  # 可调：True/False；默认 True，原因是搜索与时效判断需要日期上下文。
        )

    @agent
    def business_model_analyst(self) -> Agent:  # 设计：聚焦商业模式；功能：只读 PDF 内部证据；默认不挂搜索以减少外部噪声。
        return Agent(
            config=self.agents_config["business_model_analyst"],  # type: ignore[index]
            tools=[shared_pdf_page_index_tool, shared_pdf_page_reader_tool],
            llm=get_llm(temperature=0.3),
            function_calling_llm=None,
            max_iter=25,
            max_rpm=None,
            max_execution_time=None,
            verbose=True,
            allow_delegation=False,
            step_callback=None,
            cache=True,
            allow_code_execution=False,
            max_retry_limit=2,
            respect_context_window=True,
            use_system_prompt=True,
            reasoning=False,
            max_reasoning_attempts=None,
            inject_date=True,
        )

    @agent
    def financial_analysis_specialist(self) -> Agent:  # 设计：聚焦财务分析；功能：从原文抽取并归纳财务结论；默认低温度以压缩主观波动。
        return Agent(
            config=self.agents_config["financial_analysis_specialist"],  # type: ignore[index]
            tools=[shared_pdf_page_index_tool, shared_pdf_page_reader_tool],
            llm=get_llm(temperature=0.3),
            function_calling_llm=None,
            max_iter=25,
            max_rpm=None,
            max_execution_time=None,
            verbose=True,
            allow_delegation=False,
            step_callback=None,
            cache=True,
            allow_code_execution=False,
            max_retry_limit=2,
            respect_context_window=True,
            use_system_prompt=True,
            reasoning=False,
            max_reasoning_attempts=None,
            inject_date=True,
        )

    @agent
    def operational_data_analyst(self) -> Agent:  # 设计：聚焦经营指标；功能：提炼运营数据与趋势；默认只用 PDF 工具以保持证据闭环。
        return Agent(
            config=self.agents_config["operational_data_analyst"],  # type: ignore[index]
            tools=[shared_pdf_page_index_tool, shared_pdf_page_reader_tool],
            llm=get_llm(temperature=0.3),
            function_calling_llm=None,
            max_iter=25,
            max_rpm=None,
            max_execution_time=None,
            verbose=True,
            allow_delegation=False,
            step_callback=None,
            cache=True,
            allow_code_execution=False,
            max_retry_limit=2,
            respect_context_window=True,
            use_system_prompt=True,
            reasoning=False,
            max_reasoning_attempts=None,
            inject_date=True,
        )

    @agent
    def management_assessment_analyst(self) -> Agent:  # 设计：聚焦管理层评估；功能：结合文档与外部信息交叉判断；默认附带搜索以补公开背景。
        return Agent(
            config=self.agents_config["management_assessment_analyst"],  # type: ignore[index]
            tools=[
                shared_pdf_page_index_tool,
                shared_pdf_page_reader_tool,
                shared_search_tool,
            ],
            llm=get_llm(temperature=0.3),
            function_calling_llm=None,
            max_iter=25,
            max_rpm=None,
            max_execution_time=None,
            verbose=True,
            allow_delegation=False,
            step_callback=None,
            cache=True,
            allow_code_execution=False,
            max_retry_limit=2,
            respect_context_window=True,
            use_system_prompt=True,
            reasoning=False,
            max_reasoning_attempts=None,
            inject_date=True,
        )

    @agent
    def risk_assessment_analyst(self) -> Agent:  # 设计：聚焦风险识别；功能：总结主要经营与投资风险；默认保守配置以降低误判。
        return Agent(
            config=self.agents_config["risk_assessment_analyst"],  # type: ignore[index]
            tools=[shared_pdf_page_index_tool, shared_pdf_page_reader_tool],
            llm=get_llm(temperature=0.3),
            function_calling_llm=None,
            max_iter=25,
            max_rpm=None,
            max_execution_time=None,
            verbose=True,
            allow_delegation=False,
            step_callback=None,
            cache=True,
            allow_code_execution=False,
            max_retry_limit=2,
            respect_context_window=True,
            use_system_prompt=True,
            reasoning=False,
            max_reasoning_attempts=None,
            inject_date=True,
        )

    @agent
    def strategic_capital_analyst(self) -> Agent:  # 设计：聚焦战略与资本计划；功能：提炼募资、扩产与资本动作；默认只依赖招股书证据。
        return Agent(
            config=self.agents_config["strategic_capital_analyst"],  # type: ignore[index]
            tools=[shared_pdf_page_index_tool, shared_pdf_page_reader_tool],
            llm=get_llm(temperature=0.3),
            function_calling_llm=None,
            max_iter=25,
            max_rpm=None,
            max_execution_time=None,
            verbose=True,
            allow_delegation=False,
            step_callback=None,
            cache=True,
            allow_code_execution=False,
            max_retry_limit=2,
            respect_context_window=True,
            use_system_prompt=True,
            reasoning=False,
            max_reasoning_attempts=None,
            inject_date=True,
        )

    @agent
    def comparable_company_identifier(self) -> Agent:  # 设计：筛选可比公司；功能：依赖外部搜索建立可比池；默认较高温度以放宽候选覆盖。
        return Agent(
            config=self.agents_config["comparable_company_identifier"],  # type: ignore[index]
            tools=[shared_search_tool],
            llm=get_llm(temperature=0.5),
            function_calling_llm=None,
            max_iter=25,
            max_rpm=None,
            max_execution_time=None,
            verbose=True,
            allow_delegation=False,
            step_callback=None,
            cache=True,
            allow_code_execution=False,
            max_retry_limit=2,
            respect_context_window=True,
            use_system_prompt=True,
            reasoning=False,
            max_reasoning_attempts=None,
            inject_date=True,
        )

    @agent
    def comparable_company_data_extractor(self) -> Agent:  # 设计：抽取可比数据；功能：为估值环节整理公开指标；默认搜索驱动以覆盖多来源。
        return Agent(
            config=self.agents_config["comparable_company_data_extractor"],  # type: ignore[index]
            tools=[shared_search_tool],
            llm=get_llm(temperature=0.3),
            function_calling_llm=None,
            max_iter=25,
            max_rpm=None,
            max_execution_time=None,
            verbose=True,
            allow_delegation=False,
            step_callback=None,
            cache=True,
            allow_code_execution=False,
            max_retry_limit=2,
            respect_context_window=True,
            use_system_prompt=True,
            reasoning=False,
            max_reasoning_attempts=None,
            inject_date=True,
        )

    @agent
    def valuation_analysis_specialist(self) -> Agent:  # 设计：聚焦估值分析；功能：基于可比数据形成估值判断；默认温度略高以支持方案比较。
        return Agent(
            config=self.agents_config["valuation_analysis_specialist"],  # type: ignore[index]
            tools=[shared_search_tool],
            llm=get_llm(temperature=0.7),
            function_calling_llm=None,
            max_iter=25,
            max_rpm=None,
            max_execution_time=None,
            verbose=True,
            allow_delegation=False,
            step_callback=None,
            cache=True,
            allow_code_execution=False,
            max_retry_limit=2,
            respect_context_window=True,
            use_system_prompt=True,
            reasoning=False,
            max_reasoning_attempts=None,
            inject_date=True,
        )
    
    @agent
    def investment_highlights_and_risk_scorer(self) -> Agent:  # 设计：统一打分；功能：汇总亮点与风险形成投资结论；默认无工具以强制基于前序产物。
        return Agent(
            config=self.agents_config["investment_highlights_and_risk_scorer"],  # type: ignore[index]
            tools=[],
            llm=get_llm(temperature=0.8),
            function_calling_llm=None,
            max_iter=25,
            max_rpm=None,
            max_execution_time=None,
            verbose=True,
            allow_delegation=False,
            step_callback=None,
            cache=True,
            allow_code_execution=False,
            max_retry_limit=2,
            respect_context_window=True,
            use_system_prompt=True,
            reasoning=False,
            max_reasoning_attempts=None,
            inject_date=True,
        )

    @agent
    def management_communication_specialist(self) -> Agent:  # 设计：生成管理层沟通问题；功能：把研究结果转成尽调问法；默认中高温度以扩展问题角度。
        return Agent(
            config=self.agents_config["management_communication_specialist"],  # type: ignore[index]
            tools=[],
            llm=get_llm(temperature=0.7),
            function_calling_llm=None,
            max_iter=25,
            max_rpm=None,
            max_execution_time=None,
            verbose=True,
            allow_delegation=False,
            step_callback=None,
            cache=True,
            allow_code_execution=False,
            max_retry_limit=2,
            respect_context_window=True,
            use_system_prompt=True,
            reasoning=False,
            max_reasoning_attempts=None,
            inject_date=True,
        )

    @agent
    def research_report_writer(self) -> Agent:  # 设计：统一成文；功能：把多段分析汇编成报告；默认低温度以保持措辞稳定。
        return Agent(
            config=self.agents_config["research_report_writer"],  # type: ignore[index]
            tools=[],
            llm=get_llm(temperature=0.2),
            function_calling_llm=None,
            max_iter=25,
            max_rpm=None,
            max_execution_time=None,
            verbose=True,
            allow_delegation=False,
            step_callback=None,
            cache=True,
            allow_code_execution=False,
            max_retry_limit=2,
            respect_context_window=True,
            use_system_prompt=True,
            reasoning=False,
            max_reasoning_attempts=None,
            inject_date=True,
        )

    @agent
    def qa_consistency_analyst(self) -> Agent:  # 设计：最终质检；功能：检查一致性与遗漏；默认中温度以保留纠错敏感度。
        return Agent(
            config=self.agents_config["qa_consistency_analyst"],  # type: ignore[index]
            tools=[],
            llm=get_llm(temperature=0.7),
            function_calling_llm=None,
            max_iter=25,
            max_rpm=None,
            max_execution_time=None,
            verbose=True,
            allow_delegation=False,
            step_callback=None,
            cache=True,
            allow_code_execution=False,
            max_retry_limit=2,
            respect_context_window=True,
            use_system_prompt=True,
            reasoning=False,
            max_reasoning_attempts=None,
            inject_date=True,
        )


    @task
    def conduct_industry_research_analysis(self) -> Task:  # 设计：定义首个标准任务；功能：示范 YAML 驱动的 task 运行选项；默认同步与 markdown 输出以便衔接后续任务。
        return Task(
            config=self.tasks_config["conduct_industry_research_analysis"],  # type: ignore[index]  # 默认读 YAML；可切换同结构任务配置；原因是任务文本与代码解耦。
            tools=[],  # 可调：按任务补充工具；默认空，原因是工具已挂在 agent 侧统一管理。
            async_execution=False,  # 可调：True/False；默认 False，原因是当前流程强依赖顺序上下文。
            output_json=None,  # 可调：Pydantic/None；默认 None，原因是该任务先输出自然语言 markdown。
            output_pydantic=None,  # 可调：Pydantic/None；默认 None，原因是暂不要求强结构化。
            human_input=False,  # 可调：True/False；默认 False，原因是主流程先保持全自动。
            cache=True,  # 可调：True/False；默认 True，原因是减少重复执行成本。
            markdown=True,  # 可调：True/False；默认 True，原因是后续汇编报告更方便。
        )

    @task
    def analyze_business_model(self) -> Task:  # 设计：商业模式任务；功能：输出商业模式分析；默认沿用通用任务配置以统一产物格式。
        return Task(
            config=self.tasks_config["analyze_business_model"],  # type: ignore[index]
            tools=[],
            async_execution=False,
            output_json=None,
            output_pydantic=None,
            human_input=False,
            cache=True,
            markdown=True,
        )

    @task
    def analyze_management(self) -> Task:  # 设计：管理层任务；功能：输出管理层评估；默认同步执行以等待前序材料。
        return Task(
            config=self.tasks_config["analyze_management"],  # type: ignore[index]
            tools=[],
            async_execution=False,
            output_json=None,
            output_pydantic=None,
            human_input=False,
            cache=True,
            markdown=True,
        )

    @task
    def conduct_financial_analysis(self) -> Task:  # 设计：财务任务；功能：输出财务分析；默认 markdown 便于后续引用。
        return Task(
            config=self.tasks_config["conduct_financial_analysis"],  # type: ignore[index]
            tools=[],
            async_execution=False,
            output_json=None,
            output_pydantic=None,
            human_input=False,
            cache=True,
            markdown=True,
        )

    @task
    def analyze_operational_metrics(self) -> Task:  # 设计：经营指标任务；功能：输出运营分析；默认缓存开启以减少重复运行。
        return Task(
            config=self.tasks_config["analyze_operational_metrics"],  # type: ignore[index]
            tools=[],
            async_execution=False,
            output_json=None,
            output_pydantic=None,
            human_input=False,
            cache=True,
            markdown=True,
        )

    @task
    def assess_investment_risks(self) -> Task:  # 设计：风险任务；功能：输出投资风险评估；默认同步保证依赖完整。
        return Task(
            config=self.tasks_config["assess_investment_risks"],  # type: ignore[index]
            tools=[],
            async_execution=False,
            output_json=None,
            output_pydantic=None,
            human_input=False,
            cache=True,
            markdown=True,
        )

    @task
    def analyze_strategic_capital_plans(self) -> Task:  # 设计：战略资本任务；功能：输出资本计划分析；默认通用配置保持一致。
        return Task(
            config=self.tasks_config["analyze_strategic_capital_plans"],  # type: ignore[index]
            tools=[],
            async_execution=False,
            output_json=None,
            output_pydantic=None,
            human_input=False,
            cache=True,
            markdown=True,
        )

    @task
    def identify_comparable_companies(self) -> Task:  # 设计：可比公司筛选任务；功能：建立可比池；默认 markdown 方便人工复核。
        return Task(
            config=self.tasks_config["identify_comparable_companies"],  # type: ignore[index]
            tools=[],
            async_execution=False,
            output_json=None,
            output_pydantic=None,
            human_input=False,
            cache=True,
            markdown=True,
        )

    @task
    def extract_comparable_company_data(self) -> Task:  # 设计：可比数据任务；功能：抽取估值所需数据；默认顺序执行以承接可比池。
        return Task(
            config=self.tasks_config["extract_comparable_company_data"],  # type: ignore[index]
            tools=[],
            async_execution=False,
            output_json=None,
            output_pydantic=None,
            human_input=False,
            cache=True,
            markdown=True,
        )

    @task
    def perform_comparable_valuation_analysis(self) -> Task:  # 设计：估值任务；功能：形成可比估值结果；默认缓存以便调试复跑。
        return Task(
            config=self.tasks_config["perform_comparable_valuation_analysis"],  # type: ignore[index]
            tools=[],
            async_execution=False,
            output_json=None,
            output_pydantic=None,
            human_input=False,
            cache=True,
            markdown=True,
        )

    @task
    def investment_analysis_and_scoring(self) -> Task:  # 设计：打分任务；功能：汇总前序结论形成投资评分；默认 markdown 便于写入总报告。
        return Task(
            config=self.tasks_config["investment_analysis_and_scoring"],  # type: ignore[index]
            tools=[],
            async_execution=False,
            output_json=None,
            output_pydantic=None,
            human_input=False,
            cache=True,
            markdown=True,
        )

    @task
    def identify_management_communication_questions(self) -> Task:  # 设计：沟通问题任务；功能：输出管理层问询清单；默认全自动生成初稿。
        return Task(
            config=self.tasks_config["identify_management_communication_questions"],  # type: ignore[index]
            tools=[],
            async_execution=False,
            output_json=None,
            output_pydantic=None,
            human_input=False,
            cache=True,
            markdown=True,
        )

    @task
    def final_quality_review(self) -> Task:  # 设计：质检任务；功能：做最终一致性复核；默认同步执行以站在末端总览。
        return Task(
            config=self.tasks_config["final_quality_review"],  # type: ignore[index]
            tools=[],
            async_execution=False,
            output_json=None,
            output_pydantic=None,
            human_input=False,
            cache=True,
            markdown=True,
        )

    @task
    def compile_research_report(self) -> Task:  # 设计：汇编任务；功能：整合各段分析为完整报告；默认 markdown 输出供 PDF 转换。
        return Task(
            config=self.tasks_config["compile_research_report"],  # type: ignore[index]
            tools=[],
            async_execution=False,
            output_json=None,
            output_pydantic=None,
            human_input=False,
            cache=True,
            markdown=True,
        )

    @task
    def convert_research_report_md_to_pdf(self) -> Task:  # 设计：收口任务；功能：把 Markdown 报告转成 PDF；默认关闭 markdown 因为结果是文件产物。
        return Task(
            config=self.tasks_config["convert_research_report_md_to_pdf"],  # type: ignore[index]
            tools=[MarkdownToPdfTool(result_as_answer=True)],
            async_execution=False,
            output_json=None,
            output_pydantic=None,
            human_input=False,
            cache=True,
            markdown=False,
        )

    @crew
    def crew(self) -> Crew:  # 设计：组装主 crew；功能：串联全部 agents 与 tasks；默认顺序流程和开启 trace，原因是链路依赖强且当前重视可观测性。
        return Crew(
            name="automated_research_report_generator_v0.1",  # 可调：任意唯一名称；默认与项目同名，原因是日志更易定位。
            agents=self.agents,  # 默认收集全部 @agent；通常无需手改，原因是由 CrewBase 自动装配。
            tasks=self.tasks,  # 默认收集全部 @task；通常无需手改，原因是保持定义顺序即执行顺序。
            process=Process.sequential,  # 可调：sequential/hierarchical；默认 sequential，原因是任务依赖链明确。
            verbose=True,  # 可调：True/False；默认 True，原因是当前阶段更重视调试信息。
            manager_llm=None,  # 可调：层级流程时指定；默认 None，原因是当前不走 manager 模式。
            manager_agent=None,  # 可调：自定义 manager/None；默认 None，原因是顺序流程不需要。
            function_calling_llm=None,  # 可调：独立工具调用模型/None；默认 None，原因是复用主模型即可。
            config=None,  # 可调：额外 crew 配置/None；默认 None，原因是当前参数已显式写出。
            max_rpm=None,  # 可调：正整数/None；默认 None，原因是先不做全局限流。
            memory=False,  # 可调：True/False；默认 False，原因是避免旧运行污染当前报告。
            cache=True,  # 可调：True/False；默认 True，原因是降低重复执行成本。
            embedder=None,  # 可调：嵌入器配置/None；默认 None，原因是当前未启用 memory/knowledge。
            full_output=False,  # 可调：True/False；默认 False，原因是先收敛返回体体积。
            step_callback=None,  # 可调：回调函数/None；默认 None，原因是暂不加自定义钩子。
            task_callback=None,  # 可调：回调函数/None；默认 None，原因是日志已够用。
            share_crew=False,  # 可调：True/False；默认 False，原因是避免跨会话共享状态。
            output_log_file=True,  # 可调：True/路径/False；默认 True，原因是保留落盘日志便于复盘。
            planning=False,  # 可调：True/False；默认 False，原因是任务已人工拆分完毕。
            planning_llm=None,  # 可调：规划模型/None；默认 None，原因是未启用 planning。
            trace=True,  # 可调：True/False；默认 True，原因是需要执行链路追踪。
            chat_llm=get_llm(),  # 可调：任意 LLM；默认项目主模型，原因是交互场景保持一致。
        )
```
### 0048 ASSISTANT (commentary)

```text
`crew.py` 已经按规则收紧了。现在我在处理入口和预处理模块，会顺手把那种历史遗留、信息价值不高的注释也清掉，尽量让核心代码只剩“有用的短注释”。
```
### 0049 ASSISTANT (commentary)

```text
我在处理中间模块了。这里我会尽量保持“读代码时一扫就懂”：大块逻辑只留标题式注释，函数和类都压成一行说明，不再堆长段解释。
```
### 0050 TOOL CALL `shell_command`

```text
{"command":"Get-Content -Raw src\\automated_research_report_generator_v0_1\\tools\\document_metadata_tools.py","workdir":"C:\\PyProjects\\automated_research_report_generator_v0.1","timeout_ms":20000}
```
### 0051 TOOL OUTPUT `shell_command`

```text
Exit code: 0
Wall time: 1.1 seconds
Output:
from __future__ import annotations

import json
from pathlib import Path
from typing import Any

from pydantic import BaseModel

from automated_research_report_generator_v0_1.tools.pdf_page_tools import (
    compute_pdf_fingerprint,
    default_page_index_path,
    extract_pdf_pages,
)

# 鏂囨。鍏冩暟鎹瘑鍒彧閲囨牱鍓嶈嫢骞蹭釜闈炵┖椤碉紝灏介噺鑱氱劍灏侀潰銆佹瑙堛€佷笟鍔′粙缁嶇瓑楂樹俊鍙烽〉闈€?MAX_METADATA_SOURCE_PAGES = 20
MAX_METADATA_PAGE_CHARS = 2500


# =========================
# Metadata Cache / Storage
# =========================

def default_document_metadata_path(pdf_file_path: str | Path) -> Path:  # 鏂囨。鍩虹淇℃伅 JSON 鍜岄〉绱㈠紩涓€鏍疯惤鍒扮粺涓€ .cache 鐩綍锛屼究浜庢煡鐪嬨€?    pdf_path = Path(pdf_file_path).expanduser().resolve()
    return default_page_index_path(pdf_path).with_name(f"{pdf_path.stem}_document_metadata.json")


def load_document_metadata(metadata_path: str | Path) -> dict[str, Any]:  # 璇诲彇宸蹭繚瀛樼殑鍏徃鍚?琛屼笟璇嗗埆缁撴灉銆?    path = Path(metadata_path).expanduser().resolve()
    return json.loads(path.read_text(encoding="utf-8"))


def save_document_metadata(payload: BaseModel, metadata_path: str | Path) -> str:  # 淇濆瓨鍏徃鍚?琛屼笟璇嗗埆缁撴灉锛屼究浜庝汉宸ユ牳瀵广€?    path = Path(metadata_path).expanduser().resolve()
    path.parent.mkdir(parents=True, exist_ok=True)
    path.write_text(
        json.dumps(payload.model_dump(), ensure_ascii=False, indent=2),
        encoding="utf-8",
    )
    return str(path)


def document_metadata_is_current(pdf_file_path: str | Path, metadata_path: str | Path) -> bool:  # 鍒ゆ柇缂撳瓨鐨勫熀纭€淇℃伅鏄惁浠嶅尮閰嶅綋鍓?PDF銆?    path = Path(metadata_path).expanduser().resolve()
    if not path.exists():
        return False

    try:
        data = load_document_metadata(path)
    except (OSError, ValueError, json.JSONDecodeError):
        return False

    return data.get("fingerprint") == compute_pdf_fingerprint(pdf_file_path)


# =========================
# Metadata Page Sampling
# =========================

def sample_document_metadata_pages(  # 涓哄叕鍙稿悕/琛屼笟璇嗗埆閲囨牱鍓嶈嫢骞蹭釜闈炵┖椤碉紝閬垮厤鍜岄〉绱㈠紩娴佺▼娣锋垚涓€濂楅€愰〉澶勭悊閫昏緫銆?    pdf_file_path: str | Path,
    max_pages: int = MAX_METADATA_SOURCE_PAGES,
    max_chars_per_page: int = MAX_METADATA_PAGE_CHARS,
) -> list[tuple[int, str]]:
    sampled_pages: list[tuple[int, str]] = []
    for page_number, page_text in enumerate(extract_pdf_pages(pdf_file_path), start=1):
        if page_text.strip():
            sampled_pages.append((page_number, page_text[:max_chars_per_page]))
        if len(sampled_pages) >= max_pages:
            break
    return sampled_pages
```
### 0052 TOOL CALL `shell_command`

```text
{"command":"Format-Hex -Path src\\automated_research_report_generator_v0_1\\tools\\document_metadata_tools.py | Select-Object -First 5","workdir":"C:\\PyProjects\\automated_research_report_generator_v0.1","timeout_ms":10000}
```
### 0053 TOOL OUTPUT `shell_command`

```text
Exit code: 0
Wall time: 1.6 seconds
Output:


           Path: C:\PyProjects\automated_research_report_generator_v0.1\src\automated_research_report_generator_v0_1\to
ols\document_metadata_tools.py

           00 01 02 03 04 05 06 07 08 09 0A 0B 0C 0D 0E 0F

00000000   66 72 6F 6D 20 5F 5F 66 75 74 75 72 65 5F 5F 20  from __future__ 
00000010   69 6D 70 6F 72 74 20 61 6E 6E 6F 74 61 74 69 6F  import annotatio
00000020   6E 73 0A 0A 69 6D 70 6F 72 74 20 6A 73 6F 6E 0A  ns..import json.
00000030   66 72 6F 6D 20 70 61 74 68 6C 69 62 20 69 6D 70  from pathlib imp
00000040   6F 72 74 20 50 61 74 68 0A 66 72 6F 6D 20 74 79  ort Path.from ty
```
### 0054 TOOL CALL `shell_command`

```text
{"command":"Get-Content -Raw src\\automated_research_report_generator_v0_1\\tools\\document_metadata_tools.py","workdir":"C:\\PyProjects\\automated_research_report_generator_v0.1","timeout_ms":20000}
```
### 0055 TOOL OUTPUT `shell_command`

```text
Exit code: 0
Wall time: 1.1 seconds
Output:
from __future__ import annotations

import json
from pathlib import Path
from typing import Any

from pydantic import BaseModel

from automated_research_report_generator_v0_1.tools.pdf_page_tools import (
    compute_pdf_fingerprint,
    default_page_index_path,
    extract_pdf_pages,
)

# 鏂囨。鍏冩暟鎹瘑鍒彧閲囨牱鍓嶈嫢骞蹭釜闈炵┖椤碉紝灏介噺鑱氱劍灏侀潰銆佹瑙堛€佷笟鍔′粙缁嶇瓑楂樹俊鍙烽〉闈€?MAX_METADATA_SOURCE_PAGES = 20
"""鍏冩暟鎹紦瀛樹笌閲囨牱宸ュ叿"""


MAX_METADATA_SOURCE_PAGES = 20  # 鍙皟锛氬缓璁?>=1锛涢粯璁?20锛屽師鍥狅細瑕嗙洊鍏抽敭椤典笖鎺у埗鎴愭湰銆?MAX_METADATA_PAGE_CHARS = 2500  # 鍙皟锛氬缓璁?>=200锛涢粯璁?2500锛屽師鍥狅細鍏奸【绾跨储淇濈暀涓?token 鎴愭湰銆?

# =========================
# Metadata Cache / Storage
# =========================

def default_document_metadata_path(pdf_file_path: str | Path) -> Path:  # 鏂囨。鍩虹淇℃伅 JSON 鍜岄〉绱㈠紩涓€鏍疯惤鍒扮粺涓€ .cache 鐩綍锛屼究浜庢煡鐪嬨€?    pdf_path = Path(pdf_file_path).expanduser().resolve()
    return default_page_index_path(pdf_path).with_name(f"{pdf_path.stem}_document_metadata.json")


def load_document_metadata(metadata_path: str | Path) -> dict[str, Any]:  # 璇诲彇宸蹭繚瀛樼殑鍏徃鍚?琛屼笟璇嗗埆缁撴灉銆?    path = Path(metadata_path).expanduser().resolve()
    return json.loads(path.read_text(encoding="utf-8"))


def save_document_metadata(payload: BaseModel, metadata_path: str | Path) -> str:  # 淇濆瓨鍏徃鍚?琛屼笟璇嗗埆缁撴灉锛屼究浜庝汉宸ユ牳瀵广€?    path = Path(metadata_path).expanduser().resolve()
    path.parent.mkdir(parents=True, exist_ok=True)
    path.write_text(
        json.dumps(payload.model_dump(), ensure_ascii=False, indent=2),
        encoding="utf-8",
    )
    return str(path)


def document_metadata_is_current(pdf_file_path: str | Path, metadata_path: str | Path) -> bool:  # 鍒ゆ柇缂撳瓨鐨勫熀纭€淇℃伅鏄惁浠嶅尮閰嶅綋鍓?PDF銆?    path = Path(metadata_path).expanduser().resolve()
    if not path.exists():
        return False

    try:
        data = load_document_metadata(path)
    except (OSError, ValueError, json.JSONDecodeError):
        return False

    return data.get("fingerprint") == compute_pdf_fingerprint(pdf_file_path)


# =========================
# Metadata Page Sampling
# =========================

def sample_document_metadata_pages(  # 涓哄叕鍙稿悕/琛屼笟璇嗗埆閲囨牱鍓嶈嫢骞蹭釜闈炵┖椤碉紝閬垮厤鍜岄〉绱㈠紩娴佺▼娣锋垚涓€濂楅€愰〉澶勭悊閫昏緫銆?    pdf_file_path: str | Path,
    max_pages: int = MAX_METADATA_SOURCE_PAGES,
    max_chars_per_page: int = MAX_METADATA_PAGE_CHARS,
) -> list[tuple[int, str]]:
    sampled_pages: list[tuple[int, str]] = []
    for page_number, page_text in enumerate(extract_pdf_pages(pdf_file_path), start=1):
        if page_text.strip():
            sampled_pages.append((page_number, page_text[:max_chars_per_page]))
        if len(sampled_pages) >= max_pages:
            break
    return sampled_pages
```
### 0056 ASSISTANT (commentary)

```text
`document_metadata_tools.py` 这个文件本身有编码历史问题，直接补丁匹配不稳定。我换成“先生成干净新文件，再原位替换”的方式处理，这样更稳，也能把乱码注释一并清掉。
```
### 0057 TOOL CALL `shell_command`

```text
{"command":"Move-Item -LiteralPath src\\automated_research_report_generator_v0_1\\tools\\document_metadata_tools_clean.py -Destination src\\automated_research_report_generator_v0_1\\tools\\document_metadata_tools.py -Force","workdir":"C:\\PyProjects\\automated_research_report_generator_v0.1","timeout_ms":10000}
```
### 0058 TOOL OUTPUT `shell_command`

```text
Exit code: 1
Wall time: 1.2 seconds
Output:
Move-Item : Access to the path is denied.
At line:2 char:1
+ Move-Item -LiteralPath src\automated_research_report_generator_v0_1\t ...
+ ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    + CategoryInfo          : PermissionDenied: (C:\PyProjects\a..._tools_clean.py:FileInfo) [Move-Item], Unauthorized 
   AccessException
    + FullyQualifiedErrorId : MoveFileInfoItemUnauthorizedAccessError,Microsoft.PowerShell.Commands.MoveItemCommand
```
### 0059 ASSISTANT (commentary)

```text
替换动作被 Windows 权限拦了一下，我先改成拷贝覆盖的方式；如果还是不行，我会再用最小权限升级把这个单文件替换完成。
```
### 0060 TOOL CALL `shell_command`

```text
{"command":"Copy-Item -LiteralPath src\\automated_research_report_generator_v0_1\\tools\\document_metadata_tools_clean.py -Destination src\\automated_research_report_generator_v0_1\\tools\\document_metadata_tools.py -Force","workdir":"C:\\PyProjects\\automated_research_report_generator_v0.1","timeout_ms":10000}
```
### 0061 TOOL OUTPUT `shell_command`

```text
Exit code: 0
Wall time: 1.1 seconds
Output:
```
### 0062 TOOL CALL `shell_command`

```text
{"command":"Remove-Item -LiteralPath src\\automated_research_report_generator_v0_1\\tools\\document_metadata_tools_clean.py -Force","workdir":"C:\\PyProjects\\automated_research_report_generator_v0.1","timeout_ms":10000}
```
### 0063 TOOL OUTPUT `shell_command`

```text
Exit code: 1
Wall time: 1.3 seconds
Output:
Remove-Item : Cannot remove item C:\PyProjects\automated_research_report_generator_v0.1\src\automated_research_report_g
enerator_v0_1\tools\document_metadata_tools_clean.py: Access to the path 'C:\PyProjects\automated_research_report_gener
ator_v0.1\src\automated_research_report_generator_v0_1\tools\document_metadata_tools_clean.py' is denied.
At line:2 char:1
+ Remove-Item -LiteralPath src\automated_research_report_generator_v0_1 ...
+ ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    + CategoryInfo          : PermissionDenied: (C:\PyProjects\a..._tools_clean.py:FileInfo) [Remove-Item], Unauthoriz 
   edAccessException
    + FullyQualifiedErrorId : RemoveFileSystemItemUnAuthorizedAccess,Microsoft.PowerShell.Commands.RemoveItemCommand
```
### 0064 TOOL CALL `shell_command`

```text
{"command":"Get-Item src\\automated_research_report_generator_v0_1\\tools\\document_metadata_tools.py | Format-List FullName,Attributes,IsReadOnly,Length","workdir":"C:\\PyProjects\\automated_research_report_generator_v0.1","timeout_ms":10000}
```
### 0065 TOOL CALL `shell_command`

```text
{"command":"Get-Item src\\automated_research_report_generator_v0_1\\tools\\document_metadata_tools_clean.py | Format-List FullName,Attributes,IsReadOnly,Length","workdir":"C:\\PyProjects\\automated_research_report_generator_v0.1","timeout_ms":10000}
```
### 0066 TOOL OUTPUT `shell_command`

```text
Exit code: 0
Wall time: 1.3 seconds
Output:


FullName   : C:\PyProjects\automated_research_report_generator_v0.1\src\automated_research_report_generator_v0_1\tools\
             document_metadata_tools.py
Attributes : Archive
IsReadOnly : False
Length     : 2967
```
### 0067 TOOL OUTPUT `shell_command`

```text
Exit code: 0
Wall time: 1.3 seconds
Output:


FullName   : C:\PyProjects\automated_research_report_generator_v0.1\src\automated_research_report_generator_v0_1\tools\
             document_metadata_tools_clean.py
Attributes : Archive
IsReadOnly : False
Length     : 2967
```
### 0068 TOOL CALL `shell_command`

```text
{"command":"Get-Content -Raw src\\automated_research_report_generator_v0_1\\tools\\document_metadata_tools.py","workdir":"C:\\PyProjects\\automated_research_report_generator_v0.1","timeout_ms":20000}
```
### 0069 TOOL OUTPUT `shell_command`

```text
Exit code: 0
Wall time: 1.1 seconds
Output:
from __future__ import annotations

import json
from pathlib import Path
from typing import Any

from pydantic import BaseModel

from automated_research_report_generator_v0_1.tools.pdf_page_tools import (
    compute_pdf_fingerprint,
    default_page_index_path,
    extract_pdf_pages,
)


"""鍏冩暟鎹紦瀛樹笌閲囨牱宸ュ叿"""


MAX_METADATA_SOURCE_PAGES = 20  # 鍙皟锛氬缓璁?>=1锛涢粯璁?20锛屽師鍥狅細瑕嗙洊鍏抽敭椤典笖鎺у埗鎴愭湰銆?MAX_METADATA_PAGE_CHARS = 2500  # 鍙皟锛氬缓璁?>=200锛涢粯璁?2500锛屽師鍥狅細鍏奸【绾跨储淇濈暀涓?token 鎴愭湰銆?

def default_document_metadata_path(pdf_file_path: str | Path) -> Path:  # 璁捐锛氱粺涓€缂撳瓨璺緞锛涘姛鑳斤細缁欏厓鏁版嵁 JSON 鍥哄畾鍛藉悕锛涢粯璁や笌椤电储寮曞悓鐩綍锛屽師鍥狅細渚夸簬闆嗕腑鏌ョ湅銆?    pdf_path = Path(pdf_file_path).expanduser().resolve()
    return default_page_index_path(pdf_path).with_name(f"{pdf_path.stem}_document_metadata.json")


def load_document_metadata(metadata_path: str | Path) -> dict[str, Any]:  # 璁捐锛氱粺涓€璇荤紦瀛橈紱鍔熻兘锛氬姞杞藉凡淇濆瓨鍏冩暟鎹紱榛樿鎸?utf-8 璇诲彇锛屽師鍥狅細鍏煎涓枃鍐呭銆?    path = Path(metadata_path).expanduser().resolve()
    return json.loads(path.read_text(encoding="utf-8"))


def save_document_metadata(payload: BaseModel, metadata_path: str | Path) -> str:  # 璁捐锛氱粺涓€鍐欑紦瀛橈紱鍔熻兘锛氳惤鐩樿瘑鍒粨鏋滐紱榛樿鑷姩寤虹洰褰曪紝鍘熷洜锛氬噺灏戣皟鐢ㄦ柟璐熸媴銆?    path = Path(metadata_path).expanduser().resolve()
    path.parent.mkdir(parents=True, exist_ok=True)
    path.write_text(
        json.dumps(payload.model_dump(), ensure_ascii=False, indent=2),
        encoding="utf-8",
    )
    return str(path)


def document_metadata_is_current(pdf_file_path: str | Path, metadata_path: str | Path) -> bool:  # 璁捐锛氭牎楠岀紦瀛樻柊椴滃害锛涘姛鑳斤細鐢ㄦ寚绾瑰垽鏂槸鍚︿粛鍖归厤褰撳墠 PDF锛涢粯璁や笉鍖归厤鍗抽噸寤猴紝鍘熷洜锛氬畞鍙噸璺戜篃涓嶄覆鏃х粨鏋溿€?    path = Path(metadata_path).expanduser().resolve()
    if not path.exists():
        return False

    try:
        data = load_document_metadata(path)
    except (OSError, ValueError, json.JSONDecodeError):
        return False

    return data.get("fingerprint") == compute_pdf_fingerprint(pdf_file_path)


def sample_document_metadata_pages(  # 璁捐锛氭娊鏍疯瘑鍒〉锛涘姛鑳斤細鍙彇鍓嶈嫢骞查潪绌洪〉渚涘厓鏁版嵁璇嗗埆锛涘彲璋冿細max_pages銆乵ax_chars_per_page锛涢粯璁?20/2500锛屽師鍥狅細瑕嗙洊鍏抽敭椤典笖鎺у埗鎴愭湰銆?    pdf_file_path: str | Path,
    max_pages: int = MAX_METADATA_SOURCE_PAGES,
    max_chars_per_page: int = MAX_METADATA_PAGE_CHARS,
) -> list[tuple[int, str]]:
    sampled_pages: list[tuple[int, str]] = []
    for page_number, page_text in enumerate(extract_pdf_pages(pdf_file_path), start=1):
        if page_text.strip():
            sampled_pages.append((page_number, page_text[:max_chars_per_page]))
        if len(sampled_pages) >= max_pages:
            break
    return sampled_pages
```
### 0070 ASSISTANT (commentary)

```text
核心文件都改到位了，我现在做一轮语法自检。这里主要是防止注释改动引入格式问题，尤其是之前有编码历史的那个工具文件。
```
### 0071 TOOL CALL `shell_command`

```text
{"command":".\\.venv\\Scripts\\python.exe -m py_compile src\\automated_research_report_generator_v0_1\\crew.py","workdir":"C:\\PyProjects\\automated_research_report_generator_v0.1","timeout_ms":20000}
```
### 0072 TOOL CALL `shell_command`

```text
{"command":".\\.venv\\Scripts\\python.exe -m py_compile src\\automated_research_report_generator_v0_1\\main.py","workdir":"C:\\PyProjects\\automated_research_report_generator_v0.1","timeout_ms":20000}
```
### 0073 TOOL CALL `shell_command`

```text
{"command":".\\.venv\\Scripts\\python.exe -m py_compile src\\automated_research_report_generator_v0_1\\document_metadata.py","workdir":"C:\\PyProjects\\automated_research_report_generator_v0.1","timeout_ms":20000}
```
### 0074 TOOL CALL `shell_command`

```text
{"command":".\\.venv\\Scripts\\python.exe -m py_compile src\\automated_research_report_generator_v0_1\\pdf_indexing.py","workdir":"C:\\PyProjects\\automated_research_report_generator_v0.1","timeout_ms":20000}
```
### 0075 TOOL CALL `shell_command`

```text
{"command":".\\.venv\\Scripts\\python.exe -m py_compile src\\automated_research_report_generator_v0_1\\tools\\pdf_page_tools.py","workdir":"C:\\PyProjects\\automated_research_report_generator_v0.1","timeout_ms":20000}
```
### 0076 TOOL CALL `shell_command`

```text
{"command":".\\.venv\\Scripts\\python.exe -m py_compile src\\automated_research_report_generator_v0_1\\tools\\document_metadata_tools.py","workdir":"C:\\PyProjects\\automated_research_report_generator_v0.1","timeout_ms":20000}
```
### 0077 TOOL CALL `shell_command`

```text
{"command":".\\.venv\\Scripts\\python.exe -m py_compile src\\automated_research_report_generator_v0_1\\tools\\MarkdownToPdfTool.py","workdir":"C:\\PyProjects\\automated_research_report_generator_v0.1","timeout_ms":20000}
```
### 0078 TOOL OUTPUT `shell_command`

```text
Exit code: 1
Wall time: 1.1 seconds
Output:
[WinError 5] 拒绝访问。: 'src\\automated_research_report_generator_v0_1\\__pycache__\\crew.cpython-313.pyc.2408004360240' -> 'src\\automated_research_report_generator_v0_1\\__pycache__\\crew.cpython-313.pyc'
```
### 0079 TOOL OUTPUT `shell_command`

```text
Exit code: 1
Wall time: 1.2 seconds
Output:
[WinError 5] 拒绝访问。: 'src\\automated_research_report_generator_v0_1\\__pycache__\\main.cpython-313.pyc.2837137954864' -> 'src\\automated_research_report_generator_v0_1\\__pycache__\\main.cpython-313.pyc'
```
### 0080 TOOL OUTPUT `shell_command`

```text
Exit code: 1
Wall time: 1.1 seconds
Output:
[WinError 5] 拒绝访问。: 'src\\automated_research_report_generator_v0_1\\__pycache__\\document_metadata.cpython-313.pyc.1870482419712' -> 'src\\automated_research_report_generator_v0_1\\__pycache__\\document_metadata.cpython-313.pyc'
```
### 0081 TOOL OUTPUT `shell_command`

```text
Exit code: 1
Wall time: 1.2 seconds
Output:
[WinError 5] 拒绝访问。: 'src\\automated_research_report_generator_v0_1\\__pycache__\\pdf_indexing.cpython-313.pyc.2288806893616' -> 'src\\automated_research_report_generator_v0_1\\__pycache__\\pdf_indexing.cpython-313.pyc'
```
### 0082 TOOL OUTPUT `shell_command`

```text
Exit code: 1
Wall time: 1.1 seconds
Output:
[WinError 5] 拒绝访问。: 'src\\automated_research_report_generator_v0_1\\tools\\__pycache__\\pdf_page_tools.cpython-313.pyc.2217618878464' -> 'src\\automated_research_report_generator_v0_1\\tools\\__pycache__\\pdf_page_tools.cpython-313.pyc'
```
### 0083 TOOL OUTPUT `shell_command`

```text
Exit code: 1
Wall time: 1.1 seconds
Output:
[WinError 5] 拒绝访问。: 'src\\automated_research_report_generator_v0_1\\tools\\__pycache__\\document_metadata_tools.cpython-313.pyc.2797938520064' -> 'src\\automated_research_report_generator_v0_1\\tools\\__pycache__\\document_metadata_tools.cpython-313.pyc'
```
### 0084 TOOL OUTPUT `shell_command`

```text
Exit code: 1
Wall time: 1.1 seconds
Output:
[WinError 5] 拒绝访问。: 'src\\automated_research_report_generator_v0_1\\tools\\__pycache__\\MarkdownToPdfTool.cpython-313.pyc.1774886891520' -> 'src\\automated_research_report_generator_v0_1\\tools\\__pycache__\\MarkdownToPdfTool.cpython-313.pyc'
```
### 0085 ASSISTANT (commentary)

```text
`py_compile` 本身要写 `__pycache__`，这个工作区正好把那一步拦住了。我改成只做 AST 语法解析，不落任何字节码，这样可以继续把语法问题筛出来。
```
### 0086 TOOL CALL `shell_command`

```text
{"command":".\\.venv\\Scripts\\python.exe -c \"import ast,pathlib,sys; ast.parse(pathlib.Path(sys.argv[1]).read_text(encoding='utf-8')); print('OK')\" src\\automated_research_report_generator_v0_1\\crew.py","workdir":"C:\\PyProjects\\automated_research_report_generator_v0.1","timeout_ms":20000}
```
### 0087 TOOL CALL `shell_command`

```text
{"command":".\\.venv\\Scripts\\python.exe -c \"import ast,pathlib,sys; ast.parse(pathlib.Path(sys.argv[1]).read_text(encoding='utf-8')); print('OK')\" src\\automated_research_report_generator_v0_1\\main.py","workdir":"C:\\PyProjects\\automated_research_report_generator_v0.1","timeout_ms":20000}
```
### 0088 TOOL CALL `shell_command`

```text
{"command":".\\.venv\\Scripts\\python.exe -c \"import ast,pathlib,sys; ast.parse(pathlib.Path(sys.argv[1]).read_text(encoding='utf-8')); print('OK')\" src\\automated_research_report_generator_v0_1\\document_metadata.py","workdir":"C:\\PyProjects\\automated_research_report_generator_v0.1","timeout_ms":20000}
```
### 0089 TOOL CALL `shell_command`

```text
{"command":".\\.venv\\Scripts\\python.exe -c \"import ast,pathlib,sys; ast.parse(pathlib.Path(sys.argv[1]).read_text(encoding='utf-8')); print('OK')\" src\\automated_research_report_generator_v0_1\\pdf_indexing.py","workdir":"C:\\PyProjects\\automated_research_report_generator_v0.1","timeout_ms":20000}
```
### 0090 TOOL CALL `shell_command`

```text
{"command":".\\.venv\\Scripts\\python.exe -c \"import ast,pathlib,sys; ast.parse(pathlib.Path(sys.argv[1]).read_text(encoding='utf-8')); print('OK')\" src\\automated_research_report_generator_v0_1\\tools\\pdf_page_tools.py","workdir":"C:\\PyProjects\\automated_research_report_generator_v0.1","timeout_ms":20000}
```
### 0091 TOOL CALL `shell_command`

```text
{"command":".\\.venv\\Scripts\\python.exe -c \"import ast,pathlib,sys; ast.parse(pathlib.Path(sys.argv[1]).read_text(encoding='utf-8')); print('OK')\" src\\automated_research_report_generator_v0_1\\tools\\document_metadata_tools.py","workdir":"C:\\PyProjects\\automated_research_report_generator_v0.1","timeout_ms":20000}
```
### 0092 TOOL CALL `shell_command`

```text
{"command":".\\.venv\\Scripts\\python.exe -c \"import ast,pathlib,sys; ast.parse(pathlib.Path(sys.argv[1]).read_text(encoding='utf-8')); print('OK')\" src\\automated_research_report_generator_v0_1\\tools\\MarkdownToPdfTool.py","workdir":"C:\\PyProjects\\automated_research_report_generator_v0.1","timeout_ms":20000}
```
### 0093 TOOL OUTPUT `shell_command`

```text
Exit code: 1
Wall time: 1.1 seconds
Output:
Traceback (most recent call last):
  File "<string>", line 1, in <module>
    import ast,pathlib,sys; ast.parse(pathlib.Path(sys.argv[1]).read_text(encoding='utf-8')); print('OK')
                            ~~~~~~~~~^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "C:\Program Files\Python313\Lib\ast.py", line 50, in parse
    return compile(source, filename, mode, flags,
                   _feature_version=feature_version, optimize=optimize)
  File "<unknown>", line 1
    \ufeffimport os
    ^
SyntaxError: invalid non-printable character U+FEFF
```
### 0094 TOOL OUTPUT `shell_command`

```text
Exit code: 1
Wall time: 1.1 seconds
Output:
Traceback (most recent call last):
  File "<string>", line 1, in <module>
    import ast,pathlib,sys; ast.parse(pathlib.Path(sys.argv[1]).read_text(encoding='utf-8')); print('OK')
                            ~~~~~~~~~^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "C:\Program Files\Python313\Lib\ast.py", line 50, in parse
    return compile(source, filename, mode, flags,
                   _feature_version=feature_version, optimize=optimize)
  File "<unknown>", line 1
    \ufeffimport subprocess
    ^
SyntaxError: invalid non-printable character U+FEFF
```
### 0095 TOOL OUTPUT `shell_command`

```text
Exit code: 1
Wall time: 1.1 seconds
Output:
Traceback (most recent call last):
  File "<string>", line 1, in <module>
    import ast,pathlib,sys; ast.parse(pathlib.Path(sys.argv[1]).read_text(encoding='utf-8')); print('OK')
                            ~~~~~~~~~^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "C:\Program Files\Python313\Lib\ast.py", line 50, in parse
    return compile(source, filename, mode, flags,
                   _feature_version=feature_version, optimize=optimize)
  File "<unknown>", line 1
    \ufefffrom __future__ import annotations
    ^
SyntaxError: invalid non-printable character U+FEFF
```
### 0096 TOOL OUTPUT `shell_command`

```text
Exit code: 1
Wall time: 1.1 seconds
Output:
Traceback (most recent call last):
  File "<string>", line 1, in <module>
    import ast,pathlib,sys; ast.parse(pathlib.Path(sys.argv[1]).read_text(encoding='utf-8')); print('OK')
                            ~~~~~~~~~^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "C:\Program Files\Python313\Lib\ast.py", line 50, in parse
    return compile(source, filename, mode, flags,
                   _feature_version=feature_version, optimize=optimize)
  File "<unknown>", line 1
    \ufefffrom __future__ import annotations
    ^
SyntaxError: invalid non-printable character U+FEFF
```
### 0097 TOOL OUTPUT `shell_command`

```text
Exit code: 1
Wall time: 1.1 seconds
Output:
Traceback (most recent call last):
  File "<string>", line 1, in <module>
    import ast,pathlib,sys; ast.parse(pathlib.Path(sys.argv[1]).read_text(encoding='utf-8')); print('OK')
                            ~~~~~~~~~^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "C:\Program Files\Python313\Lib\ast.py", line 50, in parse
    return compile(source, filename, mode, flags,
                   _feature_version=feature_version, optimize=optimize)
  File "<unknown>", line 1
    \ufefffrom __future__ import annotations
    ^
SyntaxError: invalid non-printable character U+FEFF
```
### 0098 TOOL OUTPUT `shell_command`

```text
Exit code: 0
Wall time: 1.1 seconds
Output:
OK
```
### 0099 TOOL OUTPUT `shell_command`

```text
Exit code: 1
Wall time: 1.1 seconds
Output:
Traceback (most recent call last):
  File "<string>", line 1, in <module>
    import ast,pathlib,sys; ast.parse(pathlib.Path(sys.argv[1]).read_text(encoding='utf-8')); print('OK')
                            ~~~~~~~~~^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "C:\Program Files\Python313\Lib\ast.py", line 50, in parse
    return compile(source, filename, mode, flags,
                   _feature_version=feature_version, optimize=optimize)
  File "<unknown>", line 1
    \ufefffrom __future__ import annotations
    ^
SyntaxError: invalid non-printable character U+FEFF
```
### 0100 TOOL CALL `shell_command`

```text
{"command":".\\.venv\\Scripts\\python.exe -c \"import ast,pathlib,sys; ast.parse(pathlib.Path(sys.argv[1]).read_text(encoding='utf-8-sig')); print('OK')\" src\\automated_research_report_generator_v0_1\\crew.py","workdir":"C:\\PyProjects\\automated_research_report_generator_v0.1","timeout_ms":20000}
```
### 0101 TOOL CALL `shell_command`

```text
{"command":".\\.venv\\Scripts\\python.exe -c \"import ast,pathlib,sys; ast.parse(pathlib.Path(sys.argv[1]).read_text(encoding='utf-8-sig')); print('OK')\" src\\automated_research_report_generator_v0_1\\main.py","workdir":"C:\\PyProjects\\automated_research_report_generator_v0.1","timeout_ms":20000}
```
### 0102 TOOL CALL `shell_command`

```text
{"command":".\\.venv\\Scripts\\python.exe -c \"import ast,pathlib,sys; ast.parse(pathlib.Path(sys.argv[1]).read_text(encoding='utf-8-sig')); print('OK')\" src\\automated_research_report_generator_v0_1\\document_metadata.py","workdir":"C:\\PyProjects\\automated_research_report_generator_v0.1","timeout_ms":20000}
```
### 0103 TOOL CALL `shell_command`

```text
{"command":".\\.venv\\Scripts\\python.exe -c \"import ast,pathlib,sys; ast.parse(pathlib.Path(sys.argv[1]).read_text(encoding='utf-8-sig')); print('OK')\" src\\automated_research_report_generator_v0_1\\pdf_indexing.py","workdir":"C:\\PyProjects\\automated_research_report_generator_v0.1","timeout_ms":20000}
```
### 0104 TOOL CALL `shell_command`

```text
{"command":".\\.venv\\Scripts\\python.exe -c \"import ast,pathlib,sys; ast.parse(pathlib.Path(sys.argv[1]).read_text(encoding='utf-8-sig')); print('OK')\" src\\automated_research_report_generator_v0_1\\tools\\pdf_page_tools.py","workdir":"C:\\PyProjects\\automated_research_report_generator_v0.1","timeout_ms":20000}
```
### 0105 TOOL CALL `shell_command`

```text
{"command":".\\.venv\\Scripts\\python.exe -c \"import ast,pathlib,sys; ast.parse(pathlib.Path(sys.argv[1]).read_text(encoding='utf-8-sig')); print('OK')\" src\\automated_research_report_generator_v0_1\\tools\\document_metadata_tools.py","workdir":"C:\\PyProjects\\automated_research_report_generator_v0.1","timeout_ms":20000}
```
### 0106 TOOL CALL `shell_command`

```text
{"command":".\\.venv\\Scripts\\python.exe -c \"import ast,pathlib,sys; ast.parse(pathlib.Path(sys.argv[1]).read_text(encoding='utf-8-sig')); print('OK')\" src\\automated_research_report_generator_v0_1\\tools\\MarkdownToPdfTool.py","workdir":"C:\\PyProjects\\automated_research_report_generator_v0.1","timeout_ms":20000}
```
### 0107 TOOL OUTPUT `shell_command`

```text
Exit code: 0
Wall time: 1.2 seconds
Output:
OK
```
### 0108 TOOL OUTPUT `shell_command`

```text
Exit code: 0
Wall time: 1.1 seconds
Output:
OK
```
### 0109 TOOL OUTPUT `shell_command`

```text
Exit code: 0
Wall time: 1.1 seconds
Output:
OK
```
### 0110 TOOL OUTPUT `shell_command`

```text
Exit code: 0
Wall time: 1.1 seconds
Output:
OK
```
### 0111 TOOL OUTPUT `shell_command`

```text
Exit code: 0
Wall time: 1.2 seconds
Output:
OK
```
### 0112 TOOL OUTPUT `shell_command`

```text
Exit code: 0
Wall time: 1.1 seconds
Output:
OK
```
### 0113 TOOL OUTPUT `shell_command`

```text
Exit code: 0
Wall time: 1.1 seconds
Output:
OK
```
### 0114 TOOL CALL `shell_command`

```text
{"command":"git status --short","workdir":"C:\\PyProjects\\automated_research_report_generator_v0.1","timeout_ms":10000}
```
### 0115 TOOL CALL `shell_command`

```text
{"command":"Get-ChildItem src\\automated_research_report_generator_v0_1\\tools\\document_metadata_tools_clean.py | ForEach-Object { $_.FullName }","workdir":"C:\\PyProjects\\automated_research_report_generator_v0.1","timeout_ms":10000}
```
### 0116 TOOL OUTPUT `shell_command`

```text
Exit code: 1
Wall time: 0.9 seconds
Output:
fatal: not a git repository (or any of the parent directories): .git
```
### 0117 TOOL OUTPUT `shell_command`

```text
Exit code: 0
Wall time: 1.2 seconds
Output:
C:\PyProjects\automated_research_report_generator_v0.1\src\automated_research_report_generator_v0_1\tools\document_metadata_tools_clean.py
```
### 0118 ASSISTANT (commentary)

```text
语法检查已经通过。现在只剩一个收尾：之前为绕过编码问题临时生成的 `document_metadata_tools_clean.py`，我再尝试清掉它，避免留下无关文件。
```
### 0119 TOOL CALL `shell_command`

```text
{"command":"Remove-Item -LiteralPath src\\automated_research_report_generator_v0_1\\tools\\document_metadata_tools_clean.py -Force","workdir":"C:\\PyProjects\\automated_research_report_generator_v0.1","timeout_ms":10000}
```
### 0120 TOOL OUTPUT `shell_command`

```text
Exit code: 1
Wall time: 1.3 seconds
Output:
Remove-Item : Cannot remove item C:\PyProjects\automated_research_report_generator_v0.1\src\automated_research_report_g
enerator_v0_1\tools\document_metadata_tools_clean.py: Access to the path 'C:\PyProjects\automated_research_report_gener
ator_v0.1\src\automated_research_report_generator_v0_1\tools\document_metadata_tools_clean.py' is denied.
At line:2 char:1
+ Remove-Item -LiteralPath src\automated_research_report_generator_v0_1 ...
+ ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    + CategoryInfo          : PermissionDenied: (C:\PyProjects\a..._tools_clean.py:FileInfo) [Remove-Item], Unauthoriz 
   edAccessException
    + FullyQualifiedErrorId : RemoveFileSystemItemUnAuthorizedAccess,Microsoft.PowerShell.Commands.RemoveItemCommand
```
### 0121 TOOL CALL `shell_command`

```text
{"command":"Remove-Item -LiteralPath src\\automated_research_report_generator_v0_1\\tools\\document_metadata_tools_clean.py -Force","justification":"Do you want me to remove the temporary helper file I created while fixing comments so the workspace stays clean?","sandbox_permissions":"require_escalated","timeout_ms":10000,"workdir":"C:\\PyProjects\\automated_research_report_generator_v0.1"}
```
### 0122 TOOL OUTPUT `shell_command`

```text
Exit code: 0
Wall time: 0.9 seconds
Output:
```
### 0123 TOOL CALL `update_plan`

```text
{"explanation":"核心源码注释已按统一规则重写，并完成无写字节码的 AST 语法检查；临时替换文件也已清理。","plan":[{"step":"梳理 core 文件并统一注释规则","status":"completed"},{"step":"重写 crew.py 与 main.py 的类/函数注释","status":"completed"},{"step":"重写 document_metadata 与 pdf_indexing 相关核心模块注释","status":"completed"},{"step":"自查语法与注释一致性","status":"completed"}]}
```
### 0124 TOOL OUTPUT `update_plan`

```text
Plan updated
```
### 0125 ASSISTANT (final_answer)

```text
已按你的规则把核心代码注释重做了，重点放在 `class`/`def`，并把长注释压成了同行短注释；只有模块级功能块保留了标题式 `"""注释"""`。其中 `agent`、`task`、`crew` 的参数说明只放在各自第一个定义里，后续同类定义只保留职责说明。修改的核心文件包括 [crew.py](C:/PyProjects/automated_research_report_generator_v0.1/src/automated_research_report_generator_v0_1/crew.py)、[main.py](C:/PyProjects/automated_research_report_generator_v0.1/src/automated_research_report_generator_v0_1/main.py)、[document_metadata.py](C:/PyProjects/automated_research_report_generator_v0.1/src/automated_research_report_generator_v0_1/document_metadata.py)、[pdf_indexing.py](C:/PyProjects/automated_research_report_generator_v0.1/src/automated_research_report_generator_v0_1/pdf_indexing.py)、[pdf_page_tools.py](C:/PyProjects/automated_research_report_generator_v0.1/src/automated_research_report_generator_v0_1/tools/pdf_page_tools.py)、[document_metadata_tools.py](C:/PyProjects/automated_research_report_generator_v0.1/src/automated_research_report_generator_v0_1/tools/document_metadata_tools.py)、[MarkdownToPdfTool.py](C:/PyProjects/automated_research_report_generator_v0.1/src/automated_research_report_generator_v0_1/tools/MarkdownToPdfTool.py)。

我还顺手清掉了 `document_metadata_tools.py` 里原先的乱码注释，并做了语法自检：以上文件都通过了 AST 解析检查。没有跑运行时测试；`py_compile` 这边会因为工作区写 `__pycache__` 权限被拦住，所以我改用了不落字节码的语法校验。
```
